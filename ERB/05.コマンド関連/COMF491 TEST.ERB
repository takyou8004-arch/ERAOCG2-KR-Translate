;-------------------------------------------------
;TEST用
;-------------------------------------------------
@COM491
#DIM DYNAMIC 캐릭터_ID
#DIM DYNAMIC カードコスト,10

;CALL CASINO_ENTRANCE

;詰め플래그 = 1
;CALL DUEL_SYSTEM(1,952);
;詰め플래그 = 0
;CFLAG:1:현재위치 = CFLAG:MASTER:현재위치
;CFLAG:2:현재위치 = CFLAG:MASTER:현재위치
;CFLAG:3:현재위치 = CFLAG:MASTER:현재위치
CFLAG:TARGET:호감도 = 2000


;-------------------------------------------------
;実行판정
;-------------------------------------------------
@COM_ABLE491
;デバッグ中のみ표시
SIF デバッグシステム != 1
	RETURN 0
RETURN 1
