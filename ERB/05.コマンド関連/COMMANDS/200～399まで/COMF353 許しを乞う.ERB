;용서를 구한다
@COM353

RETURN 1

;-------------------------------------------------
;実行판정
;-------------------------------------------------
;용서를 구한다
@COM_ABLE353
;고백한다実行판정
SIF !TFLAG:100
	RETURN 0
;一括관리
SIF GLOBAL_COMABLE(353)
	RETURN RESULT
;二人きりでないとだめ
SIF GET_TARGETNUM() > 1
	RETURN 0
;친밀감강도1
SIF TCVAR:MASTER:친밀감 < 1
	RETURN 0
;수면中
SIF CFLAG:수면
	RETURN 0
;기력0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
RETURN 1