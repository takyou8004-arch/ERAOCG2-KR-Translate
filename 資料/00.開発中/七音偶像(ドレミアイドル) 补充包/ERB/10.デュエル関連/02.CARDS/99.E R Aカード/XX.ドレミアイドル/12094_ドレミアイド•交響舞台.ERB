@CARDNAME_12094,参照先
;ココで지정卡片の名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = ドレミアイドル•交響무대
	CASE "略称"
		RESULTS = ドレミアイドル무대
	CASE "カテゴリ"
		RESULTS:0 = 도레미코드
		RESULTS:1 = ドレミアイドル
		RESULTS:2 = E.R.A
ENDSELECT


@CARD_12094(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN フィールド
ENDSELECT

RETURN 0

@CARD_EXPLANATION_12094(종류)
#DIM DYNAMIC 종류

PRINTL 自己的场땅变为「ドレミアイドル•交響무대」。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ①：这场卡在필드表側표시存在的场合，自己필드的「ドレミアイドル」灵摆怪兽卡获得以下效果
PRINTL   ●这张卡进行战斗的场合才能发动
PRINTL    将这张卡在自己的마법•陷阱ゾーン表側표시放置，本次战斗无效，那只怪兽变为「ドレミアイドル推し」。
PRINTL    那只怪兽已经是「ドレミアイドル推し」的场合、获得其控制权。
CALLFORM CARD_ABLE(1,효과後強制誘発)
PRINTL ②：这场卡在필드表側표시存在的场合，对手특수召唤怪兽时发动，那只怪兽变成变成随机「ドレミアイドル推し」

PRINTL （我方필드有「ドレミアイドル」怪兽卡存在的场合，상대필드对应「ドレミアイドル推し」怪兽效果、不能攻击）

@CARDCAN_12094(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ


CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,12094)
SIF RESULT == 1
	RETURN 0

IF 종류 == 마함起動 || 종류 == 손패起動
	IF 종류 == 마함起動
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
			RETURN 0
	ENDIF
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1
	SIF 마함数(듀얼리스트) == 5
		RETURN 0
	RETURN 1
ELSEIF 종류 == 지속효과
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 효과後強制誘発
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") >= 2
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_FIN_DATA(듀얼리스트,"소환全般",1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_12094(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC 효과先

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 마함起動 || 종류 == 손패起動
	;卡片发动＆설치まで
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,12094,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所,마함行き)
	ENDIF

ELSEIF 종류 == 지속효과
	CALL SET_FIELD_MAGIC_INFO(듀얼리스트,"ドレミアイドル•交響무대")

ELSEIF 종류 == 효과後強制誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,12094,2)
	IF RESULT == 0
		효과先 = FINAL_SUMMON_TARGET(듀얼리스트,1)
		IF 효과先 != 5 && 효과先 != -1
			TRYCALLFORM CARDNAME_{GETVAR(@"%対面者%_필드:효과先")}, "名前"
				SETCOLOR C_PINK
			SELECTCASE RAND:7	
				CASE 0
					PRINTFORML
					PRINTFORML 「ドレミアイドル」们的演出深深感染了在%RESULTS%，让其变成了「丘蒂娅推し」！
					PRINTFORMW
					CALL CARD_SET_CATEGORY(対面者,"필드",효과先,"丘蒂娅推し")
				CASE 1
					PRINTFORML
					PRINTFORML 「ドレミアイドル」们的演出深深感染了在%RESULTS%，让其变成了「德莉米娅推し」！
					PRINTFORMW 
					CALL CARD_SET_CATEGORY(対面者,"필드",효과先,"德莉米娅推し")
				CASE 2
					PRINTFORML
					PRINTFORML 「ドレミアイドル」们的演出深深感染了在%RESULTS%，让其变成了「埃莉蒂娅推し」！
					PRINTFORMW 
					CALL CARD_SET_CATEGORY(対面者,"필드",효과先,"埃莉蒂娅推し")
				CASE 3
					PRINTFORML
					PRINTFORML 「ドレミアイドル」们的演出深深感染了在%RESULTS%，让其变成了「凡西娅推し」！
					PRINTFORMW 
					CALL CARD_SET_CATEGORY(対面者,"필드",효과先,"凡西娅推し")
				CASE 4
					PRINTFORML
					PRINTFORML 「ドレミアイドル」们的演出深深感染了在%RESULTS%，让其变成了「格蕾西娅推し」！
					PRINTFORMW 
					CALL CARD_SET_CATEGORY(対面者,"필드",효과先,"格蕾西娅推し")
				CASE 5
					PRINTFORML
					PRINTFORML 「ドレミアイドル」们的演出深深感染了在%RESULTS%，让其变成了「安琪莉娅推し」！
					PRINTFORMW 
					CALL CARD_SET_CATEGORY(対面者,"필드",효과先,"安琪莉娅推し")
				CASE 6
					PRINTFORML
					PRINTFORML 「ドレミアイドル」们的演出深深感染了在%RESULTS%，让其变成了「比蒂娅推し」！
					PRINTFORMW 
					CALL CARD_SET_CATEGORY(対面者,"필드",효과先,"比蒂娅推し")
			ENDSELECT
				RESETCOLOR
		ENDIF
	ENDIF
ENDIF
