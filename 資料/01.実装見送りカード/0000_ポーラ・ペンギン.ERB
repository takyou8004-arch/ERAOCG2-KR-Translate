@CARDNAME_0000,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 極氷수ポーラ・ペンギン
	CASE "略称"
		RESULTS = ポーラ・ペンギン
	CASE "カテゴリ"
		RESULTS:0 = ペンギン
ENDSELECT


@CARD_0000(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 물속성
	CASE "종족"
		RETURN 비행야수족
	CASE "レベル"
		RETURN 3
	CASE "攻撃力"
		RETURN 800
	CASE "守備力"
		RETURN 1000
	CASE "튜너"
		RETURN 1
ENDSELECT

RETURN 0

@CARD_EXPLANATION_0000(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①②의 효과는 １턴에 １번、어느쪽이든 １개밖에 사용할 수 없다。
PRINTL ①：この카드가 특수 소환에 성공했을 경우、
PRINTL 상대 필드의 몬스터 １体を대상として발동할 수 있다。
PRINTL 그 몬스터를 주인의 패로 되돌린다。
PRINTL ②：필드의 この카드가 戦闘・효과로 파괴된 경우、
PRINTL 상대 필드의 카드  １장을 대상として발동할 수 있다。
PRINTL その카드를 주인의 패로 되돌린다。


@CARDCAN_0000(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,0000)
SIF RESULT == 1
	RETURN 0

IF 종류 == 소환時 || 종류 == 특수소환時
	SIF 발동済_1(듀얼리스트,0000)
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_0000(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード

IF 종류 == 소환時 || 종류 == 특수소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,0000,)
	IF RESULT == 0
	ENDIF
ENDIF

