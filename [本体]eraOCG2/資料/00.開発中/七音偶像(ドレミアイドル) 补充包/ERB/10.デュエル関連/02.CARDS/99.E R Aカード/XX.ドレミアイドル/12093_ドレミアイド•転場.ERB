@CARDNAME_12093,参照先
;ココで지정卡片の名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = ドレミアイドル•転場
	CASE "略称"
		RESULTS = ドレミアイドル転場
	CASE "カテゴリ"
		RESULTS:0 = 도레미코드
		RESULTS:1 = ドレミアイドル
		RESULTS:2 = E.R.A
ENDSELECT


@CARD_12093(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 速攻마법
ENDSELECT

RETURN 0

@CARD_EXPLANATION_12093(종류)
#DIM DYNAMIC 종류

PRINTL 这个卡名的卡在1回合只能发动1张。
CALLFORM CARD_ABLE(1,마함起動 ,손패起動 ,フリー誘発)
PRINTL ①：以필드的1张「도레미코드」怪兽卡为对象才能发动
PRINTL    那张卡回到手卡，从卡组특수召唤1只「도레미코드」怪兽

@CARDCAN_12093(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所


CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,12093)
SIF RESULT == 1
	RETURN 0


IF 종류 == 마함起動 || 종류 == 손패起動 || 종류 == フリー誘発
	IF ゾーン == "손패"
		SIF ターンプレイヤー != 듀얼리스트
			RETURN 0
	ELSEIF ゾーン == "마함"
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 3
			RETURN 0
	ELSE
		RETURN 0
	ENDIF
	SIF 발동済_1(듀얼리스트,12093)
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲마함 = 1
	효과条件종류 = モン全般
	효과条件カテゴリ = 도레미코드
	효과条件표시 = 表側표시
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = 도레미코드
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_12093(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC 调教对象
#DIM DYNAMIC 実行役대상

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 마함起動 || 종류 == 손패起動 || 종류 == フリー誘発
	;卡片发动確認
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,12093,1)
	IF RESULT == 0
		;通常마법汎用処理
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		
		CALL FULLRESET
		효과範囲필드 = 1
		SIF モンスター数(듀얼리스트) < 5
			효과範囲마함 = 1
		효과条件종류 = モン全般
		효과条件カテゴリ = 도레미코드
		효과条件표시 = 表側표시
		효과条件行先플래그 = 손패行き
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)

		CALL CARD_EROTICS(RESULT:1)
		IF RESULT != -1
			调教对象 = RESULT

			CALL CARD_SS_RESTRICTION(듀얼리스트)
			CALL CARD_EROTICS(상대プレイヤー,対面者)
			実行役대상 = RESULT

			CALL FULLRESET
			효과範囲덱 = 1
			효과条件カテゴリ = 도레미코드
			효과条件종류 = モン全般
			CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
			SIF 调教对象 != -1 && 実行役대상 != -1
				CALL DoSol_business(実行役대상,调教对象)
		ENDIF
	ENDIF
ENDIF


@DoSol_business(实行대상,调教对象)
#DIM DYNAMIC 调教对象
#DIM DYNAMIC 实行대상
#DIM DYNAMIC 调教强度
#DIM DYNAMIC 호감도补正

IF TALENT:调教对象:연모 >= 2
	호감도补正 = 5
	调教强度 = 500
ELSEIF TALENT:调教对象:연모 >= 1
	호감도补正 = 3
	调教强度 = 300
ELSEIF CFLAG:调教对象:호감도 > 1000
	호감도补正 = 2
	调教强度 = 200
ELSE
	호감도补正 = 1
	调教强度 = 100
ENDIF

调教强度 = 调教强度 * (80+RAND:40) / 100

	SETCOLOR C_PINK
PRINTFORML
PRINTFORML  为了姐妹们的出场，秘密的枕营业在后台中进行着
PRINTFORMW 

SELECTCASE RAND:호감도补正
	CASE 0
		IF TALENT:调教对象:연모 >= 2
			PRINTFORML
			PRINTFORML  %CALLNAME:调教对象%爱怜땅侍奉着 %CALLNAME:实行대상%的肉棒
			PRINTFORML  左手握住 %CALLNAME:实行대상%的肉杆、右手掌心包裹住龟头转动摩擦着
			PRINTFORMW
			PRINTFORML 「정액❤❤❤快点射出来吧❤❤❤」
			PRINTFORML  %CALLNAME:调教对象%노출了绝不能被粉丝们看到的迷乱神情
			PRINTFORMW
		ELSEIF TALENT:调教对象:연모 >= 1
			PRINTFORML
			PRINTFORML  %CALLNAME:调教对象%专心致志땅侍奉着 %CALLNAME:实行대상%的肉棒
			PRINTFORML  左手握住 %CALLNAME:实行대상%的肉杆、右手掌心包裹住龟头转动摩擦着
			PRINTFORMW 
			PRINTFORML 「加油、加油、快点射出来❤」
			PRINTFORML  %CALLNAME:调教对象%鼓励着 %CALLNAME:实行대상%사정
			PRINTFORMW
		ELSEIF CFLAG:调教对象:호감도 > 1000
			PRINTFORML
			PRINTFORML  %CALLNAME:调教对象%白嫩的双手在 %CALLNAME:实行대상%的肉棒上来回摩擦着
			PRINTFORMW 
			PRINTFORML 「快点射出来、别耽误下一场演出啊」
			PRINTFORML  %CALLNAME:调教对象%全神贯注땅服侍着、看样子已经习惯了
			PRINTFORMW
		ELSE
			PRINTFORML
			PRINTFORML  %CALLNAME:调教对象%白嫩的双手在 %CALLNAME:实行대상%的肉棒上来回摩擦着
			PRINTFORMW 
			PRINTFORML 「真是的、快点射出来啊、耽误下一场演出怎么办啊」
			PRINTFORML  %CALLNAME:调教对象%满脸厌恶땅进行着服侍
			PRINTFORMW
		ENDIF
		CALL EROTIC_MOVE("手交侍奉",调教对象,实行대상,调教强度)
	CASE 1
		IF TALENT:调教对象:연모 >= 2
			PRINTFORML
			PRINTFORML  %CALLNAME:调教对象%轻吻 %CALLNAME:实行대상%的肉棒尖端
			PRINTFORML  「今天也要让小鸡鸡好好舒服哦❤❤❤」
			PRINTFORMW
			PRINTFORML  %CALLNAME:调教对象%的樱唇将肉棒顶端包裹，用혀尖不断逗弄着龟头
			PRINTFORML  抬头仰望着 %CALLNAME:实行대상%，노출迷乱的神情催促사정
			PRINTFORMW
		ELSEIF TALENT:调教对象:연모 >= 1
			PRINTFORML
			PRINTFORML  %CALLNAME:调教对象%专心致志땅吞吐着 %CALLNAME:实行대상%的肉棒
			PRINTFORML  粉嫩的혀尖不断挑逗着龟头、带来强烈的쾌락
			PRINTFORMW 
			PRINTFORML 「快点、射出来吧❤」
			PRINTFORML  %CALLNAME:调教对象%艰难땅发出声音、催促 %CALLNAME:实行대상%사정
			PRINTFORMW
		ELSEIF CFLAG:调教对象:호감도 > 1000
			PRINTFORML
			PRINTFORML  %CALLNAME:调教对象%满脸厌恶땅吞吐着 %CALLNAME:实行대상%的肉棒
			PRINTFORMW 
			PRINTFORML 「快点射出来、别耽误下一场演出啊」
			PRINTFORML  %CALLNAME:调教对象%全神贯注땅服侍着
			PRINTFORMW
		ENDIF
		CALL EROTIC_MOVE("口交侍奉",调教对象,实行대상,调教强度)
	CASE 2
		IF TALENT:调教对象:가슴사이즈 > 0
			IF TALENT:调教对象:연모 >= 2
				PRINTFORML
				PRINTFORML  %CALLNAME:调教对象%捧起双乳、形成一道诱人的沟壑
				PRINTFORML  随即肉棒被完全淹没在 %CALLNAME:调教对象%가슴部之海
				PRINTFORMW
				PRINTFORML 	「小鸡鸡“溺水”了呢❤❤❤」
				PRINTFORML  %CALLNAME:调教对象%两眼迷离땅看着 %CALLNAME:实行대상%、双乳上下交错着
				PRINTFORMW
			ELSEIF TALENT:调教对象:연모 >= 1
				PRINTFORML
				PRINTFORML  %CALLNAME:调教对象%捧起双乳、形成一道诱人的沟壑
				PRINTFORML  「把小鸡鸡塞进来吧❤」
				PRINTFORMW 
				PRINTFORML  %CALLNAME:实行대상%땅肉棒被完全淹没在 %CALLNAME:调教对象%가슴部之中
				PRINTFORML  %CALLNAME:调教对象%双乳上下摆弄着、催促 %CALLNAME:实行대상%사정
				PRINTFORMW
			ENDIF
			CALL EROTIC_MOVE("乳交侍奉",调教对象,实行대상,调教强度)
		ELSE
			IF TALENT:调教对象:연모 >= 2
				PRINTFORML
				PRINTFORML 「今天也会好好欺负 %CALLNAME:实行대상%的变态小鸡鸡的❤❤❤」
				PRINTFORML  %CALLNAME:调教对象%一边对 %CALLNAME:实行대상%노출轻蔑的眼神
				IF 调教对象 == NO_TO_CHARANUM(2086)
					PRINTFORML  一边将粉色丝袜包裹的双足贴在 %CALLNAME:实行대상%的肉棒上
				ELSEIF 调教对象 == NO_TO_CHARANUM(2087)
					PRINTFORML  一边将黑丝双足贴在 %CALLNAME:实行대상%的肉棒上
				ELSE
					PRINTFORML  一边将如玉的裸足贴在 %CALLNAME:实行대상%的肉棒上
				ENDIF
				PRINTFORMW 
				PRINTFORML 「变态小鸡鸡要坚持久一点哦❤❤❤」
				PRINTFORML  一只脚沿着阴茎来回摩擦，另一只脚的足尖则在龟头上盘旋揉动着
				PRINTFORMW
			ELSEIF TALENT:调教对象:연모 >= 1
				PRINTFORML
				PRINTFORML 「 %CALLNAME:实行대상%真的是个变态呢❤」
				PRINTFORML  %CALLNAME:调教对象%对 %CALLNAME:实行대상%노출鄙视的眼神
				IF 调教对象 == NO_TO_CHARANUM(2086)
					PRINTFORML  但还是将粉色丝袜包裹的双足贴在 %CALLNAME:实行대상%的肉棒上
				ELSEIF 调教对象 == NO_TO_CHARANUM(2087)
					PRINTFORML  但还是将黑丝双足贴在 %CALLNAME:实行대상%的肉棒上
				ELSE
					PRINTFORML  但还是将如玉的裸足贴在 %CALLNAME:实行대상%的肉棒上
				ENDIF
				PRINTFORMW 
				PRINTFORML 「真是的、快点射出来吧」
				PRINTFORML  %CALLNAME:调教对象%的双足包裹住 %CALLNAME:实行대상%的阴茎、来回摩擦着
				PRINTFORMW
			ENDIF
			CALL EROTIC_MOVE("足交侍奉",调教对象,实行대상,调教强度)
		ENDIF
	CASE 3
		PRINTFORML
		PRINTFORML 「 %CALLNAME:实行대상%的肉棒、最棒了❤❤❤」
		PRINTFORML  %CALLNAME:调教对象%爱怜땅看着 %CALLNAME:实行대상%的肉棒
		PRINTFORMW 
		PRINTFORML  看着 %CALLNAME:调教对象%积极主动的样子、这还能算枕营业吗？
		PRINTFORML 「这种事怎么都好啦」
		PRINTFORMW
		IF TALENT:调教对象:가슴사이즈 > 0
			PRINTFORML  肉棒被淹没在 %CALLNAME:调教对象%가슴部之中、仅仅노출了尖端
			PRINTFORML  %CALLNAME:调教对象%用혀尖逗弄着 %CALLNAME:调教对象%的龟头顶部
			PRINTFORML 「快点射出来吧」
			PRINTFORMW
		ELSE
			PRINTFORML  肉棒仅贴在 %CALLNAME:调教对象%가슴前、但确实挤不出乳沟
			PRINTFORML  %CALLNAME:调教对象%樱唇将肉棒顶端包裹、上下晃动着身体让가슴部摩擦着肉棒
			PRINTFORML 「快点射出来吧」
			PRINTFORMW
		ENDIF
		CALL EROTIC_MOVE("乳口侍奉",调教对象,实行대상,调教强度)
	CASE 4
		PRINTFORML
		PRINTFORML 「来、做吧❤❤❤」
		PRINTFORML  %CALLNAME:调教对象%把 %CALLNAME:实行대상%推倒、跨坐到 %CALLNAME:实行대상%身上
		PRINTFORMW 
		PRINTFORML 「不本番怎么算得上枕营业呢？」
		PRINTFORML  看着 %CALLNAME:调教对象%积极主动的样子、这还能算是枕营业吗？
		PRINTFORMW
		PRINTFORML  %CALLNAME:调教对象%爱怜땅看着 %CALLNAME:实行대상%的肉棒、引导其插入小穴
		PRINTFORML  时而上下摇摆身体、时而盘旋扭动허리部、这场淫靡之舞还要持续很久
		PRINTFORMW
		CALL EROTIC_MOVE("기승위",调教对象,实行대상,调教强度)
ENDSELECT

	SETCOLOR C_PINK
IF RESULT:1 > 0
	PRINTFORML
	PRINTFORML 「已经、不行了❤」
	PRINTFORML  明明是枕营业、 %CALLNAME:调教对象%还是满脸迷醉땅沉溺在쾌락之中
	PRINTFORMW
	调教强度 = 调教强度 * 2
ENDIF
	RESETCOLOR

CALL EVENT_FAVUP(调教对象,调教强度 / 10)


RETURN