@CARDNAME_99070,参照先
;ココで指定卡片の名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 迷乱的蜜穴
	CASE "略称"
		RESULTS = 迷乱蜜穴
	CASE "カテゴリ"
		RESULTS:0 = 蜜穴
		RESULTS:1 = E.R.A
ENDSELECT


@CARD_99070(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 通常罠
ENDSELECT

RETURN 0

@CARD_EXPLANATION_99070(種類)
#DIM DYNAMIC 種類

CALL TEXT_DECORATION("ROGUE")
PRINTL このカード名の①②の効果は1ターンに1度、いずれか1つしか使用できない。
CALLFORM CARD_ABLE(1,魔罠起動 ,手札起動)
PRINTL ①：自分フィールドモンスタなし或只有「惑乱魔」モンスタ的场合,このカードは手札から発動でき。
PRINTL 「惑乱魔」モンスター1体をデッキから守備表示特殊召喚する,この効果で特殊召喚したモンスターの攻撃力・守備力は0になる。
CALLFORM CARD_ABLE(2,墓地起動,フリー誘発)
PRINTL ②：墓地のこのカードを除外して発動できる。
PRINTL 自分フィールド1只「惑乱魔」モンスタ手札行き,自分墓地1只「惑乱魔」モンスタ特殊召唤。
CALL TEXT_DECORATION("ROGUE")

@CARDCAN_99070(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC 效果限制达成
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件達成

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,99070)
SIF RESULT == 1
	RETURN 0

IF 種類 == 魔罠起動 || 種類 == 手札起動
	IF 種類 == 手札起動
		CALL FULLRESET
		効果範囲戦場 = 1
		効果条件カテゴリ = 惑乱魔
		CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
		SIF RESULT == 1
		条件達成 ++

		CALL FULLRESET
		効果範囲戦場 = 1
		効果条件カテゴリ = 惑乱魔
		CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
		SIF RESULT != 1
			条件達成 ++
		SIF 条件達成 == 0
			RETURN 0

	ELSEIF 種類 == 魔罠起動
		SIF GETVAR(@"%決闘者%_魔_表:場所") == 3
			RETURN 0
	ENDIF

	SIF 発動済_1(決闘者,99070)
		RETURN 0
	SIF 発動済_2(決闘者,99070)
		RETURN 0
	SIF モンスター数(決闘者) >= 5
		RETURN 0
	RETURN 1		

ELSEIF 種類 == 墓地起動 || (種類 == フリー誘発 && ゾーン == "墓地")
	SIF 発動済_1(決闘者,99070)
		RETURN 0
	SIF 発動済_2(決闘者,99070)
		RETURN 0
	SIF GETVAR(@"%決闘者%_除外NG置換") >= 1
		RETURN 0

	CALL FULLRESET
	効果範囲戦場 = 1
	効果範囲墓地 = 1
	効果条件カテゴリ = 惑乱魔
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_99070(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 选择卡片
#DIM DYNAMIC 设定レベル



CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 魔罠起動 || 種類 == 手札起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,99070,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		効果範囲デッキ = 1
		効果条件カテゴリ = 惑乱魔
		効果条件種類 = モン全般
		効果追加種類 = 190
		CALL CARD_SP_SUMMON_ADV(決闘者,ゾーン,場所,"守備表示")
	ENDIF

ELSEIF 種類 == 墓地起動 || (種類 == フリー誘発 && ゾーン == "墓地")
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,99070,2)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		効果範囲戦場 = 1
		効果条件カテゴリ = 惑乱魔
		効果条件行先フラグ = 手札行き
		CALL CARD_ADD_GY_ADV(決闘者,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			効果範囲墓地 = 1
			効果条件カテゴリ = 惑乱魔
			効果条件種類 = モン全般
			CALL CARD_SP_SUMMON_ADV(決闘者,ゾーン,場所,"攻撃表示")
		ENDIF
	ENDIF
ENDIF








