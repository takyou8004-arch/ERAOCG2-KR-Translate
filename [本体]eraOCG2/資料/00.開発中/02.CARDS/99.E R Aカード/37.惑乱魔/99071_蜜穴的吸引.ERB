@CARDNAME_99071,参照先
;ココで指定卡片の名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 蜜穴的吸引
	CASE "略称"
		RESULTS = 蜜穴吸引
	CASE "カテゴリ"
		RESULTS:0 = 蜜穴
		RESULTS:1 = E.R.A
ENDSELECT


@CARD_99071(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 通常罠
ENDSELECT

RETURN 0

@CARD_EXPLANATION_99071(種類)
#DIM DYNAMIC 種類

CALL TEXT_DECORATION("ROGUE")
CALLFORM CARD_ABLE(0,魔罠起動,フリー誘発)
PRINTL ①：自分戦場の「惑乱魔」モンスタ1枚と相手戦場のモンスター1枚を対象として発動できる。
PRINTL 対象となる相手モンスターの攻撃力は、自分が対象とするモンスターの攻撃力の数値を下げる。
PRINTL この効果で対象の持有性别的モンスターの攻撃力が0に低下した場合,そのモンスタ주인의 魔法＆罠ゾーンに「蜜罐」にして表側表示で置く。
PRINTL 性別なし的场合,そのモンスタ破坏。
CALLFORM CARD_ABLE(1,墓地起動,フリー誘発)
PRINTL ②：墓地のこのカードを除外し相手フィールド1枚「蜜罐」和自分戦場1只「惑乱魔」怪兽为对象才能发动。
PRINTL このターン、その自分の「惑乱魔」モンスターは戦闘では破壊されない。
CALL TEXT_DECORATION("ROGUE")



@CARDCAN_99071(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC 效果限制达成
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件達成

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,99071)
SIF RESULT == 1
	RETURN 0

IF 種類 == 魔罠起動 || 種類 == フリー誘発
	SIF ゾーン != "魔罠"
		RETURN 0
	SIF GETVAR(@"%決闘者%_魔_表:場所") == 3
		RETURN 0

	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件カテゴリ = 惑乱魔
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0

	CALL FULLRESET
	効果範囲相手戦場 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1     
    
ELSEIF 種類 == 墓地起動 || (種類 == フリー誘発 && ゾーン == "墓地")
	SIF GETVAR(@"%決闘者%_除外NG置換") >= 1
		RETURN 0
		
	CALL FULLRESET
	効果範囲戦場 = 1
	効果範囲魔罠 = 1
	効果範囲相手戦場 = 1
	効果範囲相手魔罠 = 1
	効果条件カテゴリ = 蜜罐
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
    SIF RESULT != 1
		RETURN 0
    RETURN 1
ENDIF

@CARDEFFECT_99071(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 选择卡片
#DIM DYNAMIC 选择对方卡片
#DIM DYNAMIC 調教対象
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 守備力修正

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 魔罠起動 || 種類 == フリー誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,99071)
	IF RESULT == 0
        CALL CARD_DISPOSE(決闘者,ゾーン,場所)
        CALL FULLRESET
        効果範囲戦場 = 1
        効果条件カテゴリ = 惑乱魔
        CALL CARD_CHOOSE_BASIC(決闘者,"戦場",ゾーン,場所)
        选择卡片 = RESULT
        IF 选择卡片 != 9999
        	攻撃力修正 = GETVAR(@"%決闘者%_MZ_守:选择卡片")
        	CALL FULLRESET
            効果範囲相手戦場 = 1
            CALL CARD_CHOOSE_BASIC(決闘者,"相手戦場",ゾーン,場所)
            选择对方卡片 = RESULT
            IF 选择对方卡片 != 9999
            	CALL CARD_REMOVAL_BASIC(決闘者,"自動処理",选择对方卡片,"永攻DOWN",99071,攻撃力修正)
                TRYCALLFORM CARDNAME_{GETVAR(@"%対面者%_戦場:选择对方卡片")}("名前")
                調教対象 = GETVAR(@"%対面者%_戦場:选择对方卡片")
				TRYCALLFORM CARD_{GETVAR(@"%対面者%_戦場:选择对方卡片")}, "性別"
                IF RESULT == 牝性 || RESULT == ふたなり || RESULT == 雄性 ;&& (GETVAR(@"%対面者%_MZ_攻:选择对方卡片") - 攻撃力修正) <= 0
                	CALL FULLRESET
					PRINTL 选择的怪兽攻撃力降至了0,要让那只怪兽作为「蜜罐」在对方魔罠区放置吗?
					PRINTL [1] - 是
					PRINTL [2] - 但是我拒绝
					BINPUT
					IF RESULT == 1
                		効果追加カテ = 蜜罐
                		CALL CARD_ADD(対面者,"戦場","魔罠",选择对方卡片,"表側表示")
                
                    	CALL CARD_EROTICS(調教対象)
                    	IF RESULT != -1
                        	調教対象 = RESULT
                        	SETCOLOR C_PINK
                        	PRINTFORML
                        	PRINTFORMW %CALLNAME:調教対象%在惑乱魔的调教下屈服了,被改造成了生产"花蜜"的蜜罐……！
                        	PRINTFORML
                        	RESETCOLOR
                        	CALL EROTIC_MOVE("苗床捕縛",調教対象, -1, 100)
                    	ENDIF
					ELSE
						CALL CARD_EROTICS(調教対象)
						IF RESULT != -1
							調教対象 = RESULT
							SETCOLOR C_PINK
							PRINTFORML
							PRINTFORMW %CALLNAME:調教対象%的力量在调教中被惑乱魔吸走了！
							PRINTFORML
							RESETCOLOR
							CALL EROTIC_MOVE("全身レズ愛撫",調教対象, -1, 50)
						ENDIF
					ENDIF
                ELSEIF (GETVAR(@"%対面者%_MZ_攻:选择对方卡片") - 攻撃力修正) <= 0
					CALL FULLRESET
					CALL CARD_REMOVAL_BASIC(決闘者,"自動処理",选择卡片,"墓地",99071)
                ENDIF
            ENDIF
        ENDIF
    ENDIF

ELSEIF 種類 == 墓地起動 || (種類 == フリー誘発 && ゾーン == "墓地")
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,99071)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
    	CALL FULLRESET
		効果範囲戦場 = 1
		効果範囲魔罠 = 1
		効果範囲相手戦場 = 1
		効果範囲相手魔罠 = 1
		効果条件カテゴリ = 蜜罐
		CALL CARD_CHOOSE_BASIC(決闘者,"对方魔罠",ゾーン,場所)
		选择对方卡片 = RESULT
		IF 选择对方卡片 != 9999
			CALL FULLRESET
			効果範囲戦場 = 1
			効果条件カテゴリ = 惑乱魔
			CALL CARD_CHOOSE_BASIC(決闘者,"戦場",ゾーン,場所)
			选择卡片 = RESULT
			IF 选择卡片 != 9999
				TRYCALLFORM CARDNAME_{GETVAR(@"%対面者%_魔罠:选择对方卡片")}("名前")
				調教対象 = GETVAR(@"%対面者%_魔罠:选择对方卡片")
				CALL CARD_EROTICS(調教対象)
				IF RESULT != -1
					調教対象 = RESULT
					SETCOLOR C_PINK
					PRINTFORML
					PRINTFORMW 惑乱魔从%CALLNAME:調教対象%的股间汲取了花蜜,体力得到了补充。
					PRINTFORML
					RESETCOLOR
					CALL EROTIC_MOVE("指入れ",調教対象, -1, 300)
				ENDIF
				CALL STATCHANGE_EFFECT(決闘者,"自身",选择卡片,"戦耐",1)
			ENDIF
		ENDIF
    ENDIF
ENDIF