@CARDNAME_91636,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 勅令の星輝兵
	CASE "略称"
		RESULTS = 勅令の星輝兵
	CASE "カテゴリ"
		RESULTS:0 = グレンディオス
		RESULTS:1 = 星輝兵
		RESULTS:2 = リンクジョーカー
		RESULTS:4 = ヴァンガード
ENDSELECT

@CARD_91636(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "外部者"
		RETURN 1
	CASE "種類"
		RETURN 通常罠
ENDSELECT
RETURN 0

@CARDSUMMON_91636(決闘者,召喚カード,EX参照)
#DIMS DYNAMIC 決闘者
#DIM DYNAMIC 召喚カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
RETURN 0

@CARD_EXPLANATION_91636(種類)
#DIM DYNAMIC 種類

PRINTL このカード名の①②の効果はそれぞれ１ターンに１度しか使用できない。
CALL CARD_ABLE(1,効果後誘発)
PRINTL ①：相手がモンスターを特殊召喚した場合に、自分フィールドに「グレンディオス」モンスターが存在するなら、
PRINTL 相手フィールドのモンスター１体と自分フィールドの「Я」モンスター１体を対象として発動できる。
PRINTL 対象のお互いのモンスターを呪縛する。
SETCOLOR C_GRAY
PRINTL （呪縛状態のモンスターは裏側守備表示となり、攻撃回避を得る。
PRINTL 　ただし、相手フィールドに呪縛状態のモンスターしか存在しない場合、自分フィールドのモンスターが相手プレイヤーに与える戦闘ダメージは半分になる）
RESETCOLOR
CALL CARD_ABLE(2,墓地起動,フリー誘発)
PRINTL ②：自分・相手バトルフェイズに、自分フィールドに「グレンディオス」モンスターが存在するなら発動できる。
PRINTL 墓地のこのカードを除外して、ターン終了時まで、自分の「星輝兵」モンスター全ての攻撃力を自分フィールドの「Я」モンスターの数×５００アップする。

@CARDCAN_91636(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ数

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,91636)
SIF RESULT == 1
	RETURN 0

IF 種類 == 効果後誘発
	SIF 発動済_1(決闘者,79736)
		RETURN 0
	SIF ゾーン != "魔罠"
		RETURN 0
	SIF GETVAR(@"%決闘者%_魔_表:場所") == 3	
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_FIN_DATA(決闘者,"特殊召喚",1)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件カテゴリ = グレンディオス
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件カテゴリ = Я
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 種類 == 墓地起動	|| 種類 == フリー誘発
	SIF 発動済_2(決闘者,79736)
		RETURN 0
	SIF ゾーン != "墓地"
		RETURN 0
	SIF バトルフェイズ != 1
		RETURN 0
	SIF GETVAR(@"%決闘者%_除外NG置換") >= 1
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件カテゴリ = グレンディオス
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件カテゴリ = Я
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
    RETURN 1
ENDIF

@CARDEFFECT_91636(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カードID
#DIM DYNAMIC 修正値

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 効果後誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,91636,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		効果範囲戦場 = 1
		効果条件カテゴリ = Я
		CALL CARD_REMOVAL_ADV(決闘者,"単体除去",場所,"選択")
		選択カード = RESULT:1
		IF RESULT == 1
			CALL STATCHANGE_EFFECT(決闘者,"自身",選択カード,"表",3)
			CALL STATCHANGE_EFFECT(決闘者,"自身",選択カード,"攻避",1)
			CALL FULLRESET
			効果範囲相手戦場 = 1
			効果条件表示 = 表側表示
			CALL CARD_REMOVAL_ADV(決闘者,"単体除去",場所,"表示変更")
		ENDIF
	ENDIF
ELSEIF 種類 == 墓地起動 || 種類 == フリー誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,91636,2)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		効果範囲戦場 = 1
		効果条件カテゴリ = Я
		効果条件CNT = 1
		CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
		修正値 = RESULT * 300
		CALL FULLRESET
		効果条件カテゴリ = 星輝兵
		CALL STATCHANGE_BASIC(決闘者,"戦場",場所,修正値)
	ENDIF
ENDIF