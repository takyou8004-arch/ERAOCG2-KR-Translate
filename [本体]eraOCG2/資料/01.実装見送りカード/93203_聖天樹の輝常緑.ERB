@CARDNAME_93203,参照先
;ココで지정カードの名前、略称を返す予定
;　
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = サンアバロン・グローリアス・グロース
	CASE "略称"
		RESULTS = 聖天樹の輝常緑
	CASE "カテゴリ"
		RESULTS:0 = サンアバロン
		RESULTS:1 = 게임 오리지널
ENDSELECT


@CARD_93203(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 지속함정
ENDSELECT

RETURN 0

@CARD_EXPLANATION_93203(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ②④의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
PRINTL ①：자신 필드에 "サンアバロン」Lモンスターが존재하지 않을 경우、この 카드는 파괴된다。
PRINTL ②：자신 필드에 "サンアバロン」Lモンスターが존재하고、
PRINTL 자신이 戦闘・효과で데미지를 受けた場合に발동할 수 있다。덱 밖에서 "聖種の지령」１体を生成し、
PRINTL 자신 필드의 몬스터 を素材として식물족L몬스터의 L소환を行う。
PRINTL ③：자신의 LPが回復했을 경우에 발동한다。そ의 수치分だけ상대에게 데미지를 준다。
PRINTL ④：상대の攻撃宣言時、자신 필드의 L몬스터 １体を대상として발동할 수 있다。
PRINTL 공격 대상을 그 몬스터に移し替えてダメージ計算を行う。


@CARDCAN_93203(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,93203)
SIF RESULT == 1
	RETURN 0

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 효과後誘発 || 종류 == 攻撃後強制誘発 || 종류 == 발동強制誘発
	SIF ゾーン != "마함"
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件종류 = 링크
	효과条件カテゴリ = サンアバロン
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	IF RESULT == 1
		IF 발동済_2(듀얼리스트,93203) != 1
			CALL CARD_CHECK_FIN_BIT(듀얼리스트,"被弾者")
			SIF RESULT != 1
				RETURN 0
		ELSE
			RETURN 0
		ENDIF
	ELSE
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
	ENDIF
	RETURN 1
ELSEIF 종류 == ライフ変強制誘発
	SIF ゾーン != "마함"
		RETURN 0
	SIF ダメージ増減 != "増加"
		RETURN 0
	SIF !GROUPMATCH(ダメージ종류,"효과")
		RETURN 0
	SIF ダメージ受者 != 듀얼리스트
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻撃時誘発
	SIF 발동済_4(듀얼리스트,93203)
		RETURN 0
	SIF ゾーン != "마함"
		RETURN 0
	SIF ターンプレイヤー == 듀얼리스트
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件종류 = 링크
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 마함起動
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 3	
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_93203(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 효과後誘発 || 종류 == 攻撃後強制誘発 || 종류 == 발동強制誘発
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件종류 = 링크
	효과条件カテゴリ = サンアバロン
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	IF RESULT == 1
		CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,93203,2)
		IF RESULT == 0
			CALL CARD_GENERATE(듀얼리스트,32200)
			CALL FULLRESET
			효과条件종족 = 식물족
			효과条件종류 = 링크
			효과条件소환可 = 1
			CALL CARD_CHOOSE_BASIC(듀얼리스트,"EX덱",ゾーン,場所)
			選択カード = RESULT
			IF 選択カード != 9999
				CALLFORM RESETCONDITION
				選択カード_ID = GETVAR(@"%듀얼리스트%_EX덱:選択カード")
				TRYCALLFORM CARDSUMMON_{選択カード_ID}(듀얼리스트,選択カード)
				TRYCALLFORM LINSUMMON(듀얼리스트,選択カード)
			ENDIF
		ENDIF
	ELSE
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
	ENDIF
ELSEIF 종류 == ライフ変強制誘発
	CALL LIFE_CHANGE(듀얼리스트,"減少","효과",ダメージ受値,1)
ELSEIF 종류 == 攻撃時誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,93203,4)
	IF RESULT == 0
		CALL FULLRESET
		효과条件종류 = 링크
		CALL CARD_CHOOSE_BASIC(듀얼리스트,"필드",ゾーン,場所)
		選択カード = RESULT
		IF 選択カード != 9999
			CALL COVER_ATTACK(듀얼리스트,選択カード)
		ENDIF
	ENDIF
ELSEIF 종류 == 마함起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,93203)
	IF RESULT == 0
	ENDIF
ENDIF
