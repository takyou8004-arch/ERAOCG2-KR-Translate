@CARDNAME_53607,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = RR－レヴォリューション・ファルコン
	CASE "略称"
		RESULTS = RRレヴォ・Ｆ
	CASE "カテゴリ"
		RESULTS:0 = レイドラプターズ
ENDSELECT


@CARD_53607(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 비행야수족
	CASE "レベル"
		RETURN 6
	CASE "攻撃力"
		RETURN 2000
	CASE "守備力"
		RETURN 3000
ENDSELECT
RETURN 0

@CARDSUMMON_53607(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要素材数 = 3
素材条件レベル = 6
素材条件종족 = 비행야수족

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,53607) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_53607(종류)
#DIM DYNAMIC 종류

PRINTL 비행야수족 레벨６몬스터×３
PRINTL 묘지에 ランク５이하의 「RR」Xモンスターが存在する경우、X소환한 この 카드는 覚醒状態となる。
PRINTL 이 카드명의 ①의 효과는 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,필드起動)
PRINTL ①：이 카드의 X소재를 １개 제거하고 발동할 수 있다。
PRINTL 이 턴、この 카드는 상대モンスター全てに１回ずつ攻撃할 수 있다。
CALLFORM CARD_ABLE(0,攻撃時強制誘発)
PRINTL ②：この카드가 특수 소환된 앞면 표시 몬스터와 전투를 행한 경우에 발동한다。
PRINTL 그 몬스터의 공격력 / 수비력を０으로 한다。
CALLFORM CARD_ABLE(10,필드起動)
PRINTL ③：１턴에 １번、覚醒状態の이 카드의 X소재를 １개 제거하고、상대필드의 몬스터 １体を대상として발동할 수 있다。
PRINTL 그 몬스터를 破壊し、その攻撃力の半分の데미지를 상대에게 준다。

@CARDCAN_53607(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,53607)
SIF RESULT == 1
	RETURN 0
IF 종류 == EX소환時強制 
	CALL FULLRESET
	효과範囲묘지 = 1
	효과条件종류 = 엑시즈
	효과条件カテゴリ = レイドラプターズ
	CALLFORM 효과条件レベル_ADV(5, "以下")
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 필드起動
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	IF GETVAR(@"%듀얼리스트%_MZ_条達:場所") != 0
		SIF 발동済_1(듀얼리스트,53607) && 효과발동済(듀얼리스트,場所,ゾーン)
			RETURN 0
	ELSE
		SIF 발동済_1(듀얼리스트,53607)
			RETURN 0
	ENDIF
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	SIF モンスター数(対面者) <= 0
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻撃時強制誘発
	SIF ゾーン != "필드"
		RETURN 0
	SIF 戦闘場所(듀얼리스트) != 場所
		RETURN 0
	SIF 戦闘場所(듀얼리스트,1) == 5
		RETURN 0
	SIF GETVAR(@"%対面者%_MZ_召種:%対面者%_戦闘カード") == 召種通常
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_53607(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力상승
#DIM 選択カード


CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == EX소환時強制 
	CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"条達",2,1)
ELSEIF 종류 == 필드起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,53607,0,X消費1)
	IF RESULT == 0
		IF GETVAR(@"%듀얼리스트%_MZ_条達:場所") != 0
			IF 발동済_1(듀얼리스트,53607) == 0 && 효과발동済(듀얼리스트,場所,ゾーン) == 0
				CALL YN_CHOICE(듀얼리스트,"①상대モンスター全てに１回ずつ攻撃できる","③몬스터를 破壊して攻撃力の半分のダメージ")
			ELSEIF 발동済_1(듀얼리스트,53607)
				RESULT = 1
			ELSE
				RESULT = 0
			ENDIF
		ELSE
			RESULT = 0
		ENDIF
		IF RESULT == 0
			CALL FULLRESET
			CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,53607,1)
			CALL STATCHANGE_EFFECT(듀얼리스트,"자신",場所,"連戦",1)
		ELSEIF RESULT == 1
			CALL FULLRESET
			CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,53607,10)
			효과추가종류 = 2
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드単体",場所,"묘지",53607)
		ENDIF
	ENDIF
ELSEIF 종류 == 攻撃時強制誘発
	CALL FULLRESET
	효과추가종류 = 10
	CALL CARD_REMOVAL_BASIC(듀얼리스트,"自動処理",戦闘場所(듀얼리스트,1),"永攻DOWN",99999)
ENDIF
