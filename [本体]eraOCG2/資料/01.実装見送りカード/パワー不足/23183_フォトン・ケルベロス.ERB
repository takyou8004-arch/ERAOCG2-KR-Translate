@CARDNAME_23183,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = フォトン・ケルベロス
	CASE "略称"
		RESULTS = Ｐｔケルベロス
	CASE "カテゴリ"
		RESULTS:0 = フォトン
ENDSELECT


@CARD_23183(参照先)
#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 効果モン
	CASE "属性"
		RETURN 光属性
	CASE "種族"
		RETURN 獣族
	CASE "レベル"
		RETURN 3
	CASE "攻撃力"
		RETURN 1300
	CASE "守備力"
		RETURN 600
ENDSELECT

RETURN 0

@CARD_EXPLANATION_23183(種類)
#DIM DYNAMIC 種類

CALLFORM CARD_ABLE(0,召喚時)
PRINTL ①：このカードが召喚した場合に発動できる。
PRINTL このカードはターン終了時まで覚醒状態となり、以下の効果を得る。
CALLFORM CARD_ABLE(0,発動誘発)
PRINTL ●相手が罠カードを発動した時に発動できる。その発動を無効にする。

@CARDCAN_23183(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,23183)
SIF RESULT == 1
	RETURN 0



IF 種類 == 召喚時
	RETURN 1
ELSEIF 種類 == 発動誘発
	SIF ゾーン != "戦場" 
		RETURN 0
	SIF GETVAR(@"%決闘者%_MZ_条達:場所") == 0
		RETURN 0
	CALL FULLRESET
	効果条件種類 = 罠全般
	CALL CARD_CHECK_START_DATA(決闘者,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_23183(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 選択カード

CALL 対面者判定(決闘者)
対面者 = %RESULTS%


IF 種類 == 召喚時
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,23183)
	IF RESULT == 0
		CALL SET_FIELD_INFO(決闘者,"戦場",場所,"条達",1,1)
	ENDIF
ELSEIF 種類 == 発動誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,23183)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(決闘者,"発動無効",場所,"無効")
	ENDIF
ENDIF

