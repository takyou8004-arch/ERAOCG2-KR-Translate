@CARDNAME_93171,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = フォトン・ライジング・ストリーム
	CASE "略称"
		RESULTS = 反射光子流
	CASE "カテゴリ"
		RESULTS:0 = フォトン
ENDSELECT


@CARD_93171(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"

		RETURN 通常罠
ENDSELECT

RETURN 0

@CARD_EXPLANATION_93171(種類)
#DIM DYNAMIC 種類
PRINTL 自分フィールドに「ギャラクシーアイズ」モンスターが存在する場合、このカードの発動は手札からもできる。
CALLFORM CARD_ABLE(0,攻撃時誘発)
PRINTL ①：自分のドラゴン族・光属性モンスターが攻撃対象に選択された時に発動できる。
PRINTL そのモンスターの攻撃力はダメージステップ終了時まで、戦闘を行う相手モンスターの攻撃力分アップする。

@CARDCAN_93171(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 効果先
#DIMS DYNAMIC 対面者

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,93171)
SIF RESULT == 1
	RETURN 0
IF 種類 == 攻撃時誘発
	IF ゾーン == "手札"
		CALL FULLRESET
		効果範囲戦場 = 1
		効果条件カテゴリ = ギャラクシーアイズ
		CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
		SIF RESULT != 1
			RETURN 0
	ELSEIF ゾーン == "魔罠"
		SIF GETVAR(@"%決闘者%_魔_表:場所") == 3
			RETURN 0
	ELSE
		RETURN 0
	ENDIF
	SIF バトルフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー == 決闘者
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件属性 = 光属性
	効果条件種族 = ドラゴン族
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_93171(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 戦闘カード
#DIM DYNAMIC 対戦カード
#DIMS DYNAMIC 対面者
CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 攻撃時誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,93171)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		戦闘カード = 戦闘場所(決闘者)
		対戦カード = 戦闘場所(決闘者,1)
		CALL SET_FIELD_INFO(決闘者,"戦場",戦闘カード,"一時",GETVAR(@"%対面者%_MZ_攻:対戦カード"))
	ENDIF
ENDIF
