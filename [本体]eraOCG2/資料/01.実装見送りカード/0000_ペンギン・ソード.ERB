@CARDNAME_0000,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = ペンギン・ソード
	CASE "略称"
		RESULTS = ペンギン・ソード
	CASE "カテゴリ"
		RESULTS:0 = ペンギン
ENDSELECT


@CARD_0000(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"

		RETURN 装備마법
ENDSELECT

RETURN 0

@CARD_EXPLANATION_0000(종류)
#DIM DYNAMIC 종류


PRINTL 「ペンギン」モンスターにのみ装備可能。
PRINTL 이 카드명의 ③의 효과는 １턴에 １번밖에 사용할 수 없다。
PRINTL ①：装備몬스터의 공격력은 ８００오른다。
PRINTL ②：装備モンスターが상대에게 戦闘데미지를 与えた時に발동할 수 있다。
PRINTL 상대 필드의 카드 １장을 고르고 주인의 패로 되돌린다。
PRINTL ③：상대필드의 앞면 표시인 카드가 「ペンギン」カード의 효과で필드에서 離れ、
PRINTL 패에 戻った場合または제외된 場合に발동한다。
PRINTL 次のターンの終了時まで、そのカード及びその카드와 元々のカード名が同じカード의 효과는 無効化される。


@CARDCAN_0000(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,0000)
SIF RESULT == 1
	RETURN 0



IF 종류 == 마함起動 || 종류 == 손패起動
	IF 종류 == 마함起動
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
			RETURN 0
	ENDIF
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_0000(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード



IF 종류 == 마함起動 || 종류 == 손패起動
	;カード발동＆설치まで
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,0000)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所,마함行き)
	ENDIF

ELSEIF 종류 == 33
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,0000)
	IF RESULT == 0
		CALL CARD_EFFECT_USE(듀얼리스트,0000,1)
	ENDIF
ENDIF
