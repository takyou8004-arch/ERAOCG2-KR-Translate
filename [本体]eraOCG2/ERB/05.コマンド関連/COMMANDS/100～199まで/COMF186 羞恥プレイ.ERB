;-------------------------------------------------
;수치플레이
;특수コマンド
;-------------------------------------------------
@COM186

;着脱の処理　終了時はソース、경험の加算は行わない
TEQUIP:수치플레이 = !TEQUIP:수치플레이 
;-------------------------------------------------
;ソース추가処理
;-------------------------------------------------
;消費체력、기력
DOWNBASE:체력 += 0
DOWNBASE:기력 += 200

CALL SOURCE_CALC_노출(TARGET, MASTER, 600)

RETURN 1

;-------------------------------------------------
;装着계속の処理
;-------------------------------------------------
@EQUIP_COM186(ARG)
;수치플레이

PRINTL ＜수치플레이 중＞

DOWNBASE:ARG:체력 += 10
DOWNBASE:ARG:기력 += 100

CALL SOURCE_CALC_노출(TARGET, MASTER, 300)
RETURN 1

;-------------------------------------------------
;実行판정
;-------------------------------------------------
@COM_ABLE186
;수치플레이実行판정
SIF !TFLAG:100
	RETURN 0
;一括관리
SIF GLOBAL_COMABLE(186)
	RETURN RESULT
;解除はいつでも可能
SIF TEQUIP:수치플레이
	RETURN 1
;아이템｢大鏡｣を持っているか、아이템なし설정でないとダメ
SIF ITEM:21 == 0 && NOITEM == 0
	RETURN 0
;순종が1以上ないとダメ
SIF ABL:순종 <= 1
	RETURN 0
;야외플레이 중はダメ
SIF TEQUIP:야외플레이
	RETURN 0
;목욕탕플레이 중はダメ
SIF TEQUIP:목욕탕플레이
	RETURN 0
;새댁플레이 중はダメ
SIF TEQUIP:새댁플레이
	RETURN 0
RETURN 1