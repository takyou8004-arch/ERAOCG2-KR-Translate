;그늘로 꼬신다
@COM356
人祓いモード ^= 1
RETURN 1


;-------------------------------------------------
;実行판정
;-------------------------------------------------
;그늘로 꼬신다
@COM_ABLE356
;고백한다実行판정
;一括관리
SIF GLOBAL_COMABLE(356)
	RETURN RESULT
;데이트중以外
SIF CFLAG:TARGET:데이트중 != 1
	RETURN 0
SIF CFLAG:MASTER:현재위치 < 50
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
RETURN 1