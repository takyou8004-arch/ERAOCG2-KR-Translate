;-------------------------------------------------
;상대를 귀환시킨다
;-------------------------------------------------
@COM458
;LOCALの대신에 소환リスト機能の下で표시캐릭터を지정
#DIM DYNAMIC 캐릭터_ID
#DIM DYNAMIC 소환_ページ送り
#DIM DYNAMIC リスト_カウンタ
#DIM DYNAMIC リストスタート
#DIM DYNAMIC 캐릭터選択数

CALL SUMMON_SHORI(TARGET)
RETURN 1

;-------------------------------------------------
;実行판정
;-------------------------------------------------
;소환
@COM_ABLE458
;이동実行판정
SIF !TFLAG:100
	RETURN 0
;一括관리
SIF GLOBAL_COMABLE(458)
	RETURN RESULT
SIF TARGET == 0
	RETURN 0
SIF CFLAG:MASTER:현재위치 >= 自宅部屋最大値 && CFLAG:MASTER:현재위치 != 場所_휴식室 && CFLAG:MASTER:현재위치 != 場所_学校内
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
RETURN 1

