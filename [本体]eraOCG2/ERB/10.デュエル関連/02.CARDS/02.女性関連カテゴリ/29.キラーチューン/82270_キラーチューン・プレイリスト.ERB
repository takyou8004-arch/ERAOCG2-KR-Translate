@CARDNAME_82270,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 킬러튠 플레이리스트
	CASE "略称"
		RESULTS = ＫＴ 플리
	CASE "カテゴリ"
		RESULTS:0 = 킬러튠
ENDSELECT


@CARD_82270(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 通常함정
	CASE "추가日"
		RETURN 250823
ENDSELECT

RETURN 0

@CARD_EXPLANATION_82270(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 카드는 １턴에 ２장까지 발동할 수 있다。
CALLFORM CARD_ABLE(15,마함起動,フリー誘発)
PRINTL ①：자신의 필드 / 묘지의「킬러튠」몬스터 １체를 대상으로 발동할 수 있다。
PRINTL 이하의 효과를 각각 적용할 수 있다。
PRINTL 이 카드의 발동 후、턴 종료시까지 자신은 튜너밖에 특수 소환할 수 없다。
PRINTL ●대상의 몬스터를 패에 되돌린다。
PRINTL ●대상의 몬스터에、자신이 Ｓ소재로서 묘지로 보낸 경우에 발동하는 효과를 적용한다。


@CARDCAN_82270(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,82270)
SIF RESULT == 1
	RETURN 0

IF 종류 == 마함起動 || 종류 == フリー誘発
	SIF ゾーン != "마함"
		RETURN 0
	SIF 발동済_2(듀얼리스트,82270)
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") != 2
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲묘지 = 1
	효과条件カテゴリ = 킬러튠
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_82270(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 마함起動 || 종류 == フリー誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,82270,15)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		CALL SET_PLAYER_INFO(듀얼리스트,"튜너制限",1)
		CALL FULLRESET
		효과範囲필드 = 1
		효과範囲묘지 = 1
		효과条件표시 = 表側표시
		효과条件カテゴリ = 킬러튠
		효과条件종류 = モン全般
		CALL CARD_CHOOSE_ADV(듀얼리스트,ゾーン,場所)	
		IF RESULT != 9999
			選択カード_ID = RESULT:99
			CALL CARD_SELECT(듀얼리스트,RESULT)
			選択カード = RESULT

			CALL CARD_DISPOSE(듀얼리스트,参照先（CS）,選択カード,손패行き)
			TRYCALLFORM CARDEFFECT_{選択カード_ID}(듀얼리스트,S素材時효과,"효과処理")
		ENDIF
	ENDIF
ENDIF

