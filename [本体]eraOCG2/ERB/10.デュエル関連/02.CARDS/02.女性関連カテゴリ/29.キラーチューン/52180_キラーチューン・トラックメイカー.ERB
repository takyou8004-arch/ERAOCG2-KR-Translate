@CARDNAME_52180,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 킬러튠 트랙메이커
	CASE "略称"
		RESULTS = 트랙메이커
	CASE "カテゴリ"
		RESULTS:0 = 킬러튠
		RESULTS:5 = 튜너소재가능
ENDSELECT


@CARD_52180(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 싱크로
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 기계족
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 2500
	CASE "튜너"
		RETURN 1
	CASE "추가日"
		RETURN 250823
ENDSELECT

RETURN 0

@CARDSUMMON_52180(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要レベル数 = 4
IF EX参照 == 0 || EX参照 == 1
	素材条件カテゴリ = 킬러튠
	素材条件튜너 = 1
ELSEIF EX参照 >= 10
	素材条件튜너 = 1
ENDIF

IF EX参照 == 1
	CALL SYNCAN(듀얼리스트,소환カード,52180) 
	RETURN RESULT
ENDIF


@CARD_EXPLANATION_52180(종류)
#DIM DYNAMIC 종류

PRINTL "킬러튠" 튜너＋튜너 １장 이상
PRINTL 이 카드명의 ①②의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,EX소환時,특수소환時)
PRINTL ①：이 카드가 특수 소환된 경우에 발동할 수 있다。
PRINTL 덱에서 "킬러튠" 카드 １장을 패에 넣는다。
CALLFORM CARD_ABLE(2,S素材時효과)
PRINTL ②：이 카드가 Ｓ소재로서 묘지로 보내진 경우에 발동할 수 있다。
PRINTL 상대 필드의 카드 １장을 패로 되돌린다。
CALLFORM CARD_ABLE(10,필드起動)
PRINTL ③：１턴에 １번、자신 메인 페이즈에 발동할 수 있다。
PRINTL 패의 튜너 １장과 필드의 이 카드를 Ｓ소재로서 묘지로 보내고、
PRINTL ＥＸ덱에서 Ｓ몬스터 １장을 Ｓ소환한다。
PRINTL 이 효과는、이 카드의 효과가 무효가 되어있거나 발동할 수 없는 상태에서도 발동 가능하며、
PRINTL 상대는 이 효과에 대해서 카드의 효과를 발동할 수 없다。


@CARDCAN_52180(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 필드起動
	;裏だったらダメ
	SIF GETVAR(@"%듀얼리스트%_MZ_表:場所") == 3
		RETURN 0
	;素材불가だったらダメ
	SIF GETVAR(@"%듀얼리스트%_MZ_不材:場所") >= 1
		RETURN 0
	SIF 효과발동済(듀얼리스트,場所,ゾーン)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	CALL KILLERTUNE_CHECK(듀얼리스트,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,52180)
SIF RESULT == 1
	RETURN 0

IF 종류 == EX소환時 || 종류 == 특수소환時
	SIF 발동済_1(듀얼리스트,52180)
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = 킬러튠
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == S素材時효과
	SIF 발동済_2(듀얼리스트,52180)
		RETURN 0
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과範囲상대마함 = 1
	효과条件除去種 = 손패
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == チェーン可否
	SIF !GROUPMATCH(チェーン可否종류, 필드起動)
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_52180(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC EXカードID,50
#DIM DYNAMIC 選択カード

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == EX소환時 || 종류 == 특수소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,52180,1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲덱 = 1
		효과条件カテゴリ = 킬러튠
		CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
	ENDIF

ELSEIF 종류 == S素材時효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,52180,2)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"全場単体",場所,"손패")
	ENDIF

ELSEIF 종류 == 필드起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,52180,10)
	IF RESULT == 0
		CALL KILLERTUNE_CHECK(듀얼리스트,場所)
		IF RESULT == 1
			FOR カウンタ,0,50
				EXカードID:カウンタ = RESULT:(カウンタ+1)
			NEXT

			CALL FULLRESET
			FOR カウンタ,0,50
				효과条件ID:カウンタ = EXカードID:カウンタ
			NEXT
			CALL CARD_CHOOSE_BASIC(듀얼리스트,"EX덱",ゾーン,場所)
			IF RESULT != 9999
				選択カード = RESULT
				CALL KILLERTUNE_SYNSUMMON(듀얼리스트,ゾーン,場所,選択カード)
			ENDIF
		ENDIF
	ENDIF
ENDIF
