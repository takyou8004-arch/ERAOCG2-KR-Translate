@CARDNAME_57910,参照先
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 아미야－영소
	CASE "略称"
		RESULTS = Amiya,Blade
	CASE "カテゴリ"
		RESULTS:0 = 로도스 아일랜드
		RESULTS:1 = Guard
		RESULTS:2 = 오퍼레이터
		RESULTS:4 = 명일방주
ENDSELECT


@CARD_57910(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 미확인족 
	CASE "レベル"
		RETURN 5
	CASE "攻撃力"
		RETURN 2500
	CASE "守備力"
		RETURN 1300
	CASE "성별"
		RETURN 牝性
	CASE "単一存在"
		RETURN 1
	CASE "連動ID"
		RETURN 4860
ENDSELECT

RETURN 0
@CARDSUMMON_57910(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要素材数 = 3
素材条件レベル = 5
;추가효과用

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,57910) 
	RETURN RESULT
ENDIF


@CARD_EXPLANATION_57910(종류)
#DIM DYNAMIC 종류

PRINTL 레벨５ 몬스터×３
PRINTL 필드의 이 카드는 "아미야"로도 취급하며、"아미야"는 자신 필드에 １장밖에 존재할 수 없다。
PRINTL "아미야－영소"의 ①②의 효과는 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,발동誘発)
PRINTL ①：자신 몬스터 존의 ＥＸ 몬스터가 아닌 "아미야" １장을 제외하고 발동할 수 있다。
PRINTL ＥＸ덱의 이 카드를 Ｘ소재를 １개 가진 상태로 특수 소환한다。
CALLFORM CARD_ABLE(2,발동誘発)
PRINTL ②：상대의 효과가 발동했을 때、이 카드의 Ｘ소재를 전부 제거하고 발동할 수 있다。
PRINTL 그 발동을 무효로 하고、상대는 자신의 필드의 공격력이 가장 높은 몬스터 １장을 묘지로 보낸다。
PRINTL 그 후、이 카드의 공격력은［묘지로 보내진 몬스터의 원래 코스트］×２００오른다。
CALLFORM CARD_ABLE(0,攻守変動)
PRINTL ③：자신 필드의 "오퍼레이터"의 공격력 / 수비력은５００ 오른다。


@CARDCAN_57910(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 지속효과
	SIF !GROUPMATCH(ゾーン,"필드","마함")
		RETURN 0
	RETURN 1
ENDIF

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,57910)
SIF RESULT == 1
	RETURN 0

IF 종류 == EX덱起動
	SIF 발동済_1(듀얼리스트,57910)
		RETURN 0
	CALL CARD_EX_CHECK(듀얼리스트,57910)
	SIF RESULT == 1
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件名前 = 아미야
	효과条件종류 = 효과モン
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 발동誘発
	SIF 발동済_2(듀얼리스트,57910)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 攻守変動
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ENDIF


@CARDEFFECT_57910(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC X素材数

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == EX덱起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,57910,1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲필드 = 1
		효과条件名前 = 아미야
		효과条件종류 = 효과モン
		효과条件行先플래그 = 제외行き
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			효과추가종류 = 効追Ｘ素
			CALL CARD_SP_SUMMON(듀얼리스트,ゾーン,場所)
		ENDIF
	ENDIF

ELSEIF 종류 == 발동誘発
	X素材数 = GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") + 1
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,57910,2,X素材数)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"발동無効",場所,"無効")

		FOR カウンタ,0,5
			SIF GETVAR(@"%対面者%_필드:カウンタ") < 1
				CONTINUE
			SIF GETVAR(@"%対面者%_MZ_表:カウンタ") == 3
				CONTINUE
			SIF 攻撃力修正 >= GETVAR(@"%対面者%_MZ_攻:カウンタ")
				CONTINUE
			攻撃力修正 = GETVAR(@"%対面者%_MZ_攻:カウンタ")
		NEXT
		CALL FULLRESET
		효과範囲필드 = 1
		효과条件以攻撃力 = 攻撃力修正
		CALL CARD_ADD_GY_ADV(対面者,ゾーン,場所)
		IF RESULT == 1
			TRYCALLFORM CARD_{RESULT:1},"レベル"
			CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"永攻",RESULT*200)
		ENDIF
	ENDIF

ELSEIF 종류 == 攻守変動
	CALL FULLRESET
	효과条件カテゴリ = 오퍼레이터
	CALL STATCHANGE_BASIC(듀얼리스트,"필드",場所,500,500)

ELSEIF 종류 == 지속효과
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"変化元ID",4860)
ENDIF
