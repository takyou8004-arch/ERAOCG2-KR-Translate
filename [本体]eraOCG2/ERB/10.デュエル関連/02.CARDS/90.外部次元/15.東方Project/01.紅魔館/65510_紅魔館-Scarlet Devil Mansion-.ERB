@CARDNAME_65510,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 홍마관-Scarlet Devil Mansion-
	CASE "略称"
		RESULTS = 『홍마관』
	CASE "カテゴリ"
		RESULTS:0 = 홍마관
		RESULTS:4 = 동방 Project
ENDSELECT


@CARD_65510(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "外部者"
		RETURN 1
	CASE "종류"
		RETURN フィールド
	CASE "추가日"
		RETURN 250705
ENDSELECT

RETURN 0

@CARD_EXPLANATION_65510(종류)
#DIM DYNAMIC 종류

PRINTL 자신 필드는 「홍마관」가 된다。
PRINTL 이 카드명의 ①②의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,마함起動)
PRINTL ①：자신 메인 페이즈에 EX덱의 「뱀파이어」モンスター１枚を見せて발동할 수 있다。
PRINTL 자신 필드의 몬스터 １장을 破壊する。
PRINTL 자신 필드에 「뱀파이어」몬스터가 존재하는 경우、さらに자신의 묘지의 「홍마관」몬스터를 특수 소환한다。
CALLFORM CARD_ABLE(2,지속효과)
PRINTL ②：フィールドの카드가破壊된 경우에 발동할 수 있다。
PRINTL 자신은 カードを1장 드로우한다。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ③：자신 필드에 「뱀파이어」몬스터가 존재하는 경우、
PRINTL 상대는 「뱀파이어」몬스터 이외의 カードを효과の대상にできない。



@CARDCAN_65510(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 효과使用불가,3
CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,65510)
SIF RESULT == 1
	RETURN 0


IF 종류 == 마함起動 && GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
	SIF 발동済_1(듀얼리스트,65510)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 마함起動 || 종류 == 손패起動
	IF 종류 == 마함起動
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
			RETURN 0
	ENDIF
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1

ELSEIF 종류 == 지속효과 || 종류 == 攻守変動
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") >= 2
		RETURN 0
	RETURN 1



ELSEIF 종류 == 효과後誘発
	SIF 발동済_2(듀얼리스트,65510)
		RETURN 0
	SIF ゾーン != "마함"
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_FIN_DATA(듀얼리스트,"破壊全般",2)
	SIF RESULT != 1
		RETURN 0
	CALL CARD_DRAW_CHECK

	RETURN 1
ENDIF


@CARDEFFECT_65510(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 条件달성
#DIM DYNAMIC 효과使用불가,3




IF 종류 == 마함起動 && GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,65510,1)
	IF RESULT == 0
		CALL FULLRESET
		효과条件カテゴリ = 뱀파이어
		CALL CARD_CHOOSE_BASIC(듀얼리스트,"EX덱",ゾーン,場所)
		IF RESULT != 9999
			CALL FULLRESET
			효과範囲필드 = 1
			효과条件行先플래그 = 破壊行き
			CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
			IF RESULT == 1
				CALL FULLRESET
				효과範囲묘지 = 1
				효과条件カテゴリ = 홍마관
				CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
			ENDIF
		ENDIF
	ENDIF
ELSEIF 종류 == 마함起動 || 종류 == 손패起動
	;カード발동＆설치まで
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,65510)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所,마함行き)
	ENDIF
ELSEIF 종류 == 효과後誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,65510,2)
	IF RESULT == 0
		CALL CARDDRAW_BASIC(듀얼리스트,1)
	ENDIF

ELSEIF 종류 == 지속효과
	CALL SET_FIELD_MAGIC_INFO(듀얼리스트, "홍마관")
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件カテゴリ = 뱀파이어
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	IF RESULT == 1
		CALL FULLRESET
		효과条件固カテゴリ = 뱀파이어
		CALL STATCHANGE_EFFECT(듀얼리스트,"필드",場所,"対耐")
		CALL FULLRESET
		CALL STATCHANGE_EFFECT(듀얼리스트,"마함",場所,"対耐")
	ENDIF
ENDIF
