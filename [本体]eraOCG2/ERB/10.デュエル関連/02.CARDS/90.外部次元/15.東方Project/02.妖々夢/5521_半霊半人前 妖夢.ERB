@CARDNAME_5521,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 반령반인분 요우무
	CASE "略称"
		RESULTS = 콘파쿠 요우무
	CASE "カテゴリ"
		RESULTS:0 = 백옥루
		RESULTS:1 = 환상소녀
		RESULTS:4 = 동방 Project
ENDSELECT


@CARD_5521(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 땅속성
	CASE "종족"
		RETURN 언데드족
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1800
	CASE "守備力"
		RETURN 0
	CASE "튜너"
		RETURN 1
	CASE "성별"
		RETURN 牝性
	CASE "生成カード"
		RETURN 205521
	CASE "추가日"
		RETURN 250808
ENDSELECT

RETURN 0

@CARD_EXPLANATION_5521(종류)
#DIM DYNAMIC 종류

PRINTL このカード名の①②の효과はそれぞれ１ターンに１度しか使用できない。
;半人반령 
CALLFORM CARD_ABLE(1,소환時,특수소환時)
PRINTL ①：フィールドに「반인반령분 요우무」が存在しない場合、このカードが소환・특수소환に成功した場合に발동できる。
PRINTL 「반인반령분 요우무」１体を덱・손패・묘지から특수소환する。
PRINTL この효과の발동後、자신は언데드족モンスターしかEX덱から특수소환できない。
;スペカ確保
CALLFORM CARD_ABLE(2,필드起動)
PRINTL ②：자신メインフェイズに발동できる。
PRINTL お互いのプレイヤーは손패を２枚捨て、それぞれカードを２枚ドローする。
PRINTL その後、お互いいずれかの묘지に언데드족モンスターが存在する場合、
PRINTL 자신の손패に「인부　현세참」を１枚손패に加える。

@CARDCAN_5521(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC 条件달성

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,5521)
SIF RESULT == 1
	RETURN 0
IF 종류 == 소환時 || 종류 == 특수소환時
	SIF 발동済_1(듀얼리스트,5521)
		RETURN 0

	CALL FULLRESET
	효과範囲필드 = 1
	효과条件名前 = 반인반령분 요우무
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT == 1
		RETURN 0

	CALL FULLRESET
	효과範囲손패 = 1
	효과範囲덱 = 1
	효과範囲묘지 = 1
	효과条件名前 = 반인반령분 요우무
	효과条件소환可 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 필드起動
	SIF 발동済_2(듀얼리스트,5521)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	CALL FULLRESET
	효과範囲손패 = 1
	효과条件CNT = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT < 2
		RETURN 0
	CALL FULLRESET
	효과範囲상대손패 = 1
	효과条件CNT = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT < 2
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_5521(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 条件달성
#DIM DYNAMIC ドロー枚数

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 소환時 || 종류 == 특수소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,5521,1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲손패 = 1
		효과範囲덱 = 1
		효과範囲묘지 = 1
		효과条件名前 = 반인반령분 요우무
		CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)

		EX소환可能종족 = 언데드족
		CALL CARD_EX_RESTRICTION(듀얼리스트)
	ENDIF
ELSEIF 종류 == 필드起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,5521,2)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲손패 = 1
		효과条件複数 = 2
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		SIF RESULT == 1
			CALL CARDDRAW_BASIC(듀얼리스트,2)

		CALL FULLRESET
		효과範囲손패 = 1
		효과条件複数 = 2
		CALL CARD_ADD_GY_ADV(対面者,ゾーン,場所)
		SIF RESULT == 1
			CALL CARDDRAW_BASIC(対面者,2)


		CALL FULLRESET
		효과範囲묘지 = 1
		효과範囲상대묘지 = 1
		효과条件종족 = 언데드족
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		SIF RESULT == 1
			CALL CARD_GENERATE_HAND(듀얼리스트,205521)
	ENDIF
ENDIF


;――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――


@CARDNAME_205521,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 인부　현세참
	CASE "略称"
		RESULTS = 인부　현세참
	CASE "カテゴリ"
		RESULTS:0 = 스펠 카드
ENDSELECT


@CARD_205521(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"
		RETURN 速攻마법
	CASE "トークン"
		RETURN 1
ENDSELECT

RETURN 0

@CARD_EXPLANATION_205521(종류)
#DIM DYNAMIC 종류
PRINTL このカード名の효과は１ターンに１度しか使用できない。

CALLFORM CARD_ABLE(10,마함起動 ,손패起動)
PRINTL ①：상대フィールドに存在するモンスター１体を選択して발동できる。
PRINTL そのカードを破壊する。
PRINTL 자신フィールドの「반령반인분 요우무」が破壊されていた場合、
PRINTL 자신の묘지から「楽반령반인분 요우무」を특수소환し、このカードは「ラストワード」となる。
CALL TEXT_DECORATION("LASTWORD")
PRINTL ●상대フィールドの마법・함정カードを１枚を選び破壊する。
PRINTL その後、상대の덱の上から１０枚を묘지に送る。

@CARDCAN_205521(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ


CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,205521)
SIF RESULT == 1
	RETURN 0

IF 종류 == 마함起動 || 종류 == 손패起動 || 종류 == フリー誘発
	SIF 발동済_1(듀얼리스트,205521)
		RETURN 0
	IF ゾーン == "손패"
		SIF ターンプレイヤー != 듀얼리스트
			RETURN 0
	ELSEIF ゾーン == "마함"
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 3
			RETURN 0
	ELSE
		RETURN 0
	ENDIF
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과条件除去種 = 묘지
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_205521(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC ダメージ量
#DIM DYNAMIC ドロー枚数
CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 마함起動 || 종류 == 손패起動 || 종류 == フリー誘発
	;カード발동確認
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,205521,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)



		CALL FULLRESET
		효과条件名前 = 반령반인분 요우무
		CALL CARD_CHECK_FIN_DATA(듀얼리스트,"破壊全般")
		IF RESULT == 1
			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件名前 = 반령반인분 요우무
			CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
			CALLFORM CHARA_CUTIN("Last Word","待　宵　反　射　衛　星　斬",,,,,1)
			CALL FULLRESET
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드単体",場所,"묘지")
			CALL FULLRESET
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"마함単体",場所,"묘지")


			CALL URARA_CHANCE(듀얼리스트,処理中_효과ID,"雑多")
			IF RESULT == 0
				FOR カウンタ,0,紙束最大値
					SIF GETVAR(@"%듀얼리스트%_손패:カウンタ") == -1
						CONTINUE
					CALL CARD_DISPOSE(듀얼리스트,"손패",カウンタ)
					ドロー枚数 ++
				NEXT
				CALL CARDDRAW_BASIC(듀얼리스트,ドロー枚数,2)

				VARSET ドロー枚数
				FOR カウンタ,0,紙束最大値
					SIF GETVAR(@"%対面者%_손패:カウンタ") == -1
						CONTINUE
					CALL CARD_DISPOSE(対面者,"손패",カウンタ)
					ドロー枚数 ++
				NEXT
				CALL CARDDRAW_BASIC(対面者,ドロー枚数,2)
			ENDIF
		ELSE
			CALLFORM CHARA_CUTIN("Spell Card","「인부」현세참",,,,,1)
			CALL FULLRESET
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드単体",場所,"묘지")
		ENDIF


	ENDIF
ENDIF
