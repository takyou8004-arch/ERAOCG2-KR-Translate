@CARDNAME_4515,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 낙원의 요정 아르토리아 캐스터
	CASE "略称"
		RESULTS = 캐스토리아
	CASE "カテゴリ"
		RESULTS:0 = 요정
		RESULTS:1 = 영령
		RESULTS:2 = 캐스터
		RESULTS:3 = FATE
ENDSELECT

@CARD_4515(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 링크
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 마법사족
	CASE "レベル"
		RETURN 1
	CASE "攻撃力"
		RETURN 1800
	CASE "守備力"
		RETURN 1800
	CASE "추가日"
		RETURN 250817
ENDSELECT

RETURN 0
@CARDSUMMON_4515(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照

CALL RESETCONDITION_MAT
必要링크数 = 1
素材条件召種 = 召種通常
素材条件攻撃力 = 1000

IF EX参照 == 1
	CALL LINCAN(듀얼리스트,소환カード,4515) 
	RETURN RESULT
ENDIF

@CARD_EXPLANATION_4515(종류)
#DIM DYNAMIC 종류

PRINTL "영령" 몬스터 １장
PRINTL 이 카드명의 ①②의 효과는 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,필드起動,フリー誘発)
PRINTL ①：자신 / 상대턴에、자신 필드의 몬스터 １장을 대상으로 하고 발동할 수 있다。
PRINTL 이 턴、그 몬스터는 파괴내성을 얻는다。
CALLFORM CARD_ABLE(2,효과後誘発)
PRINTL ②：이 카드가 필드에 존재하는 상태로、다른 "영령" 카드가 효과를 발동했을 경우에 발동할 수 있다。
PRINTL 이 턴、그 몬스터의 공격력을 턴 종료시까지 １０００올린다。

@CARDCAN_4515(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,4515)
SIF RESULT == 1
	RETURN 0


IF 종류 == 필드起動 || 종류 == フリー誘発
	SIF !GROUPMATCH(ゾーン,"필드")
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件표시 = 表側표시
	효과条件他所 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 발동誘発
	SIF 발동済_2(듀얼리스트,4515)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件カテゴリ = 영령
	효과条件固名前 = 낙원의 요정 아르토리아 캐스터
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF


@CARDEFFECT_4515(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID
#DIM DYNAMIC 選択レベル
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 속성セット
#DIM DYNAMIC 속성セットビット

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 필드起動 || 종류 == フリー誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,4515,1)
	IF RESULT == 0	
		CALL FULLRESET
		효과範囲필드 = 1
		CALL CARD_GRANT_ADV(듀얼리스트,ゾーン,場所,"破耐",1)
	ENDIF
ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,4515,2)
	IF RESULT == 0
		選択カード = 効後_設場所:(処理中番号 - 1) 
		CALL SET_FIELD_INFO(듀얼리스트,"필드",選択カード,"一攻",1000)
	ENDIF
ENDIF
