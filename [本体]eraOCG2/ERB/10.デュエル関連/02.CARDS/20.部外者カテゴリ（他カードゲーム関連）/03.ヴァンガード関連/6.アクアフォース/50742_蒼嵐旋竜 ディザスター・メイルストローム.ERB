@CARDNAME_50742,参照先
;ココで지정카드의 名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
RESULTS = 창람선룡 디재스터 메일스트롬
	CASE "略称"
RESULTS = 재 메일
	CASE "カテゴリ"
RESULTS:0 = 아쿠아 포스
RESULTS:1 = 메일스트롬
RESULTS:2 = 창람
RESULTS:4 = 뱅가드
ENDSELECT


@CARD_50742(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"

		RETURN 초차원
		CASE "속성"
		RETURN 물속성
	CASE "종족"
		RETURN 바다생물족
	CASE "レベル"
		RETURN 10
	CASE "攻撃力"
		RETURN 1500
	CASE "守備力"
		RETURN 0
	CASE "성별"
		RETURN 雄性
	CASE "특수불가"
		RETURN 1

ENDSELECT

RETURN 0


@CARDSUMMON_50742(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
;素材条件


CALL RESETCONDITION_MAT
必要素材数 = 1
素材条件カテゴリ = 메일스트롬

IF EX参照 == 1
	CALL STRIDECAN(듀얼리스트,소환カード,50742) 
	RETURN RESULT
ENDIF


@CARD_EXPLANATION_50742(종류)
#DIM DYNAMIC 종류

PRINTL 『초월』 "메일스트롬" 몬스터
PRINTL (자신의 패에서 몬스터 1장을 제외하여 발동할 수 있다.
PRINTL 　엑스트라 덱의 이 카드를, 자신 필드의 지정된 몬스터 1장 위에 겹쳐서 『초월』한다.
PRINTL  『초월』한 이 카드의 이름은 겹친 몬스터와 같은 이름으로 취급하고,
PRINTL 　겹친 몬스터의 공격력 / 수비력만큼, 이 카드의 공격력 / 수비력을 올린다.
PRINTL  『초월』은 1턴에 1번밖에 할 수 없으며, 선공 1턴에는 사용할 수 없다)
PRINTL 이 카드명의 ①②의 효과는 1턴에 각각 1번밖에 사용할 수 없다.
CALLFORM CARD_ABLE(1,EX덱起動)
PRINTL ①: 4회 이상 공격 선언된 자신의 배틀 페이즈에 발동할 수 있다.
PRINTL 이 카드를 엑스트라 덱에서 『초월』시킨다.
CALLFORM CARD_ABLE(2,攻撃時誘発)
PRINTL ②: 이 카드의 공격 선언시에 발동할 수 있다.
PRINTL 덱에서 "창람" 카드 1장을 패에 추가한다.
PRINTL 이 턴, 자신 필드의 "아쿠아 포스" 몬스터의 공격력은 1000 올린다.

@CARDCAN_50742(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,50742)
SIF RESULT == 1
	RETURN 0

IF 종류 == EX덱起動
	SIF 발동済_1(듀얼리스트,50742)
 		RETURN 0
	SIF ゾーン != "EX덱"
		 RETURN 0
 	SIF バトルフェイズ != 1
 		RETURN 0
 	SIF ターンプレイヤー != 듀얼리스트
 		RETURN 0
	SIF ターン中攻撃횟수 < 4
		 RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		 RETURN 0
	CALL FULLRESET
	효과範囲손패 = 1
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件カテゴリ = 메일스트롬
	CALL CARD_CHECK_ADV(듀얼리스트)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件종류 = 초차원
	효과条件カテゴリ = 뱅가드
	CALL CARD_CHECK_ADV(듀얼리스트)
	SIF RESULT == 1
		RETURN 0
 	RETURN 1
ELSEIF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻撃時誘発
	SIF 발동済_2(듀얼리스트,50742)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF 戦闘場所(듀얼리스트) != 場所
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = 창람
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻紋章起動
	RETURN 1
ENDIF

@CARDEFFECT_50742(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 효과先
#DIM DYNAMIC カードID
#DIM DYNAMIC 選択カードID

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == EX덱起動
 	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,50742,1)
 	IF RESULT == 0
	CALL FULLRESET
	효과条件종류 = モン全般
	효과条件行先플래그 = 제외行き
	CALL CARD_ADD_GY_BASIC(듀얼리스트,"손패",ゾーン,場所)
		IF RESULT == 1
		CALL FULLRESET
		효과条件カテゴリ = 메일스트롬
		효과条件行先플래그 = 消滅行き
		효과条件任意 = 1
		CALL CARD_ADD_GY_BASIC(듀얼리스트,"필드",ゾーン,場所)
			IF RESULT == 1
				選択カードID = RESULT:1
				CALL CARD_LOADUP
				PRINTFORML
				PRINTFORML
				PRINTFORML 超越《ストライド》、ジェネレ-ション！
				CALL PRINT_STATE_DUEL(カードID)
				CALL FULLRESET
				CALL CARD_EX_SUMMON(듀얼리스트,"EX덱",場所,"攻撃표시")
				SETVAR @"%듀얼리스트%_MZ_変化元ID:カード소환先", 選択カードID
			ELSE
				RETURN 0
			ENDIF
		ELSE
			RETURN 0
		ENDIF
	ENDIF
ELSEIF 종류 == 지속효과
	CALL FULLRESET
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"不材")
ELSEIF 종류 == 攻撃時誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,50742,2)
	IF RESULT == 0
		CALL FULLRESET
		효과条件カテゴリ = 창람
		CALL CARD_ADD_BASIC(듀얼리스트,"덱",ゾーン,場所)
		CALL CARD_RESERV(듀얼리스트,50742,攻紋章予約)
	ENDIF
ELSEIF 종류 == 攻紋章起動
	CALL FULLRESET
	효과条件カテゴリ = 아쿠아 포스
	CALL STATCHANGE_BASIC(듀얼리스트,"필드",場所,1000)
ENDIF



