@CARDNAME_21085,参照先
;ココで지정카드의 名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 버닝혼 드래곤
	CASE "略称"
		RESULTS = 버닝혼
	CASE "カテゴリ"
		RESULTS:0 = 카게로우
		RESULTS:4 = 뱅가드
ENDSELECT


@CARD_21085(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"
	
		RETURN 효과モン
	CASE "속성"
		RETURN 화염속성
	CASE "종족"
		RETURN 드래곤족
	CASE "レベル"
		RETURN 7
	CASE "攻撃力"
		RETURN 2400
	CASE "守備力"
		RETURN 1000
	CASE "성별"
		RETURN 雄性
ENDSELECT
RETURN 0

@CARD_EXPLANATION_21085(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①②의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,손패起動)
PRINTL ①：EX덱의 "오버로드" 몬스터 １장을 상대에게 보여주고 발동할 수 있다。
PRINTL 이 카드를 패에서 특수 소환한다。
PRINTL 그 후、패에서 "버닝혼 드래곤" 이외의 "카게로우" 몬스터 １체를 특수 소환할 수 있다。
CALLFORM CARD_ABLE(2,S素材時효과)
PRINTL ②：이 카드가 S소환의 소재가 되었을 경우에 발동할 수 있다。
PRINTL 이 게임 중、자신은 이하의 효과를 얻는다。（이 효과는 중복되지 않는다）
PRINTL ●자신 필드에 "오버로드" 몬스터가 존재하는 경우、
PRINTL 자신 필드의 화염속성 몬스터의 공격력은 ５００오른다。


@CARDCAN_21085(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,21085)
SIF RESULT == 1
	RETURN 0

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 손패起動 ||  종류 == フリー誘発
	SIF 발동済_1(듀얼리스트,21085)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	CALL CARD_SS_CHECK(듀얼리스트,21085,"손패")
	SIF RESULT == 1
		RETURN 0
	CALL FULLRESET
	효과範囲EX덱 = 1
	효과条件カテゴリ = 오버로드
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == S素材時효과
	SIF 발동済_2(듀얼리스트,21085)
		RETURN 0
	SIF 紋章済(듀얼리스트,21085)
		RETURN 0
	RETURN 1
ELSEIF 종류 == 永攻紋章起動
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = 오버로드
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_21085(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC ダメージ

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 손패起動 ||  종류 == フリー誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,21085,1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲손패 = 1
		효과条件カテゴリ = 오버로드
		CALL CARD_CHOOSE_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT != 9999
			CALL FULLRESET
			CALL CARD_SP_SUMMON(듀얼리스트,"손패",場所)

			IF モンスター数(듀얼리스트) < 5
				CALL FULLRESET
				효과範囲손패 = 1
				효과条件カテゴリ = 카게로우
				효과条件종류 = モン全般
				효과条件固名前 = 버닝혼 드래곤
				CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
				SIF RESULT != 1
					RETURN 0

				CALL FULLRESET
				효과範囲손패 = 1
				효과条件カテゴリ = 카게로우
				효과条件종류 = モン全般
				효과条件固名前 = 버닝혼 드래곤
				효과条件소환可 = 1
				효과条件任意 = 1
				CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
			ENDIF
		ENDIF
	ENDIF
ELSEIF 종류 == S素材時효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,21085,2)
	IF RESULT == 0
		CALL CARD_RESERV(듀얼리스트,21085,永攻紋章予約)
	ENDIF
ELSEIF 종류 == 永攻紋章起動
	CALL FULLRESET
	효과条件속성 = 화염속성
	CALL STATCHANGE_BASIC(듀얼리스트,"필드",場所,500)
ENDIF


