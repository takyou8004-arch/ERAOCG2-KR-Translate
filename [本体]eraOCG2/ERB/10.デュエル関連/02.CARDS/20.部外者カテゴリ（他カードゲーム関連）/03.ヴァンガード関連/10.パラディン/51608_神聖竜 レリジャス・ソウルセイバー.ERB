@CARDNAME_51608,参照先
;ココで지정카드의 名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 신성룡 릴리저스 소울 세이버
	CASE "略称"
		RESULTS = R소울 세이버
	CASE "カテゴリ"
		RESULTS:0 = 로열 팔라딘
		RESULTS:1 = 블래스터 블레이드
		RESULTS:4 = 뱅가드
ENDSELECT


@CARD_51608(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"

		RETURN 초차원
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 드래곤족
	CASE "レベル"
		RETURN 10
	CASE "攻撃力"
		RETURN 1500
	CASE "守備力"
		RETURN 0
	CASE "성별"
		RETURN 牝性
	CASE "특수불가"
		RETURN 1
	CASE "レア度"
		RETURN ULR

ENDSELECT

RETURN 0

@CARDSUMMON_51608(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
;素材条件

CALL RESETCONDITION_MAT
必要素材数 = 1
素材条件カテゴリ = 블래스터 블레이드

IF EX参照 == 1
	CALL STRIDECAN(듀얼리스트,소환カード,51608) 
	RETURN RESULT
ENDIF

@CARD_EXPLANATION_51608(종류)
#DIM DYNAMIC 종류

PRINTL 『초월』"블래스터 블레이드" 몬스터
SETCOLOR C_GRAY
PRINTL （패에서 몬스터 １장을 버리고 발동할 수 있다。
PRINTL 　EX덱의 이 카드를、자신 필드의 지정된 몬스터 １장 위에 겹쳐서 『초월』한다。
PRINTL  『초월』한 이 카드의 이름은 겹친 몬스터와 같은 이름으로 취급하며、
PRINTL 　겹친 몬스터의 공격력 / 수비력만큼、이 카드의 공격력 / 수비력을 올린다。
PRINTL  『초월』은 １턴에 １번밖에 할 수 없으며、선공１턴째엔 사용할 수 없다）
RESETCOLOR
PRINTL 이 카드명이ㅡ ①의 효과는 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,EX소환時)
PRINTL ①：이 카드가 『초월』한 경우、EX덱에서 "신성룡 릴리저스 소울 세이버" １장을 제외하고 발동할 수 있다。
PRINTL 이 턴、자신 필드의 몬스터의 공격력은 １２００오른다。
PRINTL 그 후、자신의 묘지에 "블래스터 블레이드" 몬스터와 "블래스터 다크" 몬스터가 존재하는 경우、
PRINTL 덱에서 "블래스터 블레이드" 카드를 １장을 패에 넣는다。



@CARDCAN_51608(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,51608)
SIF RESULT == 1
	RETURN 0


IF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == EX소환時
	SIF 발동済_1(듀얼리스트,51608)
		RETURN 0
	CALL FULLRESET
	효과範囲EX덱 = 1
	효과条件名前 = 신성룡 릴리저스 소울 세이버
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻紋章起動
	RETURN 1
ENDIF

@CARDEFFECT_51608(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 효과先
#DIM DYNAMIC モンスター数
#DIM DYNAMIC ダメージ量
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC カードID
#DIM DYNAMIC 選択カードID
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%



IF 종류 == 지속효과
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"不材")
ELSEIF 종류 == EX소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,51608,1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲EX덱 = 1
		효과条件名前 = 신성룡 릴리저스 소울 세이버
		효과条件行先플래그 = 제외行き
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL CARD_RESERV(듀얼리스트,51608,攻紋章予約)
	
			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件カテゴリ = 블래스터 블레이드
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 += 1
			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件カテゴリ = 블래스터 다크
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 += 1

			IF 条件달성 >= 2
				CALL FULLRESET
				효과範囲덱 = 1
				효과条件カテゴリ = 블래스터 블레이드
				CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
				IF RESULT == 1
					CALL FULLRESET
					효과範囲덱 = 1
					효과条件カテゴリ = 블래스터 블레이드
					CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ELSEIF 종류 == 攻紋章起動
	CALL FULLRESET
	CALL STATCHANGE_BASIC(듀얼리스트,"필드",場所,1200)
ENDIF
