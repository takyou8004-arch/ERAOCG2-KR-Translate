@CARDNAME_51608,参照先
;ココで지정카드의 名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 신성룡 릴리저스 소울 세이버
	CASE "略称"
		RESULTS = R소울 세이버
	CASE "カテゴリ"
		RESULTS:0 = 로열 팔라딘
		RESULTS:1 = 블래스터 블레이드
		RESULTS:4 = 뱅가드
ENDSELECT


@CARD_51608(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"

		RETURN 초차원
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 드래곤족
	CASE "レベル"
		RETURN 10
	CASE "攻撃力"
		RETURN 1500
	CASE "守備力"
		RETURN 0
	CASE "성별"
		RETURN 牝性
	CASE "특수불가"
		RETURN 1
	CASE "レア度"
		RETURN ULR

ENDSELECT

RETURN 0

@CARDSUMMON_51608(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
;素材条件

CALL RESETCONDITION_MAT
必要素材数 = 1
素材条件カテゴリ = 블래스터 블레이드

IF EX参照 == 1
	CALL STRIDECAN(듀얼리스트,소환カード,51608) 
	RETURN RESULT
ENDIF

@CARD_EXPLANATION_51608(종류)
#DIM DYNAMIC 종류

PRINTL 『超越』「블래스터 블레이드」モンスター
SETCOLOR C_GRAY
PRINTL （손패からモンスター１体を捨てて발동できる。
PRINTL 　EX덱のこのカードを、자신フィールドの지정されたモンスター１体の上に重ねて『超越』する。
PRINTL  『超越』したこのカードの名前は重ねたモンスターと同じ名前として扱い、
PRINTL 　重ねたモンスターの攻撃力・守備力分、このカードの攻撃力・守備力をアップする。
PRINTL  『超越』は１ターンに１度しか行えず、先行１ターン目には使用できない）
RESETCOLOR
PRINTL このカード名の①の효과は１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,EX소환時)
PRINTL ①：このカードが『超越』した場合、EX덱から「神聖竜 レリジャス・ソウルセイバー」１体を제외して발동できる。
PRINTL このターン、자신フィールドのモンスターの攻撃力は１２００アップする。
PRINTL その後、자신の묘지に「블래스터 블레이드」モンスターと「블래스터 다크」モンスターが存在する場合、
PRINTL 덱から「블래스터 블레이드」カードを１枚を손패に加える。



@CARDCAN_51608(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,51608)
SIF RESULT == 1
	RETURN 0


IF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == EX소환時
	SIF 발동済_1(듀얼리스트,51608)
		RETURN 0
	CALL FULLRESET
	효과範囲EX덱 = 1
	효과条件名前 = 신성룡 릴리저스 소울 세이버
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻紋章起動
	RETURN 1
ENDIF

@CARDEFFECT_51608(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 효과先
#DIM DYNAMIC モンスター数
#DIM DYNAMIC ダメージ量
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC カードID
#DIM DYNAMIC 選択カードID
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%



IF 종류 == 지속효과
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"不材")
ELSEIF 종류 == EX소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,51608,1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲EX덱 = 1
		효과条件名前 = 신성룡 릴리저스 소울 세이버
		효과条件行先플래그 = 제외行き
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL CARD_RESERV(듀얼리스트,51608,攻紋章予約)
	
			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件カテゴリ = 블래스터 블레이드
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 += 1
			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件カテゴリ = 블래스터 다크
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 += 1

			IF 条件달성 >= 2
				CALL FULLRESET
				효과範囲덱 = 1
				효과条件カテゴリ = 블래스터 블레이드
				CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
				IF RESULT == 1
					CALL FULLRESET
					효과範囲덱 = 1
					효과条件カテゴリ = 블래스터 블레이드
					CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ELSEIF 종류 == 攻紋章起動
	CALL FULLRESET
	CALL STATCHANGE_BASIC(듀얼리스트,"필드",場所,1200)
ENDIF
