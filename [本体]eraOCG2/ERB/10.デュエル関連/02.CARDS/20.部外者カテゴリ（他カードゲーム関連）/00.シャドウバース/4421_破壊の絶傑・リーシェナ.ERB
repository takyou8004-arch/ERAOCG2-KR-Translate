@CARDNAME_4421,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 파괴의 재앙 리셰나
	CASE "略称"
		RESULTS = 리셰나
	CASE "カテゴリ"
		RESULTS:0 = 네메시스 클래스
		RESULTS:4 = Shadowverse
ENDSELECT


@CARD_4421(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 기계족
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1900
	CASE "守備力"
		RETURN 2500
	CASE "성별"
		RETURN 牝性
	CASE "추가日"
		RETURN 250909
	CASE "生成カード"
		RESULT:1 = 204421
		RETURN 104421
ENDSELECT

RETURN 0

@CARDSUMMON_4421(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要素材数 = 2
素材条件レベル = 4

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,4421) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_4421(종류)
#DIM DYNAMIC 종류

PRINTL 레벨４ 몬스터×２
CALL CARD_ABLE(0,EX소환時)
PRINTL ①：이 카드가 X소환에 성공한 경우에 발동할 수 있다。
PRINTL 자신 마법 / 함정 존에 "하얀 장"을 앞면 표시로 둔다。
CALL CARD_ABLE(0,지속효과)
PRINTL ②：이 카드는 파괴 저항을 가진다。
CALL CARD_ABLE(10,발동誘発)
PRINTL ③：１턴에１번、상대 필드의 몬스터가 효과를 발동했을 때、이 카드의 X 소재를 １개 제거하고 발동할 수 있다。
PRINTL 자신 필드의 카드를 １개 파괴한다。

@CARDCAN_4421(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,4421)
SIF RESULT == 1
	RETURN 0


IF 종류 == EX소환時

	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0

	SIF 마함数(듀얼리스트) >= 5
		RETURN 0
	RETURN 1
ELSEIF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == 발동誘発
	SIF 효과발동済(듀얼리스트,場所,ゾーン)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件종류 = モン全般
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_4421(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC ドロー枚数
#DIM DYNAMIC 破壊数

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == EX소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,4421)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_GENERATE_MAGIC(듀얼리스트,104421,1)
	ENDIF
ELSEIF 종류 == 지속효과
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"破耐")
ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,4421,10,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲필드 = 1
		효과範囲마함 = 1
		CALL CARD_REMOVAL_ADV(듀얼리스트,"単体除去",場所,"묘지")
	ENDIF
ENDIF

;――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

@CARDNAME_104421,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 하얀 장
	CASE "略称"
		RESULTS = 하얀 장
	CASE "カテゴリ"
		RESULTS:0 = 없음
ENDSELECT


@CARD_104421(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"
		RETURN 지속마법
	CASE "トークン"
		RETURN 1
ENDSELECT

RETURN 0

@CARD_EXPLANATION_104421(종류)
#DIM DYNAMIC 종류

CALLFORM CARD_ABLE(0,破壊→묘지,戦闘→묘지)
PRINTL ①：이 카드가 파괴된 경우에 발동할 수 있다。
PRINTL 자신은 ５００ＬＰ회복하고、카드를 １장 드로우하고 "검은 장"을 앞면 표시로 둔다。



@CARDCAN_104421(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,104421)
SIF RESULT == 1
	RETURN 0


IF 종류 == 破壊→묘지 || 종류 == 戦闘→묘지
	RETURN 1
ENDIF


@CARDEFFECT_104421(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ


IF 종류 == 破壊→묘지 || 종류 == 戦闘→묘지
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,104421)
	IF RESULT == 0
		CALL LIFE_CHANGE(듀얼리스트,"増加","효과",500)
		CALL CARDDRAW_BASIC(듀얼리스트,1)

		CALL FULLRESET
		CALL CARD_GENERATE_MAGIC(듀얼리스트,204421,1)
	ENDIF
ENDIF



;――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

@CARDNAME_204421,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 검은 장
	CASE "略称"
		RESULTS = 검은 장
	CASE "カテゴリ"
		RESULTS:0 = 없음
ENDSELECT


@CARD_204421(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "외부자"
		RETURN 1
	CASE "종류"
		RETURN 지속마법
	CASE "トークン"
		RETURN 1
ENDSELECT

RETURN 0

@CARD_EXPLANATION_204421(종류)
#DIM DYNAMIC 종류

CALLFORM CARD_ABLE(0,破壊→묘지,戦闘→묘지)
PRINTL ①：이 카드가 파괴된 경우에 발동할 수 있다。
PRINTL 상대에게 ５００데미지를 주고、상대 필드의 몬스터를 최대 １장을 골라 파괴하고、"하얀 장"을 앞면 표시로 둔다。



@CARDCAN_204421(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,204421)
SIF RESULT == 1
	RETURN 0


IF 종류 == 破壊→묘지 || 종류 == 戦闘→묘지
	RETURN 1
ENDIF


@CARDEFFECT_204421(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ


IF 종류 == 破壊→묘지 || 종류 == 戦闘→묘지
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,204421)
	IF RESULT == 0
		CALL LIFE_CHANGE(듀얼리스트,"減少","효과",500,1)

		CALL FULLRESET
		효과範囲필드 = 1
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"単体除去",場所,"묘지")

		CALL FULLRESET
		CALL CARD_GENERATE_MAGIC(듀얼리스트,104421,1)
	ENDIF
ENDIF
