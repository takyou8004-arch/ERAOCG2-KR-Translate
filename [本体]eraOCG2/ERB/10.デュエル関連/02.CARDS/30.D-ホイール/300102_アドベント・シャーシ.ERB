@CARDNAME_300102,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 어드벤트 섀시
	CASE "略称"
		RESULTS = 어드벤트 섀시
	CASE "カテゴリ"
		RESULTS:0 = 섀시
ENDSELECT


@CARD_300102(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN システム
ENDSELECT

RETURN 0

@CARD_EXPLANATION_300102(종류)
#DIM DYNAMIC 종류

CALLFORM CARD_ABLE(0,소환強制誘発)
PRINTL ①：자신은 EX덱에서 몬스터를 특수 소환할 수 없다。
PRINTL ②：『Top Speed！』（스피드 카운터가 ３개 이상）
PRINTL 자신은 일반 소환에 더해 １번、패의 몬스터를 소환할 수 있다。


@CARDCAN_300102(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,300102)
SIF RESULT == 1
	RETURN 0

IF 종류 == 지속효과
	RETURN 1
ENDIF

@CARDEFFECT_300102(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC カウンタB
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択肢

IF 종류 == 지속효과
	CALL SET_TMP_SS_RST(듀얼리스트,"全般")
	IF GETVAR(@"%듀얼리스트%_SpＣ") >= 3
		CALL FULLRESET
		CALL STATCHANGE_HAND(듀얼리스트,"全体",場所,"召可")
	ENDIF
ENDIF
