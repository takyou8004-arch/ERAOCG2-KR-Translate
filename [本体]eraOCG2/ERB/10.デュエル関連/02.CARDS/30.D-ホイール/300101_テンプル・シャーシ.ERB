@CARDNAME_300101,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 템플 섀시
	CASE "略称"
		RESULTS = 템플 섀시
	CASE "カテゴリ"
		RESULTS:0 = 섀시
ENDSELECT


@CARD_300101(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN システム
ENDSELECT

RETURN 0

@CARD_EXPLANATION_300101(종류)
#DIM DYNAMIC 종류

CALLFORM CARD_ABLE(0,소환強制誘発)
PRINTL ①：스피드 카운터가 ０인 한、자신은 패 및 세트된 마법 / 함정카드를 발동할 수 없다。
PRINTL ②：『Top Speed！』：（스피드 카운터가 ４개）
PRINTL 자신의 마법＆함정 존에 존재하는 카드는、세트된 턴에서도 발동할 수 있다。
PRINTL （이 효과 적용 중에 세트된 카드의 사용 가능 상태는『Top Speed！』해제 후에도 계속된다。）

@CARDCAN_300101(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,300101)
SIF RESULT == 1
	RETURN 0

IF 종류 == 常在無効
	SIF GETVAR(@"%듀얼리스트%_SpＣ") != 0
		RETURN 0
	RETURN 1
ELSEIF 종류 == 지속효과
	RETURN 1
ENDIF

@CARDEFFECT_300101(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC カウンタB
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択肢

IF 종류 == 常在無効

	CALL FULLRESET
	효과条件표시 = 裏側표시
	CALL STATCHANGE_EFFECT(듀얼리스트,"마함",場所,"無効")
ELSEIF 종류 == 지속효과
	IF GETVAR(@"%듀얼리스트%_SpＣ") == 0
		CALL SET_PLAYER_INFO(듀얼리스트,"魔香효과",3)
	ELSEIF GETVAR(@"%듀얼리스트%_SpＣ") == 4
		CALL FULLRESET
		효과条件표시 = 裏側표시
		CALL STATCHANGE_EFFECT(듀얼리스트,"마함",場所,"表",2)
	ENDIF
ENDIF
