@CARDNAME_52184,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 둠즈ＸＩＩ－드라스트리우스
	CASE "略称"
		RESULTS = 드라스트리우스
	CASE "カテゴリ"
		RESULTS:0 = 둠즈
ENDSELECT


@CARD_52184(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN エクシーズ
	CASE "属性"
		RETURN 風属性
	CASE "種族"
		RETURN 機械族
	CASE "レベル"
		RETURN 8
	CASE "攻撃力"
		RETURN 2700
	CASE "守備力"
		RETURN 1500
    CASE "性別"
        RETURN 雄性
	CASE "追加日"
		RETURN 250809
ENDSELECT

RETURN 0
@CARDSUMMON_52184(決闘者,召喚カード,EX参照)
#DIMS DYNAMIC 決闘者
#DIM DYNAMIC 召喚カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要素材数 = 3
素材条件レベル = 8
;追加効果用

IF EX参照 == 1
	CALL XYZCAN(決闘者,召喚カード,52184) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_52184(種類)
#DIM DYNAMIC 種類

PRINTL レベル８モンスター×３
PRINTL このカード名の①②の効果はそれぞれ１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,EX召喚時,特殊召喚時)
PRINTL ①：このカードが特殊召喚した場合に発動できる。
PRINTL フィールドの他の表側表示モンスター１体を装備魔法カード扱いでこのカードに装備する。
CALLFORM CARD_ABLE(0,永続効果)
PRINTL ②：このカードは１ターンに１度だけ戦闘・効果では破壊されない。
CALLFORM CARD_ABLE(3,発動誘発)
PRINTL ③：相手がフィールド・墓地のモンスターの効果を発動した時、
PRINTL このカードのＸ素材を１つ取り除いて発動できる。
PRINTL その効果を無効にする。
PRINTL その後、相手フィールドの表側表示モンスター１体を装備魔法カード扱いでこのカードに装備できる。


@CARDCAN_52184(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件達成

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,52184)
SIF RESULT == 1
	RETURN 0

IF 種類 == EX召喚時 || 種類 == 特殊召喚時
	SIF 発動済_1(決闘者,52184)
		RETURN 0
	SIF 魔罠数(決闘者) >= 5
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果範囲相手戦場 = 1
	効果条件表示 = 表側表示
	効果条件他所 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 種類 == 永続効果
	SIF ゾーン != "戦場"
		RETURN 0
	RETURN 1
ELSEIF 種類 == 発動誘発
	SIF ゾーン != "戦場"
		RETURN 0
	SIF 発動済_3(決闘者,52184)
		RETURN 0
	SIF GETVAR(@"%決闘者%_MZ_Ｘ:場所") <= -1
		RETURN 0
	CALL FULLRESET
	効果条件種類 = モン全般
	効果範囲戦場 = 1
	効果範囲墓地 = 1
	CALL CARD_CHECK_START_DATA(決闘者,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_52184(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == EX召喚時 || 種類 == 特殊召喚時
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,52184,1)
	IF RESULT == 0
		CALL FULLRESET
		効果範囲戦場 = 1
		効果範囲相手戦場 = 1
		効果条件他所 = 1
		CALL CARD_EQUIP_MONSTER_ADV(決闘者,ゾーン,場所,,52184)
	ENDIF
ELSEIF 種類 == 永続効果
	CALL STATCHANGE_EFFECT(決闘者,"自身",場所,"一破")
ELSEIF 種類 == 発動誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,52184,3,x消費1)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(決闘者,"発動無効",場所,"無効")
		IF 魔罠数(決闘者) < 5
			CALL FULLRESET
			効果範囲相手戦場 = 1
			CALL CARD_EQUIP_MONSTER_ADV(決闘者,ゾーン,場所,,52184)
		ENDIF
	ENDIF
ENDIF