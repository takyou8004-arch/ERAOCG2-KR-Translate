@CARDNAME_33152,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 흑익의 마술사
	CASE "略称"
		RESULTS = 흑익의 마술사
	CASE "カテゴリ"
		RESULTS:0 = 버스터
		RESULTS:1 = 마술사
ENDSELECT


@CARD_33152(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 効果モン
	CASE "属性"
		RETURN 風属性
	CASE "種族"
		RETURN 魔法使い族
	CASE "レベル"
		RETURN 3
	CASE "攻撃力"
		RETURN 1300
	CASE "守備力"
		RETURN 1200
	CASE "性別"
		RETURN 牝性
ENDSELECT

RETURN 0

@CARD_EXPLANATION_33152(種類)
#DIM DYNAMIC 種類

PRINTL このカード名の①②の効果はそれぞれ１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,召喚時 ,特殊召喚時)
PRINTL ①：このカードが召喚・特殊召喚に成功した場合に発動できる。
PRINTL デッキから「버스터 모드」１枚を自分フィールドにセットする。
PRINTL このカードはセットしたターンに発動できる。
CALLFORM CARD_ABLE(2,戦場起動)
PRINTL ②：手札を１枚捨て、EXデッキの「버스터」モンスターを１枚見せて発動できる。
PRINTL 見せたモンスターと同じ属性の「버스터」モンスターをデッキから特殊召喚する。

@CARDCAN_33152(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所


CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,33152)
SIF RESULT == 1
	RETURN 0



IF 種類 == 召喚時 || 種類 == 特殊召喚時
	SIF 発動済_1(決闘者,33152)
		RETURN 0
	SIF 魔罠数(決闘者) >= 5
		RETURN 0
	CALL FULLRESET
	効果範囲デッキ = 1
	効果条件名前 = 버스터 모드
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL CARD_DRAW_CHECK(1)
	RETURN 1
ELSEIF 種類 == 戦場起動
	SIF 発動済_2(決闘者,33152)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0

	CALL FULLRESET
	効果範囲手札 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲EXデッキ = 1
	効果条件カテゴリ = 버스터
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲デッキ = 1
	効果条件カテゴリ = 버스터
	効果条件召喚可 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF


@CARDEFFECT_33152(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択属性

IF 種類 == 召喚時 || 種類 == 特殊召喚時
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,33152,1)
	IF RESULT == 0
		CALL FULLRESET
		効果条件名前 = 버스터 모드
		CALL CARD_SET_BASIC(決闘者,"デッキ",ゾーン,場所,"使化裏側表示")
	ENDIF
ELSEIF 種類 == 戦場起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,33152,2)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_ADD_GY_BASIC(決闘者,"手札",ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			効果条件カテゴリ = 버스터
			CALL CARD_CHOOSE_BASIC(決闘者,"EXデッキ",ゾーン,場所,33152)
			選択カード = RESULT
			IF 選択カード != 9999
				CALLFORM CARD_{RESULT:99}("属性")
				選択属性 = RESULT
				CALL FULLRESET
				効果範囲デッキ = 1
				効果条件カテゴリ = 버스터
				効果条件属性 = 選択属性
				CALL CARD_SP_SUMMON_ADV(決闘者,ゾーン,場所)
			ENDIF
		ENDIF
	ENDIF
ENDIF