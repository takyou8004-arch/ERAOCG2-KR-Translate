@CARDNAME_72427,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 앰플리파이어
	CASE "略称"
		RESULTS = Amplifire
	CASE "カテゴリ"
		RESULTS:0 = 사운드 카운터
ENDSELECT


@CARD_72427(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN フィールド
	CASE "レア度"
		RETURN CMN
	CASE "추가日"
		RETURN 250914
ENDSELECT

RETURN 0

@CARD_EXPLANATION_72427(종류)
#DIM DYNAMIC 종류

PRINTL ①：「사운드 워리어」カードの효과が발동する度に、このカードに사운드 카운터を１つ置く。
CALLFORM CARD_ABLE(0,攻守変動)
PRINTL ②：フィールドの「사운드 워리어」モンスターの攻撃力は、このカードの사운드 카운터の数×１００アップする。
PRINTL ③：자신メインフェイズに、자신フィールドの사운드 카운터を以下の数だけ取り除き、その효과を발동できる
PRINTL （このカード名の以下の효과はそれぞれ１ターンに１度しか選択できない）。
CALLFORM CARD_ABLE(1,마함起動)
PRINTL ●３つ：덱から「사운드 워리어」モンスター１体を손패に加える。
CALLFORM CARD_ABLE(2,마함起動)
PRINTL ●５つ：フィールドの「사운드 워리어」カードの数×３００ダメージを상대に与える。
CALLFORM CARD_ABLE(3,마함起動)
PRINTL ●７つ：フィールドの「사운드 워리어」カードの数まで、상대のフィールド・묘지のカードを選んで제외する。


@CARDCAN_72427(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 발동불가,3

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,72427)
SIF RESULT == 1
	RETURN 0

IF 종류 == 마함起動 && GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") >= 2
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	
	CALL FULLRESET
	효과範囲마함 = 1
	효과条件カテゴリ = 사운드 카운터
	효과条件CNT = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	カウンタ = RESULT:9
	
	SIF 발동済_1(듀얼리스트,72427)
		발동불가:0 = 1
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = 사운드 워리어
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:0 = 1
	SIF カウンタ < 3
		발동불가:0 = 1
	
	SIF 발동済_2(듀얼리스트,72427)
		발동불가:1 = 1
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲마함 = 1
	효과範囲상대필드 = 1
	효과範囲상대마함 = 1
	효과条件カテゴリ = 사운드 워리어
	효과条件표시 = 表側표시
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:1 = 1
	SIF カウンタ < 5
		발동불가:1 = 1

	SIF 발동済_3(듀얼리스트,72427)
		발동불가:2 = 1
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲마함 = 1
	효과範囲상대필드 = 1
	효과範囲상대마함 = 1
	효과条件カテゴリ = 사운드 워리어
	효과条件표시 = 表側표시
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:2 = 1
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과範囲상대마함 = 1
	효과範囲상대묘지 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:2 = 1
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		발동불가:2 = 1
	SIF カウンタ < 7
		발동불가:2 = 1
	
	SIF 발동불가:0 == 1 && 발동불가:1 == 1 && 발동불가:2 == 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 효과後強制誘発
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") >= 2
		RETURN 0
	CALL FULLRESET
	효과条件カテゴリ = 사운드 워리어
	CALL CARD_CHECK_FIN_DATA(듀얼리스트,"효과발동",2)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 마함起動 || 종류 == 손패起動
	IF 종류 == 마함起動
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
			RETURN 0
	ENDIF
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1

ELSEIF 종류 == 攻守変動
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") >= 2
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_72427(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 발동불가,3
#DIM DYNAMIC 条件달성
#DIM DYNAMIC 選択カード数
#DIM DYNAMIC 攻撃力修正

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 마함起動 && GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
	CALL FULLRESET
	효과範囲마함 = 1
	효과条件カテゴリ = 사운드 카운터
	효과条件CNT = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	カウンタ = RESULT:9
	
	SIF 발동済_1(듀얼리스트,72427)
		발동불가:0 = 1
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = 사운드 워리어
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:0 = 1
	SIF カウンタ < 3
		발동불가:0 = 1
	
	SIF 발동済_2(듀얼리스트,72427)
		발동불가:1 = 1
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲마함 = 1
	효과範囲상대필드 = 1
	효과範囲상대마함 = 1
	효과条件カテゴリ = 사운드 워리어
	효과条件표시 = 表側표시
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:1 = 1
	SIF カウンタ < 5
		발동불가:1 = 1

	SIF 발동済_3(듀얼리스트,72427)
		발동불가:2 = 1
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲마함 = 1
	효과範囲상대필드 = 1
	효과範囲상대마함 = 1
	효과条件カテゴリ = 사운드 워리어
	효과条件표시 = 表側표시
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:2 = 1
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과範囲상대마함 = 1
	효과範囲상대묘지 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		발동불가:2 = 1
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		발동불가:2 = 1
	SIF カウンタ < 7
		발동불가:2 = 1

	IF 듀얼리스트 == "자신"
		PRINTL 【효과を選択してください】
		PRINTL
		SIF 발동불가:0 != 1
			PRINTL [3] - 덱から「사운드 워리어」モンスター１体を손패に加える。
		SIF 발동불가:1 != 1
			PRINTL [5] - フィールドの「사운드 워리어」カードの数×３００ダメージ与える。
		SIF 발동불가:2 != 1
			PRINTL [7] - フィールドの「사운드 워리어」カードの数まで상대のフィールド・묘지のカードを제외する。
		BINPUT

	ELSE
		RESULT = 9999
		IF 발동불가:0 != 1
			RESULT = 3
		ELSEIF 발동불가:2 != 1
			RESULT = 7
		ELSEIF 발동불가:1 != 1
			RESULT = 5
		ENDIF
	ENDIF

	IF RESULT == 3
		CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,72427,1)
		IF RESULT == 0
			REPEAT 3
				CALL FULLRESET
				효과範囲마함 = 1
				효과条件カテゴリ = 사운드 카운터
				효과条件個Ｃ所持 = 999
				CALL CARD_GRANT_ADV(듀얼리스트,ゾーン,場所,"個Ｃ",-1)
				SIF RESULT == 1
					条件달성++
			REND
			SIF 条件달성 != 3
				RETURN 0
			CALL FULLRESET
			효과範囲덱 = 1
			효과条件カテゴリ = 사운드 워리어
			효과条件종류 = モン全般
			CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
		ENDIF

	ELSEIF RESULT == 5
		CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,72427,2)
		IF RESULT == 0
			REPEAT 5
				CALL FULLRESET
				효과範囲마함 = 1
				효과条件カテゴリ = 사운드 카운터
				효과条件個Ｃ所持 = 999
				CALL CARD_GRANT_ADV(듀얼리스트,ゾーン,場所,"個Ｃ",-1)
				SIF RESULT == 1
					条件달성++
			REND
			SIF 条件달성 != 5
				RETURN 0
			CALL FULLRESET
			효과範囲필드 = 1
			효과範囲마함 = 1
			효과範囲상대필드 = 1
			효과範囲상대마함 = 1
			효과条件カテゴリ = 사운드 워리어
			효과条件표시 = 表側표시
			효과条件CNT = 1
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			CALL LIFE_CHANGE(듀얼리스트,"減少","효과",RESULT*300,1)
		ENDIF
	
	ELSEIF RESULT == 7
		CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,72427,3)
		IF RESULT == 0
			REPEAT 7
				CALL FULLRESET
				효과範囲마함 = 1
				효과条件カテゴリ = 사운드 카운터
				효과条件個Ｃ所持 = 999
				CALL CARD_GRANT_ADV(듀얼리스트,ゾーン,場所,"個Ｃ",-1)
				SIF RESULT == 1
					条件달성++
			REND
			SIF 条件달성 != 7
				RETURN 0
			CALL FULLRESET
			효과範囲필드 = 1
			효과範囲마함 = 1
			효과範囲상대필드 = 1
			효과範囲상대마함 = 1
			효과条件カテゴリ = 사운드 워리어
			효과条件표시 = 表側표시
			효과条件CNT = 1
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			選択カード数 = RESULT

			CALL FULLRESET
			효과範囲상대필드 = 1
			효과範囲상대마함 = 1
			효과範囲상대묘지 = 1
			효과条件複数 = 100 - 選択カード数
			CALL CARD_REMOVAL_ADV(듀얼리스트,"単体除去",場所,"제외")
		ENDIF
	ENDIF

ELSEIF 종류 == 효과後強制誘発
	CALL SET_FIELD_INFO(듀얼리스트,ゾーン,場所,"個Ｃ",1)

ELSEIF 종류 == 마함起動 || 종류 == 손패起動
	;カード발동＆설치まで
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,72427)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所,마함行き)
	ENDIF

ELSEIF 종류 == 攻守変動
	攻撃力修正 = GETVAR(@"%듀얼리스트%_魔_個Ｃ:場所") * 100
	CALL FULLRESET
	효과条件カテゴリ = 사운드 워리어
	CALL STATCHANGE_BASIC(듀얼리스트,"필드",場所,攻撃力修正)
	CALL STATCHANGE_BASIC(対面者,"필드",場所,攻撃力修正)
ENDIF