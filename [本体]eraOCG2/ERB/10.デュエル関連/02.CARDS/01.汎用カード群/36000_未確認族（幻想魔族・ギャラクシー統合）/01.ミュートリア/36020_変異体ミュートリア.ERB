@CARDNAME_36020,参照先
;ココで지정카드의 名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
RESULTS = 변이체 뮤트리아
	CASE "略称"
RESULTS = Myutant M
	CASE "カテゴリ"
RESULTS:0 = 뮤트리아
ENDSELECT


@CARD_36020(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 물속성
	CASE "종족"
		RETURN 미확인족
	CASE "レベル"
		RETURN 1
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 0
ENDSELECT

RETURN 0

@CARD_EXPLANATION_36020(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①②의 효과는 각각 1턴에 1번밖에 사용할 수 없다.
CALL CARD_ABLE(1,손패起動)
PRINTL ①: 자신 필드에 "뮤트리아" 카드가 존재할 경우에 발동할 수 있다.
PRINTL 이 카드를 패에서 특수 소환한다.
CALL CARD_ABLE(2,필드起動)
PRINTL ②: 이 카드를 릴리스하고, 패 / 덱에서 "뮤트리아" 카드 1장을 제외하고 발동할 수 있다.
PRINTL 제외한 카드의 종류에 따라 이하의 몬스터 1장을 패 / 덱에서 특수 소환하고, 자신은 그 몬스터의 원래 공격력만큼의 LP를 잃는다.
PRINTL ● 몬스터: "뮤트리얼 비스트"
PRINTL ● 마법: "뮤트리얼 미스트"
PRINTL ● 함정: "뮤트리얼 암즈"


@CARDCAN_36020(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,36020)
SIF RESULT == 1
	RETURN 0

IF 종류 == 손패起動
	SIF 발동済_1(듀얼리스트,36020)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	CALL CARD_SS_CHECK(듀얼리스트,36020,"손패")
	SIF RESULT == 1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲마함 = 1
	효과条件カテゴリ = 뮤트리아
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 필드起動
	SIF 발동済_2(듀얼리스트,36020)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과範囲손패 = 1
	효과条件カテゴリ = 뮤트리아
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件名前:0 = 뮤트리얼 비스트
	효과条件名前:1 = 뮤트리얼 미스트
	효과条件名前:2 = 뮤트리얼 암즈
	효과条件소환可 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_36020(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 戦闘カード
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件달성
#DIM DYNAMIC 選択カード_ID
CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 손패起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,36020,1)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_SP_SUMMON(듀얼리스트, "손패", 場所)
	ENDIF
ELSEIF 종류 == 필드起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,36020,2)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		CALL FULLRESET
		효과範囲손패 = 1
		효과範囲덱 = 1
		효과条件カテゴリ = 뮤트리아
		효과条件行先플래그 = 제외行き
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			選択カード_ID = RESULT:1
			TRYCALLFORM CARD_{選択カード_ID}, "종류"
			IF INRANGE(RESULT,0,10)
				CALL FULLRESET
				효과範囲손패 = 1
				효과範囲덱 = 1
효과条件名前 = 뮤트리얼 비스트
				CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
				選択カード_ID = RESULT:1
				TRYCALLFORM CARD_{選択カード_ID}, "攻撃力"
				CALL LIFE_CHANGE(듀얼리스트,"減少","コスト",RESULT)
			ELSEIF INRANGE(RESULT,20,29)
				CALL FULLRESET
				효과範囲손패 = 1
				효과範囲덱 = 1
효과条件名前 = 뮤트리얼 미스트
				CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
				選択カード_ID = RESULT:1
				TRYCALLFORM CARD_{選択カード_ID}, "攻撃力"
				CALL LIFE_CHANGE(듀얼리스트,"減少","コスト",RESULT)
			ELSEIF INRANGE(RESULT,30,39)
				CALL FULLRESET
				효과範囲손패 = 1
				효과範囲덱 = 1
효과条件名前 = 뮤트리얼 암즈
				CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
				選択カード_ID = RESULT:1
				TRYCALLFORM CARD_{選択カード_ID}, "攻撃力"
				CALL LIFE_CHANGE(듀얼리스트,"減少","コスト",RESULT)
			ENDIF
		ENDIF
	ENDIF
ENDIF
