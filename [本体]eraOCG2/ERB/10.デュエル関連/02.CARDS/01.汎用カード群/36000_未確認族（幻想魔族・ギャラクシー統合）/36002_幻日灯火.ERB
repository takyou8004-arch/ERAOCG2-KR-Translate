@CARDNAME_36002,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 유에프오라이트
	CASE "略称"
		RESULTS = 환일등화
	CASE "カテゴリ"
		RESULTS:0 = 없음
ENDSELECT


@CARD_36002(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 화염속성
	CASE "종족"
		RETURN 미확인족
	CASE "レベル"
		RETURN 8
	CASE "攻撃力"
		RETURN 1500
	CASE "守備力"
		RETURN 1500
	CASE "추가日"
		RETURN 250804
ENDSELECT

RETURN 0

@CARD_EXPLANATION_36002(종류)
#DIM DYNAMIC 종류

PRINTL このカード名の③の효과は１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ①：バトルフェイズ以外では、フィールドのこのカードを상대は효과の대상にできない。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ②：このカードがモンスターと戦闘を行う場合、その２体はその戦闘では破壊されない。
CALLFORM CARD_ABLE(3,ターン終了時)
PRINTL ③：자신・상대のエンドフェイズに발동できる。
PRINTL 자신は１枚ドローする。
PRINTL その後、자신の손패を任意の数だけ選んで제외し、
PRINTL このカードの攻撃力はこの효과で제외したカードの数×５００アップする。

@CARDCAN_36002(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,36002)
SIF RESULT == 1
	RETURN 0

IF 종류 == ターン終了時
	SIF ゾーン != "필드"
		RETURN 0
	CALL CARD_DRAW_CHECK
	RETURN 1

ELSEIF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	SIF 戦闘場所(듀얼리스트,1) == 5
		RETURN 0
	SIF 戦闘場所(듀얼리스트) != 場所
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_36002(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == ターン終了時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,36002,3)
	IF RESULT == 0
		CALL CARDDRAW_BASIC(듀얼리스트,1)
		$INPUT_LOOP
		PRINTFORMW 제외장수：{攻撃力修正}
		CALL FULLRESET
		효과範囲손패 = 1
		SIF 듀얼리스트 != "자신"
			효과条件종류 = モン全般
		효과条件行先플래그 = 제외行き
		효과条件任意 = 1
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)

		SIF 듀얼리스트 != "자신" && 攻撃力修正 == 1
			GOTO INPUT_LOOP2
		IF RESULT == 1
			攻撃力修正 ++
			GOTO INPUT_LOOP
		ENDIF

		$INPUT_LOOP2
		CALL SET_FIELD_INFO(듀얼리스트,ゾーン,場所,"永攻",攻撃力修正*500)
	ENDIF

ELSEIF 종류 == 지속효과
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"戦耐")
	CALL STATCHANGE_EFFECT(対面者,"自身",戦闘場所(듀얼리스트,1),"戦耐")

	SIF バトルフェイズ != 1
		CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"対耐")
ENDIF
