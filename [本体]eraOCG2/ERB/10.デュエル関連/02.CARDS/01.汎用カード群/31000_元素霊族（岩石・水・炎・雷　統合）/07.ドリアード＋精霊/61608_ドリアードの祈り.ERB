@CARDNAME_61608,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
RESULTS = 드리아드의 기도
	CASE "略称"
RESULTS = 드리아드의 기도
	CASE "カテゴリ"
RESULTS:1 = 의식마법
ENDSELECT


@CARD_61608(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 通常마법
	CASE "추가日"
		RETURN 250420
ENDSELECT

RETURN 0

@CARD_EXPLANATION_61608(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①의 효과는 1턴에 1번밖에 사용할 수 없다.
PRINTL 「엘레멘탈 마스터 드리아드」의 강림에 필요.
CALLFORM CARD_ABLE(0,마함起動,손패起動)
PRINTL ①: 자신의 손패 / 필드 위에서, 레벨의 합계가 3 이상이 되도록 몬스터를 릴리스하고,
PRINTL 손패에서 「엘레멘탈 마스터 드리아드」 1장을 의식 소환한다.
PRINTL 그 후, 자신의 묘지에 땅 / 물 / 화염 / 바람 속성 몬스터가 모여 있는 경우,
PRINTL 덱에서 땅 / 물 / 화염 / 바람 속성 몬스터를 1장씩 골라 손패에 넣는다.


@CARDCAN_61608(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 条件달성,5

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,61608)
SIF RESULT == 1
	RETURN 0
IF 종류 == 마함起動 || 종류 == 손패起動
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0

	CALL RESETAREA_MAT
	素材範囲필드 = 1
	素材範囲손패 = 1
	CALL FULLRESET
	효과範囲손패 = 1
	효과条件종류 = 의식
효과条件名前 = 구세의 미신 노스웸코
	효과条件소환可 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF
@CARDEFFECT_61608(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 条件달성
#DIM DYNAMIC 選択カード_ID

IF 종류 == 마함起動 || 종류 == 손패起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,61608)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)		
		CALL RESETAREA_MAT
		素材範囲손패 = 1
		素材範囲필드 = 1
		CALL FULLRESET
		효과範囲손패 = 1
효과条件名前 = 엘레멘탈 마스터 드리아드
		효과条件소환可 = 1
		CALL CARD_CHOOSE_ADV(듀얼리스트,ゾーン,場所)
		選択カード = RESULT
		選択カード_ID = RESULT:99
		IF 選択カード != 9999
			CALL CARD_SELECT(듀얼리스트,選択カード)
			選択カード = RESULT
			CALL RESETAREA_MAT
			TRYCALLFORM CARDSUMMON_{選択カード_ID}(듀얼리스트,選択カード)
			素材範囲필드 = 1
			素材範囲손패 = 1
			TRYCALLFORM RITSUMMON(듀얼리스트, 選択カード,"손패")

			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件속성 = 땅속성
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 ++

			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件속성 = 물속성
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 ++

			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件속성 = 화염속성
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 ++

			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件속성 = 바람속성
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성 ++
			IF 条件달성 >= 4
				CALL FULLRESET
				효과範囲덱 = 1
				효과条件속성 = 땅속성
				CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)

				CALL FULLRESET
				효과範囲덱 = 1
				효과条件속성 = 물속성
				CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)

				CALL FULLRESET
				효과範囲덱 = 1
				효과条件속성 = 바람속성
				CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)

				CALL FULLRESET
				효과範囲덱 = 1
				효과条件속성 = 화염속성
				CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
			ENDIF
		ENDIF
	ENDIF
ENDIF

