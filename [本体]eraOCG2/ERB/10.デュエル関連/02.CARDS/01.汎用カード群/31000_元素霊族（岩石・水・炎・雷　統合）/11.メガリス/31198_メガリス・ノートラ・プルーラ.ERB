@CARDNAME_31198,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 메가리스 노트라 플루라
	CASE "略称"
		RESULTS = 트라 플루
	CASE "カテゴリ"
		RESULTS:0 = 메가리스
ENDSELECT


@CARD_31198(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 의식
	CASE "속성"
		RETURN 땅속성
	CASE "종족"
		RETURN 원소정령족
	CASE "レベル"
		RETURN 12
	CASE "攻撃力"
		RETURN 3700
	CASE "守備力"
		RETURN 2700
	CASE "추가日"
		RETURN 250726
ENDSELECT

RETURN 0

@CARDSUMMON_31198(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 出処
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照

CALL RESETCONDITION_MAT
必要レベル数 = 12
素材条件종류 = 의식

IF EX参照 == 1
	CALL RITCAN(듀얼리스트,소환カード,31198) 
	RETURN RESULT
ENDIF

@CARD_EXPLANATION_31198(종류)
#DIM DYNAMIC 종류

PRINTL 「메가리스」カードにより降臨
PRINTL このカードは의식モンスターのみを使用した의식소환でしか특수소환できない。
PRINTL このカード名の①②の효과はそれぞれ１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,손패起動)
PRINTL ①：손패のこのカードを상대に見せて발동できる。
PRINTL このターン、자신の「메가리스」カードの효과の발동に対して상대は효과を발동できない。
CALLFORM CARD_ABLE(2,발동誘発)
PRINTL ②：상대がカードの효과を발동した時に발동できる。
PRINTL その발동を無効にし破壊する。
PRINTL その後、자신の묘지の「메가리스」カード２枚を덱に戻し、상대フィールドのモンスター１体をリリースできる。


@CARDCAN_31198(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 条件달성
#DIMS DYNAMIC 対面者


CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,31198)
SIF RESULT == 1
	RETURN 0

IF 종류 == 손패起動
	SIF 발동済_1(듀얼리스트,31198)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1

ELSEIF 종류 == 短紋章起動
	RETURN 1

ELSEIF 종류 == 발동誘発
	SIF 발동済_2(듀얼리스트,31198)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_31198(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 選択カード
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 손패起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,31198,1)
	IF RESULT == 0
		CALL CARD_RESERV(듀얼리스트,31198,短紋章予約)
	ENDIF

ELSEIF 종류 == 短紋章起動
	CALL SET_CAT_SOGAI_INFO(듀얼리스트,"메가리스")

ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,31198,2)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"발동無効",場所,"묘지")

		CALL FULLRESET
		효과範囲상대필드 = 1
		효과条件除去種 = リリース
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		SIF RESULT != 1
			RETURN 0

		CALL FULLRESET
		효과範囲묘지 = 1
		효과条件カテゴリ = 메가리스
		효과条件複数 = 2
		효과条件行先플래그 = 덱行き
		효과条件任意 = 1
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드単体",場所,"リリース")
		ENDIF
		RETURN 1
	ENDIF
	RETURN 0
ENDIF

