@CARDNAME_31223,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 노드 라이제올
	CASE "略称"
		RESULTS = 전 라이제올
	CASE "カテゴリ"
		RESULTS:0 = 라이제올
ENDSELECT


@CARD_31223(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 効果モン
	CASE "属性"
		RETURN 炎属性
	CASE "種族"
		RETURN 元素霊族
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1400
	CASE "守備力"
		RETURN 1600

ENDSELECT
RETURN 0

@CARD_EXPLANATION_31223(種類)
#DIM DYNAMIC 種類

PRINTL このカード名の、①の方法による特殊召喚は１ターンに１度しかできず、②の効果は１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,手札起動)
PRINTL ①：自分のフィールドか墓地にＸモンスターが存在する場合、このカードは手札から特殊召喚できる。
PRINTL この方法で特殊召喚したターン、自分はランク４のＸモンスターしかＥＸデッキから特殊召喚できない。
CALLFORM CARD_ABLE(2,戦場起動)
PRINTL ②：自分の手札・フィールドからカード１枚を墓地へ送り、
PRINTL 「노드 라이제올」以外の自分の墓地の「라이제올」モンスター１体を対象として発動できる。
PRINTL そのモンスターを効果を無効にして守備表示で特殊召喚する。


@CARDCAN_31223(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,31223)
SIF RESULT == 1
	RETURN 0

IF 種類 == 手札起動
	SIF 発動済_1(決闘者,31223)
		RETURN 0
	CALL CARD_SS_CHECK(決闘者,31223,"手札")
	SIF RESULT == 1
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0
	CALL FULLRESET
	効果範囲墓地 = 1
	効果範囲戦場 = 1
	効果条件種類 = エクシーズ
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 種類 == 戦場起動
	SIF 発動済_2(決闘者,31223)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0
	CALL FULLRESET
	効果範囲手札 = 1
	効果範囲戦場 = 1
	効果範囲魔罠 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲墓地 = 1
	効果条件カテゴリ = 라이제올
	効果条件固名前 = 노드 라이제올
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF


@CARDEFFECT_31223(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 手札起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,31223,1)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_SP_SUMMON(決闘者,"手札",場所)
		EX召喚可能レベル = 4
		EX召喚可能種類 = エクシーズ
		CALL CARD_EX_RESTRICTION(決闘者)
	ENDIF
ELSEIF 種類 == 戦場起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,31223,2)
	IF RESULT == 0
		CALL FULLRESET
		IF モンスター数(決闘者) < 5
			効果範囲手札 = 1
			効果範囲魔罠 = 1
		ENDIF
		効果範囲戦場 = 1
		CALL CARD_ADD_GY_ADV(決闘者,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			効果範囲墓地 = 1
			効果条件カテゴリ = 라이제올
			効果条件固名前 = 노드 라이제올
			効果追加種類 = 効追無効
			CALL CARD_SP_SUMMON_ADV(決闘者,ゾーン,場所,"守備表示")
		ENDIF
	ENDIF
ENDIF


