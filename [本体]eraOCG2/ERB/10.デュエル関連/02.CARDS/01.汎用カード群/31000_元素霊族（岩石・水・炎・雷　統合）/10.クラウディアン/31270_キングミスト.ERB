@CARDNAME_31270,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 킹 미스트
	CASE "略称"
		RESULTS = 킹 미스트
	CASE "カテゴリ"
		RESULTS:0 = 없음
ENDSELECT


@CARD_31270(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 물속성
	CASE "종족"
		RETURN 원소정령족
	CASE "レベル"
		RETURN 7
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 0
	CASE "성별"
		RETURN 雄性
	CASE "추가日"
		RETURN 250630
ENDSELECT
RETURN 0

@CARD_EXPLANATION_31270(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의、①②의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(0,攻守変動)
PRINTL ①：이 카드를 소환할 때의 레벨은、자신의 필드 / 묘지의 일반 몬스터의 수만큼 내린다。
CALLFORM CARD_ABLE(0,常在無効)
PRINTL ②：자신 턴 동안、상대 필드의 몬스터의 효과는 무효화된다。
CALLFORM CARD_ABLE(0,攻撃時強制誘発)
PRINTL ③：이 카드가 전투를 행한 경우에 강제적으로 발동한다。
PRINTL 이 카드의 공격력은 １이 되며、전투하는 상대 몬스터의 공격력을 １로 하고、이 카드는 전투내성을 얻는다。


@CARDCAN_31270(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,31270)
SIF RESULT == 1
	RETURN 0

IF 종류 == 攻守変動
	SIF ゾーン != "손패"
		RETURN 0
	RETURN 1
ELSEIF 종류 == 常在無効
	SIF ゾーン != "필드"
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻撃時強制誘発
	SIF ゾーン != "필드"
		RETURN 0
	SIF 戦闘場所(듀얼리스트) != 場所
		RETURN 0
	SIF 戦闘場所(듀얼리스트,1) == 5
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_31270(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 軽減数
#DIM DYNAMIC 戦闘カード

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 攻守変動
	CALL FULLRESET
	효과条件종류 = 通常モン
	효과範囲필드 = 1
	효과範囲마함 = 1
	효과範囲묘지 = 1
	효과条件CNT = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	軽減数 = RESULT * 1
	CALL FULLRESET
	CALL STATCHANGE_HAND(듀얼리스트,"지정",場所,"一星",軽減数)
ELSEIF 종류 == 소환時 || 종류 == 특수소환時
		CALL FULLRESET
		CALL STATCHANGE_EFFECT(듀얼리스트,"상대필드",場所,"無効",0,31270)
ELSEIF 종류 == 常在無効
	戦闘カード = 戦闘場所(듀얼리스트)
	CALL SET_FIELD_INFO(듀얼리스트,"필드",戦闘カード,"攻固",1,上書きモード)

	戦闘カード = 戦闘場所(듀얼리스트,1)
	CALL SET_FIELD_INFO(듀얼리스트,"필드",戦闘カード,"攻固",1,上書きモード,1)
ENDIF


