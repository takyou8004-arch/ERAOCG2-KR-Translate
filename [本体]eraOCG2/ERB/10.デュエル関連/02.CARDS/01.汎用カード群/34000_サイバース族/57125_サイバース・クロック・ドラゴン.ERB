@CARDNAME_57125,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 사이버스 클락 드래곤
	CASE "略称"
		RESULTS = Ｃ Ｃ 드래곤
	CASE "カテゴリ"
		RESULTS:0 = 사이버스
ENDSELECT


@CARD_57125(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 융합
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 사이버스족
	CASE "レベル"
		RETURN 7
	CASE "攻撃力"
		RETURN 2500
	CASE "守備力"
		RETURN 2000
	CASE "추가日"
		RETURN 250908
ENDSELECT

RETURN 0

@CARD_EXPLANATION_57125(종류)
#DIM DYNAMIC 종류

PRINTL "클락 와이반"＋링크 몬스터 １장 이상
CALL CARD_ABLE(0,EX소환時)
PRINTL ①：이 카드의 융합 소환에 성공한 경우、이 카드가 소재로 한 L몬스터의 수 만큼、
PRINTL 자신 묘지의 L몬스터를 대상으로 하고 발동할 수 있다。
PRINTL 그 카드의 링크수 만큼、자신의 덱 위에서 카드를 묘지로 보낸다。
PRINTL 다음 턴 종료시 까지、다른 자신의 몬스터는 공격하지 못하며、
PRINTL 이 카드의 공격력은 이 효과로 묘지로 보낸 수×１０００ 올린다。
CALL CARD_ABLE(0,지속효과)
PRINTL ②：자신 필드에 링크 몬스터가 존재하는 한、
PRINTL 자신 필드의 다른 몬스터는 공격 회피와 대상 내성을 얻는다。

CALL CARD_ABLE(0,戦破壊→묘지)
PRINTL ③：융합 소환한 이 카드가 상대의 효과로 묘지로 보내질 경우에 발동할 수 있다。
PRINTL 덱에서 마법 카드 １장을 패에 넣는다。

@CARDSUMMON_57125(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
;素材条件

CALL RESETCONDITION_MAT
必要素材数 = 99
素材条件数自由 = 1
素材条件素材数起動 = 1

IF EX参照 == 0 || EX参照 == 1
	素材条件名前 = 클락 와이반
ELSEIF EX参照 >= 10
	素材条件종류 = 링크
ENDIF

IF EX参照 == 1
	CALL FUSCAN(듀얼리스트,소환カード,57125) 
	RETURN RESULT
ENDIF

@CARDCAN_57125(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 条件달성

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,57125)
SIF RESULT == 1
	RETURN 0


IF 종류 == EX소환時
	CALL FULLRESET
	효과範囲묘지 = 1
	효과条件종류 = 링크
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 지속효과
	SIF !GROUPMATCH(ゾーン,"필드")
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件종류 = 링크
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 戦破壊→묘지
	SIF 処理中_듀얼리스트 == 듀얼리스트
		RETURN 0
	SIF !GROUPMATCH(묘지送り종류,"破壊","リリース")
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件종류 = 마법全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_57125(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 選択カード,100
#DIM DYNAMIC 選択カード_ID
#DIM DYNAMIC 素材数
#DIM DYNAMIC 選択数
#DIM DYNAMIC 選択レベル
IF 종류 == EX소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,57125)
	IF RESULT == 0

		素材数 = GETVAR(@"%듀얼리스트%_MZ_汎数:場所") - 1

		CALL FULLRESET
		효과条件종류 = 링크
		효과条件複数 = 100 - 素材数
		CALL CARD_CHOOSE_BASIC(듀얼리스트,"묘지",ゾーン,場所)	
		FOR カウンタ,0,10
			選択カード:カウンタ = RESULT:カウンタ
		NEXT
		IF 選択カード != 9999
			選択数 = RESULT:100
			FOR カウンタ,0,選択数
				TRYCALLFORM CARD_{GETVAR(@"%듀얼리스트%_묘지:(選択カード:カウンタ)")}, "レベル"
				選択レベル += RESULT
			NEXT

			攻撃力修正 = 選択レベル * 1000
			CALL CARD_DECK_DISPOSE(듀얼리스트,"덱",選択レベル)
			CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"永攻",攻撃力修正)

			CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"単攻",1)
		ENDIF
	ENDIF
ELSEIF 종류 == 지속효과
	CALL FULLRESET
	효과条件他所 = 1
	CALL STATCHANGE_EFFECT(듀얼리스트,"필드",場所,"対耐")

	CALL FULLRESET
	효과条件他所 = 1
	CALL STATCHANGE_EFFECT(듀얼리스트,"필드",場所,"攻避")

ELSEIF 종류 == 戦破壊→묘지
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,57125)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲덱 = 1
		효과条件종류 = 마법全般
		CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
	ENDIF
ENDIF



