@CARDNAME_23521,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 전갈-가시의 미네
	CASE "略称"
		RESULTS = 가시의 미네
	CASE "カテゴリ"
		RESULTS:0 = 전갈
ENDSELECT


@CARD_23521(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	;효과モン/通常モン/의식/융합/싱크로/엑시즈/링크/초차원
	CASE "종류"
		RETURN 효과モン
	;어둠속성/빛속성/땅속성/물속성/화염속성/바람속성
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 전사족
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1000
	CASE "守備力"
		RETURN 1800
	;牝性/雄性があるよ
	CASE "성별"
		RETURN 牝性
	CASE "추가日"
		RETURN 250917
ENDSELECT
RETURN 0

@CARD_EXPLANATION_23521(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①②의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,손패起動)
PRINTL ①：자신의 "돈 잘우그" 필드에 존재하는 경우、발동할 수 있다。
PRINTL 이 카드를 패에서 특수 소환한다。
CALLFORM CARD_ABLE(2,소환時,특수소환時)
PRINTL ②：이 카드가 소환 / 특수 소환된 경우에 발동할 수 있다。
PRINTL 자신의 덱 / 묘지에서 "전갈" 카드 １장을 패에 넣는다。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ③：이 카드가 몬스터 존에 존재하는 한、자신의 "돈 잘우그"는 대상 내성을 얻는다。


@CARDCAN_23521(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,23521)
SIF RESULT == 1
	RETURN 0

IF 종류 == 손패起動
    SIF 발동済_1(듀얼리스트,23521)
		RETURN 0
    CALL CARD_SS_CHECK(듀얼리스트,23521,"손패",1)
	SIF RESULT == 1
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件名前 = 돈 잘우그
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 소환時 || 종류 == 특수소환時
	SIF 발동済_2(듀얼리스트,23521)
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과範囲묘지 = 1
	효과条件カテゴリ = 전갈
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1	

ELSEIF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1



ENDIF	


@CARDEFFECT_23521(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 選択カード

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 손패起動
    CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,23521,1)
	IF RESULT == 0
	    CALL FULLRESET
		CALL CARD_SP_SUMMON(듀얼리스트, "손패", 場所)	
	ENDIF

ELSEIF 종류 == 소환時 || 종류 == 특수소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,23521,2)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲덱 = 1
	    효과範囲묘지 = 1
		효과条件カテゴリ = 전갈
		CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
	ENDIF	

ELSEIF 종류 == 지속효과
    CALL FULLRESET
	효과条件名前 = 돈 잘우그
	CALL STATCHANGE_EFFECT(듀얼리스트,"필드",場所,"対耐")
	

ENDIF
