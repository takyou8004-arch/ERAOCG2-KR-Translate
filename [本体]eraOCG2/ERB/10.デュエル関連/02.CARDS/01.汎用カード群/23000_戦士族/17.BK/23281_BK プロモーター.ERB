@CARDNAME_23281,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = BK 프로모터
	CASE "略称"
		RESULTS = BK프로모터
	CASE "カテゴリ"
		RESULTS:0 = BK
ENDSELECT


@CARD_23281(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 効果モン
	CASE "属性"
		RETURN 炎属性
	CASE "種族"
		RETURN 戦士族 
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1700
	CASE "守備力"
		RETURN 800
	CASE "追加日"
		RETURN 250707
ENDSELECT

RETURN 0

@CARD_EXPLANATION_23281(種類)
#DIM DYNAMIC 種類

PRINTL このカード名の①②③の効果はそれぞれ１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,手札起動)
PRINTL ①：相手フィールドにモンスターが存在する場合に発動できる。
PRINTL このカードを手札から特殊召喚する。
CALLFORM CARD_ABLE(2,戦場起動)
PRINTL ②：このカードをリリースして発動できる。（この効果を発動するターン、自分は戦士族モンスターしか特殊召喚できない）
PRINTL デッキから「ＢＫ 프로모터」以外の「ＢＫ」モンスターを２体まで特殊召喚する（同名カードは１枚まで）。
CALLFORM CARD_ABLE(3,墓地起動)
PRINTL ②：墓地のこのカードを除外して発動できる。
PRINTL 自分フィールドの「ＢＫ」モンスターのレベルを全て１つ上げる。

@CARDCAN_23281(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者
CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 手札起動
	SIF 発動済_1(決闘者,23281)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0
	CALL FULLRESET
	CALL CARD_SS_CHECK(決闘者,23281,"手札")
	SIF RESULT == 1
		RETURN 0
	CALL FULLRESET
	効果範囲相手戦場 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 種類 == 戦場起動
	SIF 発動済_2(決闘者,23281)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0
	SIF ゾーン  != "戦場"
		RETURN 0
	CALL FULLRESET
	CALL 効果条件種族_ADV(戦士族, "以外")
	CALL CARD_CHECK_FIN_DATA(決闘者,"SS履歴")
	SIF RESULT != 0
		RETURN 0
	CALL FULLRESET
	効果範囲デッキ = 1
	効果条件カテゴリ = BK
	効果条件固名前 = BK 프로모터
	効果条件召喚可 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 種類 == 墓地起動
	SIF 発動済_3(決闘者,23281)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0
	SIF ゾーン  != "墓地"
		RETURN 0
	SIF GETVAR(@"%決闘者%_除外NG置換") >= 1
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件カテゴリ = BK
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_23281(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 手札起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,23281,1)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_SP_SUMMON(決闘者, "手札", 場所)
	ENDIF
ELSEIF 種類 == 戦場起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,23281,2)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		効果範囲デッキ = 1
		効果条件カテゴリ = BK
		効果条件固名前 = BK 프로모터
		効果条件固名前選択 = 1
		効果条件任意 = 1
		効果条件複数 = 98
		CALL CARD_SP_SUMMON_ADV(決闘者,ゾーン,場所)
		SS召喚可能種族 = 戦士族
		CALL CARD_SS_RESTRICTION(決闘者)
	ENDIF
ELSEIF 種類 == 墓地起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,23281,3)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		FOR カウンタ,0,5
			SIF GETVAR(@"%決闘者%_戦場:カウンタ") == -1
				CONTINUE
			SIF GETVAR(@"%決闘者%_MZ_表:カウンタ") == 3
				CONTINUE
			TRYCALLFORM CARDNAME_{GETVAR(@"%決闘者%_戦場:カウンタ")},"カテゴリ"
			SIF !MATCH(RESULTS,"BK")
				CONTINUE
			TRYCALLFORM CARD_{GETVAR(@"%決闘者%_戦場:カウンタ")},"種類"
			SIF MATCH(RESULT,エクシーズ)
				CONTINUE
			CALL SET_FIELD_INFO(決闘者,"戦場",カウンタ,"永星",1)
		NEXT
	ENDIF
ENDIF