@CARDNAME_26002,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 크로스로드 러너
	CASE "略称"
		RESULTS = 로드 러너
	CASE "カテゴリ"
		RESULTS:0 = 파워 오브 펠로우즈
		;「정크 워리어」のカード名が記されたモンスター
ENDSELECT


@CARD_26002(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 땅속성
	CASE "종족"
		RETURN 비행야수족
	CASE "レベル"
		RETURN 2
	CASE "攻撃力"
		RETURN 300
	CASE "守備力"
		RETURN 300
	CASE "추가日"
		RETURN 250906
ENDSELECT

RETURN 0

@CARD_EXPLANATION_26002(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①의 효과는 １턴에 １번밖에 사용할 수 없다。
CALL CARD_ABLE(1,효과後誘発)
PRINTL ①：튜너가 자신의 묘지로 보내진 경우에 발동할 수 있다。
PRINTL 이 카드를 패 / 묘지에서 특수 소환한다。
PRINTL 이 카드를 제외한、"정크 워리어" 또는 "파워 오브 펠로우즈"몬스터가 필드에 존재하는 경우,
PRINTL 추가로 상대 필드의 공격력 １９００이상인 몬스터를 전부 수비 표시로 한다。
PRINTL 이 효과로 특수 소환한 이 카드는 필드에서 벗어날 경우에 제외된다。
PRINTL 이 턴、자신은 S몬스터밖에 EX덱에서 특수 소환할 수 없다。

@CARDCAN_26002(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,26002)
SIF RESULT == 1
	RETURN 0

IF 종류 == 효과後誘発
	SIF 발동済_1(듀얼리스트,26002)
		RETURN 0
	CALL FULLRESET
	CALL CARD_SS_CHECK(듀얼리스트,26002,ゾーン)
	SIF RESULT == 1
		RETURN 0
	CALL FULLRESET
	효과条件튜너 = 1
	CALL CARD_CHECK_FIN_DATA(듀얼리스트,"묘지送り全般+H")
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_26002(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択レベル
CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 효과後誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,26002,1)
	IF RESULT == 0
		CALL FULLRESET
		효과추가종류 = 2
		CALL CARD_SP_SUMMON(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			효과範囲필드 = 1
			효과条件スーパー名前 = 정크 워리어
			효과条件カテゴリ = 파워 오브 펠로우즈
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			IF RESULT == 1
				CALL FULLRESET
				효과条件以攻撃力 = 1900
				효과추가종류 = 2
				CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드全体",場所,"표시변경")
			ENDIF
		ENDIF
		EX소환可能종류 = 싱크로
		CALL CARD_EX_RESTRICTION(듀얼리스트)
	ENDIF
ENDIF


