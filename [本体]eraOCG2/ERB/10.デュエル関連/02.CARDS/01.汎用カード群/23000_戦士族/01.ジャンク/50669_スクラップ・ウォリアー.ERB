@CARDNAME_50669,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 스크랩 워리어
	CASE "略称"
		RESULTS = 스크랩 Ｗ
	CASE "カテゴリ"
		RESULTS:0 = 스크랩
		RESULTS:1 = 워리어
		RESULTS:2 = 파워 오브 펠로우즈
		;「정크 워리어」のカード名が記されたモンスター
ENDSELECT


@CARD_50669(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 싱크로
	CASE "속성"
		RETURN 땅속성
	CASE "종족"
		RETURN 전사족
	CASE "レベル"
		RETURN 3
	CASE "攻撃力"
		RETURN 1700
	CASE "守備力"
		RETURN 100
	CASE "성별"
		RETURN 雄性
	CASE "추가日"
		RETURN 250906
ENDSELECT

RETURN 0

@CARDSUMMON_50669(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要レベル数 = 3
IF EX参照 == 0 || EX参照 == 1
	素材条件튜너 = 1
ELSEIF EX参照 >= 10
	素材条件튜너 = -1
ENDIF

IF EX参照 == 1
	CALL SYNCAN(듀얼리스트,소환カード,50669) 
	RETURN RESULT
ENDIF


@CARD_EXPLANATION_50669(종류)
#DIM DYNAMIC 종류

PRINTL 튜너＋튜너以外のモンスター１体以上
PRINTL このカード名の①の효과は１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(0,EX소환時)
PRINTL ①：このカードがS소환した場合に발동できる。
PRINTL 「정크 싱크론」１体または「파워 오브 펠로우즈」カード１枚を덱から손패に加える事ができる。
PRINTL 손패に加えなかった場合、代わりに「정크 싱크론」１体または「파워 오브 펠로우즈」カード１枚を묘지へ送る。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ②：「스크랩 워리어」以外の자신フィールドの以下のモンスターが발동した효과に対して상대は마법・함정・モンスターの효과を발동できない。
PRINTL ●「파워 오브 펠로우즈」モンスター
PRINTL ●「워리어」モンスター



@CARDCAN_50669(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,50669)
SIF RESULT == 1
	RETURN 0

IF 종류 == EX소환時
	SIF 발동済_1(듀얼리스트,50669)
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件スーパー名前 = 정크 싱크론
	효과条件カテゴリ = 파워 오브 펠로우즈
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_50669(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM 選択カード


CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == EX소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,50669,1)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲덱 = 1
		효과条件スーパー名前 = 정크 싱크론
		효과条件カテゴリ = 파워 오브 펠로우즈
		효과条件任意 = 1
		CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT != 1
			CALL FULLRESET
			효과範囲덱 = 1
			효과条件スーパー名前 = 정크 싱크론
			효과条件カテゴリ = 파워 오브 펠로우즈
			CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		ENDIF
	ENDIF
ELSEIF 종류 == 지속효과
	CALL SET_CAT_SOGAI_INFO(듀얼리스트,"파워 오브 펠로우즈")
	CALL SET_CAT_SOGAI_INFO(듀얼리스트,"워리어")
ENDIF
