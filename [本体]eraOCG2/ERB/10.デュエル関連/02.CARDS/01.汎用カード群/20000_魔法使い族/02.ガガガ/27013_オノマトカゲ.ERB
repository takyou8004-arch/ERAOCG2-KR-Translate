@CARDNAME_27013,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 오노마토 도마뱀
	CASE "略称"
		RESULTS = 오노마토 도마뱀
	CASE "カテゴリ"
		RESULTS:0 = 오노마토
		;このカード名はルール上「주바바」、「가가가」、「고고고」、「도도도」カードとしても扱う。
		RESULTS:1 = 주바바
		RESULTS:2 = 가가가
		RESULTS:3 = 고고고
		RESULTS:4 = 도도도
ENDSELECT


@CARD_27013(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 비늘야수족
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1100
	CASE "守備力"
		RETURN 1500
	CASE "추가日"
		RETURN 250726
ENDSELECT

RETURN 0

@CARD_EXPLANATION_27013(종류)
#DIM DYNAMIC 종류

PRINTL このカード名の①②の효과はそれぞれ１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,손패起動,묘지起動)
PRINTL ①：このカードが손패・묘지に存在し、
PRINTL 자신フィールドに「주바바」、「가가가」、「고고고」、「도도도」モンスターのいずれかが存在する場合に발동できる
PRINTL このカードを특수소환する。
PRINTL この효과で특수소환したこのカードは、フィールドから離れた場合に제외される。
PRINTL ②：묘지のこのカードの효과を발동し、このカードを특수소환しなかった場合に適用する。
PRINTL 묘지のこのカードを제외し、자신の묘지からXモンスターを２体までEX덱に戻す。


@CARDCAN_27013(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,27013)
SIF RESULT == 1
	RETURN 0

IF 종류 == 손패起動
	SIF 발동済_1(듀얼리스트,27013)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	CALL CARD_SS_CHECK(듀얼리스트,27013,ゾーン)
	SIF RESULT == 1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件カテゴリ:0 = 주바바
	효과条件カテゴリ:1 = 가가가
	효과条件カテゴリ:2 = 고고고
	효과条件カテゴリ:3 = 도도도
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 묘지起動
	SIF 발동済_1(듀얼리스트,27013)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_27013(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ



IF 종류 == 손패起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,27013,1)
	IF RESULT == 0
		CALL FULLRESET
		효과추가종류 = 2
		CALL CARD_SP_SUMMON(듀얼리스트,ゾーン,場所)
	ENDIF
ELSEIF 종류 == 묘지起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,27013)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲묘지 = 1
		효과条件名前 = 오노마토 도마뱀
		효과추가종류 = 2
		효과条件任意 = 1
		CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT != 1
			CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
			IF RESULT == 1
				CALL FULLRESET
				효과条件종류 = 엑시즈
				효과条件複数 = 98
				효과条件行先플래그 = 덱行き
				CALL CARD_ADD_GY_BASIC(듀얼리스트,"묘지",ゾーン,場所)
				SIF RESULT == 1
					CALL CARD_EFFECT_USE(듀얼리스트,27013,2)
			ENDIF
		ELSE
			CALL CARD_EFFECT_USE(듀얼리스트,27013,1)
		ENDIF
	ENDIF
ENDIF
