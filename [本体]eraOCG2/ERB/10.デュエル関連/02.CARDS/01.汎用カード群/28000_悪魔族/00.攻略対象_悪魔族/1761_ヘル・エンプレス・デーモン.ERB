@CARDNAME_1761,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 헬 임프레스 데몬
	CASE "略称"
		RESULTS = 헬 임프레스
	CASE "カテゴリ"
		RESULTS:0 = 데몬
ENDSELECT


@CARD_1761(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 악마족
	CASE "レベル"
		RETURN 8
	CASE "攻撃力"
		RETURN 2900
	CASE "守備力"
		RETURN 2100
ENDSELECT

RETURN 0

@CARD_EXPLANATION_1761(종류)
#DIM DYNAMIC 종류

CALLFORM CARD_ABLE(0,身代효과)
PRINTL ①：이 카드 이외의 자신 필드의 악마족・어둠속성몬스터 １장 が파괴되어 る경우、
PRINTL 대신에 자신 묘지의 악마족・어둠속성몬스터 １장을 게임에서 제외する事ができる。
CALLFORM CARD_ABLE(0,戦破壊→묘지)
PRINTL ②：필드의 이 카드가 파괴된 경우에 발동할 수 있다。
PRINTL 「헬 임프레스 데몬」이외의 자신 묘지의 악마족・어둠속성・レベル６以上の몬스터 １장을 특수 소환한다。
PRINTL 

@CARDCAN_1761(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,1761)
SIF RESULT == 1
	RETURN 0

IF 종류 == 身代효과
	SIF ゾーン != "필드"
		RETURN 0
	SIF !GROUPMATCH(破壊종류,"효과","戦闘")
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		RETURN 0
	TRYCALLFORM CARD_{破壊대상ID}("종족")
	SIF RESULT != 악마족
		RETURN 0
	TRYCALLFORM CARD_{破壊대상ID}("속성")
	SIF RESULT != 어둠속성
		RETURN 0
	SIF 破壊대상ゾーン != "필드"
		RETURN 0
	SIF 破壊대상場所 == 場所
		RETURN 0
	CALL FULLRESET
	효과範囲묘지 = 1
	효과条件종족 = 악마족
	효과条件속성 = 어둠속성
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 戦破壊→묘지
	CALL FULLRESET
	효과範囲묘지 = 1
	효과条件固名前 = 헬 임프레스 데몬
	효과条件종족 = 악마족
	효과条件속성 = 어둠속성
	효과条件종류 = モン全般
	CALLFORM 효과条件レベル_ADV(6,"以上")
	효과条件소환可 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_1761(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード

IF 종류 == 身代효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,1761)
	IF RESULT == 0
		CALL FULLRESET
		효과条件종족 = 악마족
		효과条件속성 = 어둠속성
		효과条件종류 = モン全般
		CALL CARD_ADD_GY_BASIC(듀얼리스트,"묘지",ゾーン,場所)
		IF RESULT == 1
			RETURN 1
		ENDIF
	ENDIF
	RETURN 0
ELSEIF 종류 == 戦破壊→묘지
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,1761)
	IF RESULT == 0
		CALL FULLRESET
		효과条件固名前 = 헬 임프레스 데몬
		효과条件종족 = 악마족
		효과条件속성 = 어둠속성
		효과条件종류 = モン全般
		CALLFORM 효과条件レベル_ADV(6,"以上")
		CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
	ENDIF
ENDIF

