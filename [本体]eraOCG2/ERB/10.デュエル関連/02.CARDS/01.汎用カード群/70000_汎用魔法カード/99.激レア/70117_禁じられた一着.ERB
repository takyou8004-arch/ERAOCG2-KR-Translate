@CARDNAME_70117,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 금지된 한벌
	CASE "略称"
		RESULTS = 금지된 한벌
	CASE "カテゴリ"
		RESULTS:0 = 금지된
		RESULTS:1 = 게임 오리지널
ENDSELECT


@CARD_70117(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 速攻魔法
ENDSELECT

RETURN 0

@CARD_EXPLANATION_70117(種類)
#DIM DYNAMIC 種類

PRINTL 이 카드명의 카드는 １턴에 １장밖에 발동할 수 없으며、
PRINTL 이 카드의 효과의 발동에 대해 상대는 효과를 발동할 수 없다。
CALLFORM CARD_ABLE(1,魔罠起動 ,手札起動,フリー誘発)
PRINTL ①：필드의 앞면 표시 몬스터 １장을 대상으로 하고 발동할 수 있다。
PRINTL 그 몬스터의 공격력은 턴 종료시까지、공격력이 ０이 되며、철벽내성（전투 / 효과파괴내성＋대상내성）을 얻는다。
 


@CARDCAN_70117(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,70117)
SIF RESULT == 1
	RETURN 0

IF 種類 == 魔罠起動 || 種類 == 手札起動 || 種類 == フリー誘発
	IF ゾーン == "手札"
		SIF ターンプレイヤー != 決闘者
			RETURN 0
	ELSEIF ゾーン == "魔罠"
		SIF GETVAR(@"%決闘者%_魔_表:場所") == 3
			RETURN 0
	ELSE
		RETURN 0
	ENDIF
	SIF 発動済_1(決闘者,70117)
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果範囲相手戦場 = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所,70117)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 種類 == チェーン可否
	RETURN 1
ENDIF


@CARDEFFECT_70117(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC リピート数
IF 種類 == 魔罠起動 || 種類 == 手札起動 || 種類 == フリー誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,70117,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		効果条件表示 = 表側表示
		効果追加種類 = 22
		CALL CARD_REMOVAL_BASIC(決闘者,"両戦場単体",場所,"一攻DOWN",99999)

	ENDIF
ENDIF