@CARDNAME_99665,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 이쿠기어 프리즈너 컨테너
	CASE "略称"
		RESULTS = 광서암괴농상
	CASE "カテゴリ"
		RESULTS:0 = 광서의 기계
		RESULTS:1 = E.R.A
ENDSELECT


@CARD_99665(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 지속함정
	CASE "추가日"
		RETURN 250706
ENDSELECT

RETURN 0

@CARD_EXPLANATION_99665(종류)
#DIM DYNAMIC 종류

CALL TEXT_DECORATION("ROGUE")
PRINTL
PRINTL 이 카드명의 ②③의 효과는 각각 1턴에 1번밖에 사용할 수 없다.
CALLFORM CARD_ABLE(1,攻守変動)
PRINTL ①: 이 카드가 마법 / 함정 존에 존재하는 한, 상대 배틀 페이즈 동안, 상대 필드의 모든 미약카운터를 가진 몬스터는
PRINTL 공격 표시가 되고, 공격 가능할 경우 반드시 공격해야 한다.
CALLFORM CARD_ABLE(2,마함起動)
PRINTL ②: 자신 / 상대의 메인 페이즈에, 필드 위의 성별을 가진 몬스터 1장을 대상으로 발동할 수 있다.
PRINTL 그 몬스터에게 미약카운터를 1개 놓는다.
CALLFORM CARD_ABLE(3,발동誘発)
PRINTL ③: 자신 / 상대의 배틀 페이즈 중에 상대가 몬스터 효과를 발동했을 때, 자신 필드 위의 "광서의 기계" 몬스터 1장을 파괴하여 발동할 수 있다.
PRINTL 그 발동을 무효로 한다.
PRINTL
SETCOLOR C_RED
PRINTL ＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊ーー경고ーー＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊
CALLFORM CARD_ABLE(4,ターン終了時)
PRINTL ④: 자신의 턴 종료시에 발동할 수 있다.
PRINTL 기계성에 붙잡혀 있는 암컷 구멍들을 범해, 기계를 계속 활성화시키며,
PRINTL 상대 플레이어의 패에 강제력을 가진 손패를 추가하게 한다.
PRINTL ＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊ーー주의ーー＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊
PRINTL
RESETCOLOR
CALL TEXT_DECORATION("ROGUE")


@CARDCAN_99665(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 조교대상
#DIM DYNAMIC 효과条件플래그 = 0

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,99665)
SIF RESULT == 1
RETURN 0

IF 종류 == 攻守変動
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") != 1
		RETURN 0
	SIF ターンプレイヤー == 듀얼리스트
		RETURN 0
	SIF バトルフェイズ != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 마함起動 && GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
	SIF ゾーン != "마함"
		RETURN 0
	SIF 발동済_2(듀얼리스트,99665)
		RETURN 0
    SIF メインフェイズ != 1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
    효과範囲상대필드 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所,99665)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 발동誘発
	SIF 발동済_3(듀얼리스트,99665)
		RETURN 0
	SIF ゾーン != "마함"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") != 1
		RETURN 0
	SIF バトルフェイズ != 1
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件カテゴリ = 광서의 기계
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과条件종류 = モン全般
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == ターン終了時
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	SIF ゾーン != "마함"
		RETURN 0
	SIF 발동済_4(듀얼리스트,99665)
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 마함起動 
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 3	
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_99665(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 조교대상
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 戦闘カードID
#DIM DYNAMIC 효과条件플래그 = 0

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 마함起動 && GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99665,2)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲필드 = 1
        효과範囲상대필드 = 1
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"両필드単体",場所,"選択")
		IF RESULT == 1
			選択カード = RESULT:1
			CALL CARD_SELECT(듀얼리스트,選択カード)
			CALL SET_FIELD_INFO(대상者（CS）,"필드",選択カード（CS）,"薬Ｃ",1) 
		ENDIF
	ENDIF

ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99665,3)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲필드 = 1
		효과条件カテゴリ = 광서의 기계
        효과条件行先플래그 = 破壊行き
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"발동無効",場所,"無効")
			RETURN 1
		ENDIF
	ENDIF

ELSEIF 종류 == 攻守変動
	CALL SET_FIELD_MAGIC_INFO(듀얼리스트, "기계감금암농상")
	CALL FULLRESET
	효과条件薬Ｃ所持 = 1
	CALL STATCHANGE_EFFECT(듀얼리스트,"상대필드",場所,"表",1,99665)
    CALL FULLRESET
    효과条件薬Ｃ所持 = 1
	CALL STATCHANGE_EFFECT(듀얼리스트,"상대필드",場所,"戦狂",3,99665)

	ELSEIF 종류 == 마함起動
	;カード발동＆설치まで
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99665)
	IF RESULT == 0
	ENDIF

ELSEIF 종류 == ターン終了時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99665,2)
    	IF RESULT == 0
        	CALL CARD_EROTICS(상대プレイヤー,듀얼리스트)
			조교대상 = RESULT
            IF 조교대상 != -1 && TALENT:조교대상:성별 == 1
				PRINTFORMW 장소에 충만한 음란한 성분을 포함한 증기가 암농상에 채워져、
				PRINTFORMW %CALLNAME:조교대상%의 패에 한 장의 카드를 형성한다……
				SELECTCASE RAND:9
					CASE 0
						CALL CARD_GENERATE_HAND(対面者,79840)
					CASE 1
						CALL CARD_GENERATE_HAND(対面者,79841)
					CASE 2
						CALL CARD_GENERATE_HAND(対面者,79842)
					CASE 3
						CALL CARD_GENERATE_HAND(対面者,79843)
					CASE 4
						CALL CARD_GENERATE_HAND(対面者,79844)
					CASE 5
						CALL CARD_GENERATE_HAND(対面者,79845)
					CASE 6
						CALL CARD_GENERATE_HAND(対面者,79846)
					CASE 7
						CALL CARD_GENERATE_HAND(対面者,79847)
					CASE 8
						CALL CARD_GENERATE_HAND(対面者,79848)
				ENDSELECT
            ENDIF

        ENDIF

ENDIF




