@CARDNAME_59904,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 어신체
	CASE "略称"
		RESULTS = 어신체

	CASE "要塞名"
		RESULTS = 어신체
	CASE "カテゴリ"
		RESULTS:0 = 퍼실리즘
		RESULTS:1 = 어신체
		RESULTS:2 = 포트리스
		RESULTS:3 = E.R.A
ENDSELECT


@CARD_59904(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 초차원
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 천사족
	CASE "レベル"
		RETURN 11
	CASE "コスト"
		RETURN 11
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 4000
	CASE "生成カード"
		RETURN 159904
	CASE "성별"
		RETURN 雄性
	CASE "추가日"
		RETURN 250908
ENDSELECT

RETURN 0

@CARDSUMMON_59904(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
;素材条件

RETURN 0

@CARD_EXPLANATION_59904(종류)
#DIM DYNAMIC 종류

PRINTL 『초차원』에 의해 강림。
PRINTL （이 카드는 카드의 효과로만 EX덱에서 필드에 특수 소환 / 마함존에 놓을 수 있다）
CALL TEXT_DECORATION("FORTRESS")
PRINTL 포트리스측　《어신체》 코스트：１１
CALLFORM CARD_ABLE(1,지속효과)
PRINTL ①：이 카드가 존재하는 한、자신은 직접 공격할 수 없으며、
PRINTL "퍼실리즘" 또는 "어신체" 몬스터밖에 EX덱에서 특수 소환할 수 없다。
PRINTL 또한、이 카드는 대상내성을 갖는다。
CALLFORM CARD_ABLE(2,마함起動,フリー誘発)
PRINTL ②：자신 / 상대 턴에 한번、이 카드의 신앙 카운터를 １개 제거하고 발동할 수 있다。
PRINTL 상대 필드 / 묘지의 몬스터 １장을 "퍼실리즘 슬레이브"로서、
PRINTL 지속 마법카드 취급으로 자신의 마법＆함정 존에 앞면 표시로 둔다。
CALLFORM CARD_ABLE(3,발동強制誘発)
PRINTL ③："퍼실리즘" 마법 / 함정 카드의 효과가 발동한 경우에 발동한다。
PRINTL 이 카드에 신앙 카운터를 １개 놓는다（최대 ２０개까지）。
;퍼실리즘の효과や自身の④で20個以上置けるのは仕様。
CALL CARD_ABLE(4,ターン終了時)
PRINTL ④：엔드 페이즈에 발동할 수 있다。자신의 마법＆함정 존에 존재하고 있는 몬스터는 봉사를 행해、그 횟수만큼 신앙 카운터를 놓는다。
PRINTL 이것이 자신의 엔드 페이즈 중이라면、추가로 이 카드의 신앙 카운터의 수×１００ 데미지를 준다。
PRINTL 그 후、자신 필드에 "퍼실리즘 슬레이브"가 ４장 이상 존재하고있는 경우、이 카드를 몬스터로서 특수 소환할 수 있다。
CALL TEXT_DECORATION("FORTRESS")


@CARDCAN_59904(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,59904)
SIF RESULT == 1
	RETURN 0

IF 종류 == 지속효과
	;마법面①の효과（EX덱에서の소환制限・直接攻撃制限）
	;モンスター面①の효과（대상耐性）
	SIF !GROUPMATCH(ゾーン, "마함")
		RETURN 0
	RETURN 1
ELSEIF 종류 == 마함起動 || 종류 == フリー誘発
	;마법面②の효과
	SIF ゾーン != "마함"
		RETURN 0
	SIF 발동済_2(듀얼리스트,59904)
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_個Ｃ:場所") < 1
		RETURN 0
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과範囲상대묘지 = 1
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	SIF 마함数(듀얼리스트) >= 5
		RETURN 0
	RETURN 1
ELSEIF 종류 == 발동強制誘発
	;마법面③の효과（카운터を乗せる）
	SIF ゾーン != "마함"
		RETURN 0
	;카운터の上限は２０個
	CALL FULLRESET
		SIF GETVAR(@"%듀얼리스트%_魔_個Ｃ:場所") >= 20
		RETURN 0
	효과条件カテゴリ = 퍼실리즘
	효과条件固カテゴリ = 어신체
	효과条件종류 = 마함全般
	CALL CARD_CHECK_START_DATA_2(듀얼리스트,2)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == ターン終了時
	;마법面④の효과
	SIF ゾーン != "마함"
		RETURN 0
	;セクハラ효과があるので카운터が無くても발동可能な仕様
	;空打ちできるようになっちゃうけどまあいいでしょう
	RETURN 1
ENDIF

@CARDEFFECT_59904(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 설정場所
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC ダメージ量
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID
#DIM DYNAMIC カード추가先
#DIM DYNAMIC 조교대상
#DIMS DYNAMIC 元カード名


IF 종류 == 지속효과
	;포트리스状態での지속효과

	CALL SET_TMP_EX_RST_CAT(듀얼리스트,"퍼실리즘")
;	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"直無")
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"直無")
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"対耐")

ELSEIF 종류 == 마함起動 || 종류 == フリー誘発
	;마법面の磔효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,59904,2)
		IF RESULT == 0
			;텍스트
			PRINTFORMW %CALLNAME:MASTER%가 목을 휙하고 꺾은 순간、공기가 변한다。
			PRINTFORMW 신호를 받은 퍼실리즘들이 바로 손을 뻗어、천을 스르륵하고 벗겨내니、"어신체"가 모습을 나타냈다。
			PRINTFORMW 신앙에 힘이 가득찬 "어신체"는、시야에 들어온 자를 굴복시킨다……。
			;カードを置く位置の情報を취득
			カード추가先 = 現마함(듀얼리스트)
			;상대のカードを置く
			CALL FULLRESET
			효과範囲상대필드 = 1
			효과範囲상대묘지 = 1
			효과条件표시 = 10
			효과条件종류 = モン全般
			효과条件除去種 = 強奪
			CALL CARD_SET_ADV(듀얼리스트,ゾーン,場所,"表側표시")
			;名前を「Pha슬레이브」に변경（処理上はこの表記）ついでに元々の名前も취득
			選択カード_ID = GETVAR(@"%듀얼리스트%_마함:カード추가先")
			TRYCALLFORM CARDNAME_{選択カード_ID},"名前"
			元カード名 = %RESULTS%
			CALL FULLRESET
			CALL CARD_SET_RENAME(듀얼리스트,"마함",カード추가先,"Pha슬레이브")
			;텍스트
			CALL FULLRESET
			CALL CARD_EROTICS_FIELD(듀얼리스트,"마함",カード추가先)
			조교대상 = RESULT
			PRINTFORML 조교대상:{조교대상}/RESULT:{RESULT}
			IF RESULT != -1
				CALL KOJO_SYMMETRIA_59904(듀얼리스트,元カード名,조교대상,1)
			ELSE
				PRINTFORMW %CALLNAME:MASTER%는 압도적인 "수컷력"으로 상대를 굴복시킨다！
			ENDIF
			;카운터を取り除く
			CALL FULLRESET
			CALL SET_FIELD_INFO(듀얼리스트,"마함",場所,"個Ｃ",-1)
		ENDIF
ELSEIF 종류 == 발동強制誘発
	;자신に카운터を乗せる
	PRINTFORMW "어신체"에 카운터가 놓였다。
	CALL SET_FIELD_INFO(듀얼리스트,"마함",場所,"個Ｃ",1)
ELSEIF 종류 == ターン終了時
	;마법面のEP효과
	;セクハラを行う
	CALL CARD_HOSTAGE_FORT_59904(듀얼리스트,종류,ゾーン,場所)
	
	;자신のターンである事をチェック
	IF ターンプレイヤー == 듀얼리스트
		;카운터の数だけダメージを与える
		ダメージ量 = GETVAR(@"%듀얼리스트%_魔_個Ｃ:場所") * 100
		CALL CARD_DAMAGE_59904(듀얼리스트,종류,ゾーン,場所,ダメージ量)
		;「퍼실리즘 슬레이브」が4枚あるかチェック
		CALL FULLRESET
		효과範囲필드 = 1
		효과範囲마함 = 1
		효과条件名前 = Pha슬레이브
		효과条件종류 = モン全般
		효과条件CNT = 1
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		IF RESULT >= 4
			;条件を満たしているので変形
			CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,59904)
			IF RESULT == 0
				PRINTFORMW 신자들이 어신체를 둘러싸、음란한 소리를 내며 혀과 손으로 쓰다듬는다。
				PRINTFORML 움찔…❤움찔❤하며 맥동이 강해지며、봉 전체가 열을 띠니、"어신체"에게서 빛이 점점 나기 시작했다。
				SETCOLOR C_PINK 
				PRINTFORML ❤쾌락과 신앙이 임계를 넘어、어신체는 한층 늠름하게 확장하여 "어신체"는 더욱 강한 신성을 얻었다❤
				RESETCOLOR
				PRINTFORML 
				PRINTFORML %CALLNAME:MASTER%의 늠름한 "어신체"는、다음 봉납을 요구하는 듯이、「움찔❤움찔❤」하며 맥동하고있다。
				CALL CARD_RECONSTRUCT(듀얼리스트,ゾーン,場所,159904)
		
				CALL FULLRESET
				CALL CARD_SP_SUMMON(듀얼리스트,ゾーン,場所)
			ENDIF
		ENDIF
	ENDIF
ENDIF





@CARDNAME_159904,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 신명현현전간천
	CASE "略称"
		RESULTS = 전간천

	CASE "要塞名"
		RESULTS = 어신체
	CASE "カテゴリ"
		RESULTS:0 = 퍼실리즘
		RESULTS:1 = 어신체
		RESULTS:2 = 포트리스
		RESULTS:3 = E.R.A
ENDSELECT

@CARD_159904(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 초차원
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 천사족
	CASE "レベル"
		RETURN 11
	CASE "コスト"
		RETURN 11
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 4000
	CASE "성별"
		RETURN 雄性
	CASE "추가日"
		RETURN 250721
ENDSELECT

@CARD_EXPLANATION_159904(종류)
#DIM DYNAMIC 종류

CALL TEXT_DECORATION("D_HEART_C")
CALLFORM CARD_ABLE(0,身代효과)
PRINTL ①：이 카드가 필드를 벗어날 경우、대신에 마법＆함정 존에 놓는다。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ②：이 카드가 존재하는 한、이하의 효과를 적용한다。
PRINTL ●이 카드는 대상 내성을 갖는다。
PRINTL ●"퍼실리즘" 카드에 의해 카드 효과를 고를 경우、그 효과는 어느쪽도 적용되어、요구하는 행위는 과격해진다。
;②の효과は퍼실리즘モンスターに추가条件として書く
CALLFORM CARD_ABLE(0,필드起動,フリー誘発)
PRINTL ③：자신 / 상대턴에、상대 필드 / 묘지의 몬스터 １장을 대상으로 하고 발동할 수 있다。
PRINTL 그 몬스터를 "퍼실리즘 슬레이브"로서、지속 마법 카드 취급으로 자신의 마법＆함정 존에 앞면 표시로 둔다。
CALL CARD_ABLE(10,ターン終了時)
PRINTL ④：엔드 페이즈에 발동할 수 있다。자신의 마법＆함정존에 존재하는 몬스터에게 축복을 부여한다。
PRINTL 그 후、２０００ 데미지를 준다。
CALL TEXT_DECORATION("D_HEART_2")



@CARDCAN_159904(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,159904)
SIF RESULT == 1
	RETURN 0

IF 종류 == 지속효과
	;마법面①の효과（EX덱からの소환制限・直接攻撃制限）
	;モンスター面①の효과（대상耐性）
	SIF !GROUPMATCH(ゾーン,"필드")
		RETURN 0
	RETURN 1

ELSEIF 종류 == ターン終了時
	;モンスター面④の효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == 필드起動 || 종류 == フリー誘発
	;モンスター面③の효과
	SIF ゾーン != "필드"
		RETURN 0
	SIF 발동済_3(듀얼리스트,1159904)
		RETURN 0
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과範囲상대묘지 = 1
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	SIF 마함数(듀얼리스트) >= 5
		RETURN 0
	RETURN 1
ELSEIF 종류 == 身代효과
	;龍回避
	SIF ゾーン != "필드"
		RETURN 0
	SIF 破壊대상ゾーン != "필드"
		RETURN 0
	SIF 破壊대상場所 != 場所
		RETURN 0
	SIF GROUPMATCH(破壊종류,"마함")
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_159904(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 설정場所
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC ダメージ量
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID
#DIM DYNAMIC カード추가先
#DIM DYNAMIC 조교대상
#DIMS DYNAMIC 元カード名


IF 종류 == 지속효과
	;モンスター状態での지속효과
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"対耐")
	CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"変化元ID",59904,上書きモード)
ELSEIF 종류 == ターン終了時
	;モンスター面のEP효과
	;セクハラを行う
	CALL CARD_HOSTAGE_MONSTER_59904(듀얼리스트,종류,ゾーン,場所)
	ダメージ量 = 2000
	CALL CARD_DAMAGE_59904(듀얼리스트,종류,ゾーン,場所,ダメージ量)
ELSEIF 종류 == 필드起動 || 종류 == フリー誘発
	;モンスター面の磔효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,159904,3)
		IF RESULT == 0
			;텍스트
			PRINTFORMW 늠늠하게 서있는"어신체"가、필드를 완전히 지배하고있다。
			;カードを置く位置の情報を취득
			カード추가先 = 現마함(듀얼리스트)
			;상대のカードを置く
			CALL FULLRESET
			효과範囲상대필드 = 1
			효과範囲상대묘지 = 1
			효과条件표시 = 10
			효과条件종류 = モン全般
			효과条件除去種 = 強奪
			CALL CARD_SET_ADV(듀얼리스트,ゾーン,場所,"表側표시")
			;名前を「Phaスレイヴ」に변경（処理上はこの表記）ついでに元々の名前も취득
			選択カード_ID = GETVAR(@"%듀얼리스트%_마함:カード추가先")
			TRYCALLFORM CARDNAME_{選択カード_ID},"名前"
			元カード名 = %RESULTS%
			CALL FULLRESET
			CALL CARD_SET_RENAME(듀얼리스트,"마함",カード추가先,"Pha슬레이브")
			;텍스트표시の분기
			CALL FULLRESET
			CALL CARD_EROTICS_FIELD(듀얼리스트,"마함",カード추가先)
			조교대상 = RESULT
			IF RESULT != -1
				CALL KOJO_SYMMETRIA_59904(듀얼리스트,元カード名,조교대상,1)
			ELSE
				PRINTFORMW %CALLNAME:MASTER%는 압도적인 "수컷력"으로 상대를 굴복시켰다！
			ENDIF
		ENDIF
ELSEIF 종류 == 身代효과
	;モンスター面の龍回避
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,159904)
	IF RESULT == 0
		CALL CARD_RECONSTRUCT(듀얼리스트,ゾーン,場所,59904)
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所,마함行き)
		RETURN 1
	ENDIF
	RETURN 0
ENDIF


