@CARDNAME_1098,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 엘리멘틀 히어로 버스트 레이디（UR）
	CASE "略称"
		RESULTS = 버스트 레이디Ｒ
	CASE "カテゴリ"
		RESULTS:0 = HERO
		RESULTS:1 = E•HERO
ENDSELECT


@CARD_1098(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 通常モン
	CASE "속성"
		RETURN 화염속성
	CASE "종족"
		RETURN 전사족
	CASE "レベル"
		RETURN 3
	CASE "攻撃力"
		RETURN 1200
	CASE "守備力"
		RETURN 800
	CASE "성별"
		RETURN 牝性
	CASE "分割"
		RETURN デュアル
	CASE "別途ID"
		RETURN 38002
	CASE "レア度"
		RETURN SPR
ENDSELECT
RETURN 0

@CARD_EXPLANATION_1098(종류)
#DIM DYNAMIC 종류
PRINTL ①：이 카드가 앞면 표시로 존재하는 한 자신 메인 페이즈시、１번만 "각성"할 수 있다。
PRINTL "각성"은 １턴에 １번밖에 할 수 없다。
PRINTL 이 경우、필드 위의 오너가 자신인 상대 필드의 몬스터를 전부 소유자의 패로 되돌린다。



@CARDCAN_1098(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,1098)
SIF RESULT == 1
	RETURN 0
SIF LOCAL == 0
	RETURN 0

IF 종류 == 필드起動 && GETVAR(@"%듀얼리스트%_MZ_条達:場所") == 0
	SIF 발동済_1(듀얼리스트,覚醒발동)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1

ENDIF


@CARDEFFECT_1098(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC カウンタ数
#DIM DYNAMIC 攻撃力修正

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 필드起動 && GETVAR(@"%듀얼리스트%_MZ_条達:場所") == 0
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,覚醒발동,1)
	IF RESULT == 0
		CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"条達",2,1)
		PRINTFORML "버스트 리턴！"

		CALL FULLRESET
		효과条件他所 = 1
		효과条件표시 = 表側표시
		효과条件所有 = 1
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드全体",場所,"손패")
	ENDIF
ENDIF
