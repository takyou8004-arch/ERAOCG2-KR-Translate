@CARDNAME_58207,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = CNo.107 네오 갤럭시아이즈 타키온 드래곤
	CASE "略称"
		RESULTS = C#H7 超銀河眼
	CASE "カテゴリ"
		RESULTS:0 = 넘버즈
		RESULTS:1 = 오버 헌드레드
		RESULTS:2 = 카오스
		RESULTS:3 = 갤럭시
		RESULTS:4 = 타키온
ENDSELECT


@CARD_58207(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 드래곤족
	CASE "レベル"
		RETURN 9
	CASE "攻撃力"
		RETURN 4500
	CASE "守備力"
		RETURN 3000
	CASE "추가日"
		RETURN 250809
ENDSELECT
RETURN 0

@CARDSUMMON_58207(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要素材数 = 3
素材条件レベル = 9

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,58207) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_58207(종류)
#DIM DYNAMIC 종류

PRINTL 레벨９ 몬스터×３
CALLFORM CARD_ABLE(10,필드起動)
PRINTL ①：１턴에 １번、이 카드의 X소재를 １개 제거하고 발동할 수 있다。
PRINTL 이 턴 상대는 필드에서 발동하는 효과를 발동할 수 없으며、
PRINTL 이 카드 이외의 필드의 모든 앞면 표시 카드의 효과는 턴 종료시까지 무효화된다。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ②：이 카드가 "No.107 네오 갤럭시아이즈 타키온 드래곤"을 X소재로 하고있는 경우、이하의 효과를 얻는다。
PRINTL 이 카드는 １번의 배틀 페이즈 중에 ３번까지 공격할 수 있다。

@CARDCAN_58207(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,58207)
SIF RESULT == 1
	RETURN 0
IF 종류 == 필드起動
	SIF 효과발동済(듀얼리스트,場所,ゾーン)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_条達:場所") == 0
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_58207(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM 選択カード


CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58207,10,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		효과条件他所 = 1
		CALL STATCHANGE_EFFECT(듀얼리스트,"필드",場所,"無効",1)
		CALL FULLRESET
		CALL STATCHANGE_EFFECT(듀얼리스트,"마함",場所,"無効",1)

		CALL FULLRESET
		CALL STATCHANGE_EFFECT(듀얼리스트,"상대필드",場所,"無効",1,58207)

		CALL FULLRESET
		CALL STATCHANGE_EFFECT(듀얼리스트,"상대마함",場所,"無効",1,58207)

		CALL CARD_RESERV(듀얼리스트,58207,短紋章予約)
	ENDIF
ELSEIF 종류 == 短紋章起動
	CALL FULLRESET
	CALL STATCHANGE_EFFECT(듀얼리스트,"상대필드",場所,"発無",3)
ELSEIF 종류 == 지속효과
	CALL FULLRESET
	CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"戦",3,1)
ENDIF