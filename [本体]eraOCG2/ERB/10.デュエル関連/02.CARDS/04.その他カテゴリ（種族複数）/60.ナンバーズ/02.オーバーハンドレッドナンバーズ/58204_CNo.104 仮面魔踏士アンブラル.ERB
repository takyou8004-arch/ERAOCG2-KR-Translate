@CARDNAME_58204,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = CNo.104 마스커레이드 매지션 엄브럴
	CASE "略称"
		RESULTS = C#H4 엄브럴
	CASE "カテゴリ"
		RESULTS:0 = 카오스
		RESULTS:1 = 넘버즈
		RESULTS:2 = 오버 헌드레드
ENDSELECT


@CARD_58204(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 천사족 
	CASE "レベル"
		RETURN 5
	CASE "攻撃力"
		RETURN 3000
	CASE "守備力"
		RETURN 1500
	CASE "추가日"
		RETURN 250809
ENDSELECT

RETURN 0
@CARDSUMMON_58204(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL RESETCONDITION_MAT
必要素材数 = 4
素材条件レベル = 5
지정素材名前 = No.104 마스커레이드 매지션 샤이닝

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,58204) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_58204(종류)
#DIM DYNAMIC 종류

PRINTL レベル５モンスター×４
CALLFORM CARD_ABLE(0,특수소환時,EX소환時)
PRINTL ①：このカードが특수소환に成功した時、フィールドのカード１枚を대상として발동できる。
PRINTL そのカードを破壊する。

CALLFORM CARD_ABLE(10,발동誘発)
PRINTL ②：このカードが「No.104 마스커레이드 매지션 샤이닝」をX素材としている場合、以下の효과を得る。
PRINTL ●１ターンに１度、상대フィールドのモンスターの효과が발동した時、
PRINTL このカードのX素材を１つ取り除いて발동できる。
PRINTL その발동を無効にする。
PRINTL その後、상대の손패をランダムに１枚選んで묘지へ送り、상대のLPを半分にできる。


@CARDCAN_58204(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,58204)
SIF RESULT == 1
	RETURN 0

IF 종류 == 특수소환時 || 종류 == EX소환時
	RETURN 1
ELSEIF 종류 == 발동誘発
	SIF 효과발동済(듀얼리스트,場所,ゾーン)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_58204(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 最大選択数
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 특수소환時 || 종류 == EX소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58204)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲상대필드 = 1
		효과範囲상대마함 = 1
		CALL CARD_REMOVAL_ADV(듀얼리스트,"単体除去",場所,"묘지")
	ENDIF
ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58204,10,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"발동無効",場所,"無効")
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"손패破壊",場所,"묘지")
		CALL LIFE_CHANGE(듀얼리스트,"半分","변경",0,1)
	ENDIF
ENDIF

