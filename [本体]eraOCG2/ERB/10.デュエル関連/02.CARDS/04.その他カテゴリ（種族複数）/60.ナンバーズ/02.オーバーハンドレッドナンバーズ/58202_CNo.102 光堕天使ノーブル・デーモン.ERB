@CARDNAME_58202,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = CNo.102 다크 라이트닝 노블 데몬
	CASE "略称"
		RESULTS = C#H2 노블 D
	CASE "カテゴリ"
		RESULTS:0 = 홀리 라이트닝
		RESULTS:1 = 카오스
		RESULTS:2 = 넘버즈
		RESULTS:3 = 오버 헌드레드
ENDSELECT


@CARD_58202(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 천사족 
	CASE "レベル"
		RETURN 5
	CASE "攻撃力"
		RETURN 2900
	CASE "守備力"
		RETURN 2400
	CASE "추가日"
		RETURN 250809
ENDSELECT

RETURN 0
@CARDSUMMON_58202(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL RESETCONDITION_MAT
必要素材数 = 4
素材条件レベル = 5
지정素材名前 = No.102 홀리 라이트닝 글로리어스 헤일로

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,58202) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_58202(종류)
#DIM DYNAMIC 종류

PRINTL 레벨５ 몬스터×４
CALLFORM CARD_ABLE(10,필드起動,フリー誘発)
PRINTL ①：서로의 턴에 １번、이 카드의 X소재를 １개 제거하고、
PRINTL 상대 필드의 앞면 표시 몬스터 １장을 대상으로 하고 발동할 수 있다。
PRINTL 그 몬스터의 효과를 무효로 하고、공격력을 ０으로 한다。
CALLFORM CARD_ABLE(0,身代효과)
PRINTL ②：자신 필드의 카드가 전투 / 효과로 파괴된 경우、
PRINTL 대신에 이 카드의 X소재를 １개 제거할 수 있다。
PRINTL 이 카드가 "No.102 홀리 라이트닝 글로리어스 헤일로"를 Ｘ소재로 하고있는 경우、
PRINTL 그 후、상대 라이프 포인트에 １５００데미지를 준다。

@CARDCAN_58202(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,58202)
SIF RESULT == 1
	RETURN 0

IF 종류 == 필드起動 || 종류 == フリー誘発
	SIF ゾーン != "필드"
		RETURN 0
	SIF 효과발동済(듀얼리스트,場所,ゾーン)
		RETURN 0
	CALL FULLRESET
	효과範囲상대필드 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 身代효과
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	SIF !GROUPMATCH(破壊종류,"효과","戦闘")
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_58202(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 最大選択数
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 필드起動 || 종류 == フリー誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58202,10,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		효과条件표시 = 表側표시
		효과추가종류 = 21
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드単体",場所,"지속無効")
	ENDIF
ELSEIF 종류 == 身代효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58202,0,X消費1)
	IF RESULT == 0
		IF GETVAR(@"%듀얼리스트%_MZ_条達:場所") >= 1
			CALL LIFE_CHANGE(듀얼리스트,"減少","효과",1500,1)
		ENDIF
		RETURN 1
	ENDIF
	RETURN 0
ENDIF
