@CARDNAME_58205,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = CNo.105 BK 혜성의 카에스토스
	CASE "略称"
		RESULTS = C#105 카에스토스
	CASE "カテゴリ"
		RESULTS:0 = BK
		RESULTS:1 = 카오스
		RESULTS:2 = 넘버즈
		RESULTS:3 = 오버 헌드레드
ENDSELECT


@CARD_58205(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 화염속성
	CASE "종족"
		RETURN 전사족 
	CASE "レベル"
		RETURN 5
	CASE "攻撃力"
		RETURN 2800
	CASE "守備力"
		RETURN 2000
	CASE "추가日"
		RETURN 250707
ENDSELECT

RETURN 0
@CARDSUMMON_58205(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL RESETCONDITION_MAT
必要素材数 = 4
素材条件レベル = 5
지정素材名前 = No.105 BK 유성의 세스터스

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,58205) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_58205(종류)
#DIM DYNAMIC 종류

PRINTL 레벨５ 몬스터×４
PRINTL 이 카드명의 ①의 효과는 １턴에 １번밖에 사용할 수 없다。
CALL CARD_ABLE(1,EX소환時)
PRINTL ①：이 카드가 Ｘ소환에 성공한 경우에 발동할 수 있다。
PRINTL 이 카드의 Ｘ소재의 수까지、자신의 묘지에서 일반 함정 카드를 골라서 자신 필드에 세트한다。
PRINTL 이 효과로 「카운터」함정카드를 세트한 경우、그 카드는 세트한 턴에 발동할 수 있다。
CALL CARD_ABLE(2,攻撃後誘発)
PRINTL ②：이 카드가 전투로 몬스터를 파괴한 경우에 발동할 수 있다。
PRINTL 상대 필드의 공격 표시 몬스터를 전부 파괴하고、파괴한 몬스터의 공격력의 합계의 절반의 데미지를 상대에게 준다。
CALL CARD_ABLE(3,발동誘発)
PRINTL ③："Ｎｏ.１０５ ＢＫ 유성의 세스터스" 를Ｘ소재로 하고있는 이 카드가 필드에 존재하고、배틀 페이즈 중에 카드의 효과가 발동했을 때、이 카드의 Ｘ소재를 １개 제거하고 발동할 수 있다。
PRINTL 상대 필드의 카드 １장을 파괴하고、상대에게 ５００데미지를 준다。

@CARDCAN_58205(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,58205)
SIF RESULT == 1
	RETURN 0

IF 종류 == EX소환時
	SIF 발동済_1(듀얼리스트,58205)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	SIF 마함数(듀얼리스트) >= 5
		RETURN 0
	CALL FULLRESET
	효과範囲묘지 = 1
	효과条件종류 = 通常함정
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 攻撃後誘発
	SIF ゾーン != "필드"
		RETURN 0
	SIF 戦闘場所(듀얼리스트) != 場所
		RETURN 0
	SIF 直前戦闘破壊ID(듀얼리스트,1) == 0
		RETURN 0
	SIF モンスター数(対面者) == 0
		RETURN 0
	RETURN 1

ELSEIF 종류 == 발동誘発
	SIF ゾーン != "필드"
		RETURN 0
	SIF バトルフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_条達:場所") == 0
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	SIF モンスター数(対面者) == 0 && 마함数(対面者) == 0
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_START_DATA(듀얼리스트)
	SIF RESULT == 1
		RETURN 1
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT == 1
		RETURN 1
	RETURN 0
ENDIF


@CARDEFFECT_58205(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 最大選択数
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == EX소환時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58205,1)
	IF RESULT == 0
		最大選択数 = GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") + 1
		SIF 最大選択数 > 5 - 마함数(듀얼리스트) 
			最大選択数 = 5 - 마함数(듀얼리스트) 
		CALL FULLRESET
		효과範囲묘지 = 1
		효과条件종류 = 通常함정
		효과条件CNT = 1
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		SIF 最大選択数 > RESULT
			最大選択数 = RESULT
		FOR カウンタ,0,最大選択数
			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件종류 = 通常함정
			CALL CARD_CHOOSE_ADV(듀얼리스트,ゾーン,場所)
			選択カード = RESULT
			選択カード_ID = RESULT:99
			IF 選択カード_ID != 9999
				CALL CARD_SELECT(듀얼리스트,選択カード)
				選択カード = RESULT
				TRYCALLFORM CARDNAME_{選択カード_ID},"カテゴリ"
				IF MATCH(RESULTS,"카운터")
					CALL FULLRESET
					PRINTL 
					SETCOLOR C_YELLOW
					PRINTL 카에스토스는 반격의 자세를 취했다！
					RESETCOLOR
					PRINTL 
					CALL CARD_ADD(듀얼리스트,"묘지","마함", 選択カード, "使化裏側표시")
				ELSE
					CALL FULLRESET
					CALL CARD_ADD(듀얼리스트,"묘지","마함", 選択カード, "裏側표시")
				ENDIF
			ENDIF
			CALL FULLRESET
			IF 듀얼리스트 == "자신" && 最大選択数 - カウンタ > 1
				PRINTL さらに카드를 セットしますか？
				CALL YN_CHOICE(듀얼리스트)
				SIF RESULT == いいえ
					BREAK
			ENDIF
		NEXT
	ENDIF
ELSEIF 종류 == 攻撃後誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58205,2)
	IF RESULT == 0
		효과추가종류 = 2
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"필드全体",場所,"묘지")
	ENDIF
ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58205,3,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		効果範囲相手戦場 = 1
効果範囲相手魔罠 = 1
CALL CARD_REMOVAL_ADV(決闘者,"単体除去",場所,"墓地")
		CALL LIFE_CHANGE(듀얼리스트,"減少","효과",500,1)
	ENDIF
ENDIF