@CARDNAME_58104,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = No.104 마스커레이드 매지션 샤이닝
	CASE "略称"
		RESULTS = #H4 가면마도사
	CASE "カテゴリ"
		RESULTS:0 = 넘버즈
		RESULTS:1 = 오버 헌드레드
ENDSELECT


@CARD_58104(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 마법사족
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 2700
	CASE "守備力"
		RETURN 1200
	CASE "추가日"
		RETURN 250809
ENDSELECT
RETURN 0

@CARDSUMMON_58104(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要素材数 = 3
素材条件レベル = 4

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,58104) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_58104(종류)
#DIM DYNAMIC 종류

PRINTL 레벨４ 몬스터×３
CALLFORM CARD_ABLE(10,필드起動)
PRINTL ①：１턴에 １번、자신 메인 페이즈에 발동할 수 있다。
PRINTL 상대의 덱 위에서 카드를 ３장 묘지로 보낸다。
CALLFORM CARD_ABLE(0,발동誘発)
PRINTL ②：자신 / 상대의 배틀 페이즈에 상대가 몬스터의 효과를 발동했을 때、이 카드의 X소재를 １개 제거하고 발동할 수 있다。
PRINTL 그 발동을 무효로 하고、상대에게 ８００데미지를 준다。

@CARDCAN_58104(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,58104)
SIF RESULT == 1
	RETURN 0
IF 종류 == 필드起動
	SIF 효과발동済(듀얼리스트,場所,ゾーン)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1
ELSEIF 종류 == 발동誘発
	SIF バトルフェイズ != 1
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	CALL FULLRESET
	효과条件종류 = モン全般
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_58104(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM 選択カード


CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 필드起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58104,10)
	IF RESULT == 0
		CALL CARD_DECK_DISPOSE(듀얼리스트,"덱",3,0,1)
	ENDIF
ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58104,0,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"발동無効",場所,"無効")
		CALL LIFE_CHANGE(듀얼리스트,"減少","효과",800,1)
	ENDIF
ENDIF
