@CARDNAME_58106,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = No.106 거암장 자이언트 핸드
	CASE "略称"
		RESULTS = #H6 G 핸드
	CASE "カテゴリ"
		RESULTS:0 = 넘버즈
		RESULTS:1 = 오버 헌드레드

ENDSELECT


@CARD_58106(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN エクシーズ
	CASE "属性"
		RETURN 地属性
	CASE "種族"
		RETURN 元素霊族
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 2000
	CASE "守備力"
		RETURN 2000
	CASE "追加日"
		RETURN 250809
ENDSELECT
RETURN 0

@CARDSUMMON_58106(決闘者,召喚カード,EX参照)
#DIMS DYNAMIC 決闘者
#DIM DYNAMIC 召喚カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要素材数 = 2
素材条件レベル = 4

IF EX参照 == 1
	CALL XYZCAN(決闘者,召喚カード,58106) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_58106(種類)
#DIM DYNAMIC 種類

PRINTL レベル４モンスター×２
CALLFORM CARD_ABLE(20,発動誘発)
PRINTL ①：相手フィールドのモンスターの効果が発動した時このカードのX素材を１つ取り除き、
PRINTL 相手フィールドの効果モンスター１体を対象として１度だけ発動できる。
PRINTL 対象の効果モンスターの効果は無効化し、表示形式の変更もできない。
CALLFORM CARD_ABLE(0,身代効果)
PRINTL ②：フィールドのこのカードが戦闘・効果で破壊される場合、
PRINTL 代わりにこのカードのX素材を１つ取り除く事ができる。

@CARDCAN_58106(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード
CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,58106)
SIF RESULT == 1
	RETURN 0
IF 種類 == 戦場起動 || 種類 == フリー誘発
	SIF ゾーン != "戦場"
		RETURN 0
	SIF 効果発動済(決闘者,場所,ゾーン)
		RETURN 0
	SIF GETVAR(@"%決闘者%_MZ_Ｘ:場所") <= -1
		RETURN 0
	RETURN 1
ELSEIF 種類 == 身代効果
	SIF ゾーン != "戦場"
		RETURN 0
	SIF GETVAR(@"%決闘者%_MZ_Ｘ:場所") <= -1
		RETURN 0
	SIF 破壊対象ゾーン != "戦場"
		RETURN 0
	SIF 破壊対象場所 != 場所
		RETURN 0
	SIF !GROUPMATCH(破壊種類,"効果","戦闘")
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_58106(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM 選択カード


CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 発動誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,58106,20,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		効果条件表示 = 表側表示
		効果追加種類 = 30
		CALL CARD_REMOVAL_BASIC(決闘者,"戦場単体",場所,"永続無効")
	ENDIF
ELSEIF 種類 == 身代効果
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,58106,0,X消費1)
	IF RESULT == 0
		RETURN 1
	ENDIF
	RETURN 0
ENDIF