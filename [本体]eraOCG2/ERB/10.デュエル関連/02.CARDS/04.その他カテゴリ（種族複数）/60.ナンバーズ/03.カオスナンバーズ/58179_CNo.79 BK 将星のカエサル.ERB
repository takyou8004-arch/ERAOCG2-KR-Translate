@CARDNAME_58179,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = CNo.79 BK 장성의 카이사르
	CASE "略称"
		RESULTS = C#79 카이사르
	CASE "カテゴリ"
		RESULTS:0 = BK
		RESULTS:1 = 카오스
		RESULTS:2 = 넘버즈
ENDSELECT

@CARD_58179(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 화염속성
	CASE "종족"
		RETURN 전사족 
	CASE "レベル"
		RETURN 5
	CASE "攻撃力"
		RETURN 2600
	CASE "守備力"
		RETURN 2000
	CASE "추가日"
		RETURN 250707
ENDSELECT

RETURN 0
@CARDSUMMON_58179(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL RESETCONDITION_MAT
必要素材数 = 3
素材条件レベル = 5
지정素材名前 = No.79 BK 항성의 카이저

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,58179) 
	RETURN RESULT
ENDIF



@CARD_EXPLANATION_58179(종류)
#DIM DYNAMIC 종류

PRINTL 레벨５ 몬스터×３
PRINTL 이 카드명의 ②의 효과는 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(0,攻守変動)
PRINTL ①：이 카드의 공격력은、이 카드의 Ｘ소재의 수×２００올린다。
CALLFORM CARD_ABLE(1,소환誘発)
PRINTL ②：상대가 몬스터를 특수 소환할 때에 발동할 수 있다。
PRINTL 이 카드의 Ｘ소재를 ２개 제거하고、그것을 무효로 하고 파괴한다。
CALLFORM CARD_ABLE(10,攻撃時誘発)
PRINTL ③：이 카드가 "Ｎｏ.７９ ＢＫ 항성의 카이저"를 소재로 하고 Ｘ소환한 이 카드는 이하의 효과를 얻는다。
PRINTL ●１턴에 １번、자신의 "ＢＫ"몬스터가 상대 몬스터와 전투를 하는 공격 선언시에 
PRINTL 패 / 덱에서 "ＢＫ"몬스터 １장을 묘지로 보내고 발동할 수 있다。
PRINTL 그 상대 몬스터를 제외하고、이 카드의 Ｘ소재를 １개 늘린다。

@CARDCAN_58179(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,58179)
SIF RESULT == 1
	RETURN 0

IF 종류 == 소환誘発
	SIF 발동済_1(듀얼리스트,58179)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= 0
		RETURN 0
	SIF 最終소환決者 == 듀얼리스트
		RETURN 0
	RETURN 1

ELSEIF 종류 == 攻撃時誘発
	SIF 효과발동済(듀얼리스트,場所,ゾーン)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF バトルフェイズ != 1
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_条達:場所") == 0
		RETURN 0
	SIF 戦闘場所(듀얼리스트) == 5
		RETURN 0
	SIF 戦闘場所(듀얼리스트,1) == 5
		RETURN 0
	TRYCALLFORM CARDNAME_{戦闘カードID(듀얼리스트)},"カテゴリ"
	SIF RESULTS != "BK"
		RETURN 0
	CALL FULLRESET
	효과範囲손패 = 1
	효과範囲덱 = 1
	효과条件カテゴリ = BK
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1

ELSEIF 종류 == 攻守変動
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") < 0
		RETURN 0
	RETURN 1
ENDIF


@CARDEFFECT_58179(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 제외カード
#DIM DYNAMIC 효과先

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 소환誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58179,1,X消費2)
	IF RESULT == 0
		효과先 = 最終소환場所
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"自動処理",효과先,"지속無効")
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"自動処理",효과先,"묘지")
	ENDIF

ELSEIF 종류 == 攻撃時誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,58179,10)
	IF RESULT == 0
		제외カード = 戦闘場所(듀얼리스트,1)

		CALL FULLRESET
		효과範囲손패 = 1
		효과範囲덱 = 1
		효과条件カテゴリ = BK
		효과条件종류 = モン全般
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"自動処理",제외カード,"제외")
			CALL FULLRESET
			CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"Ｘ",1)
			PRINTL 
			SETCOLOR C_YELLOW
			PRINTL 카이사르의 오버레이 유닛이 추가되었다！
			RESETCOLOR
			PRINTL 
		ENDIF
	ENDIF

ELSEIF 종류 == 攻守変動
	攻撃力修正 = (GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") + 1) * 200
	CALL STATCHANGE_BASIC(듀얼리스트,"自身",場所,攻撃力修正)
ENDIF