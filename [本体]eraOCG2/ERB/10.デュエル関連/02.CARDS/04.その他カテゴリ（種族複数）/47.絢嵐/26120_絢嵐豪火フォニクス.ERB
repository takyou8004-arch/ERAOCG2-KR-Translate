@CARDNAME_26120,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 현람호화 포닉스
	CASE "略称"
		RESULTS = 포닉스
	CASE "カテゴリ"
		RESULTS:0 = 현람
ENDSELECT


@CARD_26120(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 바람속성
	CASE "종족"
		RETURN 원소정령족
	CASE "レベル"
		RETURN 9
	CASE "攻撃力"
		RETURN 3000
	CASE "守備力"
		RETURN 0
	CASE "추가日"
		RETURN 250726
ENDSELECT
RETURN 0

@CARD_EXPLANATION_26120(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①②③의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,효과後誘発)
PRINTL ①：速攻마법 카드が발동한 경우에 발동할 수 있다。
PRINTL 이 카드를 패에서 특수 소환한다。
CALLFORM CARD_ABLE(2,효과後誘発)
PRINTL ②：速攻마법 카드が발동한 경우、상대 필드의 카드를 ２장까지 대상으로 하고 발동할 수 있다。
PRINTL その카드를 덱に戻す。
CALLFORM CARD_ABLE(3,효과後誘発)
PRINTL ③：이 카드가 묘지에 存在する状態で「サイクロン」が발동한 경우에 발동할 수 있다。
PRINTL 이 카드를 특수 소환한다。


@CARDCAN_26120(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,26120)
SIF RESULT == 1
	RETURN 0

IF 종류 == 효과後誘発
	IF ゾーン == "손패"
		SIF 발동済_1(듀얼리스트,26120)
			RETURN 0
		CALL CARD_SS_CHECK(듀얼리스트,26120,ゾーン)
		SIF RESULT == 1
			RETURN 0
		CALL FULLRESET
		효과条件종류 = 速攻마법
		CALL CARD_CHECK_FIN_DATA(듀얼리스트,"효과발동",2)
		SIF RESULT != 1
			RETURN 0
	
	ELSEIF ゾーン == "필드"
		SIF 발동済_2(듀얼리스트,26120)
			RETURN 0
		CALL FULLRESET
		효과条件종류 = 速攻마법
		CALL CARD_CHECK_FIN_DATA(듀얼리스트,"효과발동",2)
		SIF RESULT != 1
			RETURN 0
		CALL FULLRESET
		효과範囲상대필드 = 1
		효과範囲상대마함 = 1
		효과条件除去種 = 덱
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		SIF RESULT != 1
			RETURN 0

	ELSEIF ゾーン == "묘지"
		SIF 발동済_3(듀얼리스트,26120)
			RETURN 0
		CALL CARD_SS_CHECK(듀얼리스트,26120,ゾーン)
		SIF RESULT == 1
			RETURN 0
		CALL FULLRESET
		효과条件名前 = サイクロン
		CALL CARD_CHECK_FIN_DATA(듀얼리스트,"효과발동",2)
		SIF RESULT != 1
			RETURN 0
	ELSE
		RETURN 0
	ENDIF
	RETURN 1
ENDIF

@CARDEFFECT_26120(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 選択カード

IF 종류 == 효과後誘発
	IF ゾーン == "손패"
		CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,26120,1)
	ELSEIF ゾーン == "필드"
		CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,26120,2)
	ELSEIF ゾーン == "묘지"
		CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,26120,3)
	ENDIF
	IF RESULT == 0
		IF ゾーン == "필드"
			CALL FULLRESET
			효과条件複数 = 98
			CALL CARD_REMOVAL_BASIC(듀얼리스트,"全場単体",場所,"덱")

		ELSE
			CALL FULLRESET
			CALL CARD_SP_SUMMON(듀얼리스트,ゾーン,場所)
		ENDIF
	ENDIF
ENDIF
