@CARDNAME_93800,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 메달리온 레코드
	CASE "略称"
		RESULTS = 문장의 기록
	CASE "カテゴリ"
		RESULTS:0 = 메달리온
ENDSELECT


@CARD_93800(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 通常罠
	CASE "追加日"
		RETURN 250713
ENDSELECT
RETURN 0

@CARD_EXPLANATION_93800(種類)
#DIM DYNAMIC 種類

PRINTL このカード名のカードは１ターンに１度しか発動できない。
CALLFORM CARD_ABLE(1,発動誘発)
PRINTL ①：相手のモンスターの効果・魔法・罠カードが発動した時に発動できる。
PRINTL 自分の「문장수」XモンスターのX素材を１つ取り除き、その発動を無効にし破壊する。


@CARDCAN_93800(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,93800)
SIF RESULT == 1
	RETURN 0

IF 種類 == 発動誘発
	SIF 発動済_1(決闘者,93800)
		RETURN 0
	SIF ゾーン != "魔罠"
		RETURN 0
	SIF GETVAR(@"%決闘者%_魔_表:場所") == 3
		RETURN 0
	CALL FULLRESET
	CALL CARD_CHECK_START_DATA(決闘者,1)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲戦場 = 1
	効果条件カテゴリ = 문장수
	効果条件Ｘ素所持 = 999
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF


@CARDEFFECT_93800(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正
#DIM DYNAMIC 選択カード

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 発動誘発
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,93800,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(決闘者,ゾーン,場所)
		CALL FULLRESET
		効果範囲戦場 = 1
		効果条件カテゴリ = 문장수
		効果条件Ｘ素所持 = 999
		CALL CARD_CHOOSE_ADV(決闘者,ゾーン,場所)
		選択カード = RESULT
		SIF 選択カード == 9999
			RETURN 0
		CALL CARD_SELECT(決闘者,選択カード)
		選択カード = RESULT
		CALL SET_FIELD_INFO(対象者（CS）,"戦場",選択カード（CS）,"Ｘ",-1)
		
        CALL FULLRESET
	    CALL CARD_REMOVAL_BASIC(決闘者,"発動無効",場所,"墓地")
	ENDIF
ENDIF


