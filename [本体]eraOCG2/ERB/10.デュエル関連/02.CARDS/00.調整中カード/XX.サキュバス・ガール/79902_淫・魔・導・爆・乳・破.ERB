@CARDNAME_79902,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

;ブラック・バーニング
SELECTCASE 参照先
	CASE "名前"
		IF GETBIT(TALENT:MASTER:성별,1)
			RESULTS = 서큐버스 매직 파이즈리
		ELSEIF GETBIT(TALENT:MASTER:성별,0)
			RESULTS = 서큐버스 매직 파후파후
		ELSE
			RESULTS = 서큐버스 매직 파이즈리
		ENDIF
	CASE "略称"
		RESULTS = 음마술폭유파
	CASE "カテゴリ"
	    RESULTS:0 = 매지션 걸
		RESULTS:1 = 서큐버스 걸
		RESULTS:2 = E.R.A
ENDSELECT

@CARD_79902(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 速攻마법
	CASE "生成カード"
		RETURN 宝液ID
ENDSELECT
RETURN 0

@CARDSUMMON_79902(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
RETURN 0

@CARD_EXPLANATION_79902(종류)
#DIM DYNAMIC 종류

CALL TEXT_DECORATION("ROGUE")
PRINTL 이 카드명의 ①②의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALL CARD_ABLE(1,마함起動,손패起動,フリー誘発)
PRINTL ①：자신 필드의 "서큐버스 걸" 몬스터를 ３장까지 대상으로 하고 발동할 수 있다（최소 １장 필요）。
PRINT 자신은
SETCOLOR C_H_PINK
PRINT 대상의 몬스터 전부에게
IF GETBIT(TALENT:MASTER:성별,1)
	PRINT ❤파이즈리❤
ELSEIF GETBIT(TALENT:MASTER:성별,0)
	PRINT ❤파후파후❤
ELSE
	PRINT ❤파이즈리❤
ENDIF
PRINT 를 하게하는것으로、
RESETCOLOR
PRINTL 대상의 몬스터의 수×３００ＬＰ을 잃고、
PRINTL 대상의 몬스터 １장마다 묘지에 "현자의 보액" 토큰을 １장 생성한다。
CALL CARD_ABLE(2,묘지起動)
PRINTL ②：자신 메인 페이즈에 발동할 수 있다。
PRINTL 묘지의 이 카드를 제외하고、덱 / 묘지에서 "매지션 걸" 몬스터 １장、
PRINTL 또는 "매지컬 실크 베드룸－음마술의 비의"１장을 패에 넣는다。
PRINTL 이 때、자신 필드에 "서큐버스 걸" 몬스터가 존재하는 경우、
PRINTL 대신에 자신 필드의 "서큐버스 걸" 몬스터의 수까지 이하의 효과를 적용할 수 있다。
PRINTL ●묘지의 "서큐버스 걸" 마법 카드를 １장 제외하는 것으로、상대 필드의 마법 / 함정 카드 １장을 대상으로 하고 파괴한다。
CALL TEXT_DECORATION("ROGUE")

@CARDCAN_79902(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

#DIM DYNAMIC 条件달성

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,79902)
SIF RESULT == 1
	RETURN 0

IF 종류 == 마함起動 || 종류 == 손패起動 || 종류 == フリー誘発
	SIF 발동済_1(듀얼리스트,79902)
		RETURN 0
	IF ゾーン == "손패"
		SIF ターンプレイヤー != 듀얼리스트
			RETURN 0
	ELSEIF ゾーン == "마함"
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 3
			RETURN 0
	ELSE
		RETURN 0
	ENDIF
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件カテゴリ = 서큐버스 걸
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 묘지起動
	SIF 발동済_2(듀얼리스트,79902)
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		RETURN 0
	SIF ゾーン != "묘지"
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과範囲묘지 = 1
	효과条件スーパー名前 = 매지컬 실크 베드룸－음마술의 비의
	효과条件カテゴリ = 매지션 걸
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT == 1
		条件달성++
	CALL FULLRESET
	효과範囲필드 = 1
	효과条件カテゴリ = 서큐버스 걸
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	IF RESULT == 1
		CALL FULLRESET
		효과範囲묘지 = 1
		효과条件カテゴリ = 서큐버스 걸
		효과条件종류 = 마법全般
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			효과範囲상대마함 = 1
			효과条件除去種 = 묘지
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			SIF RESULT == 1
				条件달성++
		ENDIF
	ENDIF
	SIF 条件달성 < 1
		RETURN 0
    RETURN 1
ENDIF

@CARDEFFECT_79902(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIMS DYNAMIC 対面者
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ

#DIM DYNAMIC 選択マジシャン,3
#DIM DYNAMIC 条件Ａ
#DIM DYNAMIC 条件Ｂ
#DIM DYNAMIC 選択효과
#DIM DYNAMIC 選択カードID
#DIM DYNAMIC カウンタB
#DIM DYNAMIC カウンタC

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 마함起動 || 종류 == 손패起動 || 종류 == フリー誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,79902,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		CALL FULLRESET
		효과範囲필드 = 1
		효과条件カテゴリ = 서큐버스 걸
		효과条件CNT = 1
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		カウンタ = RESULT
		FOR カウンタB, 0, MIN(カウンタ,3)
			CALL FULLRESET
			효과範囲필드 = 1
			효과条件カテゴリ = 서큐버스 걸
			IF カウンタB > 0
				FOR カウンタC, 0, カウンタB
					효과条件選択済:カウンタC = 選択マジシャン:カウンタC
				NEXT
				효과条件任意 = 1
			ENDIF
			효과条件複数 = 1
			CALL CARD_CHOOSE_ADV(듀얼리스트,ゾーン,場所)
			IF RESULT != 9999
				選択マジシャン:カウンタB = RESULT
			ELSE
				BREAK
			ENDIF
		NEXT
		IF カウンタB > 0
			CALL LIFE_CHANGE(듀얼리스트,"減少","コスト",300 * カウンタB)
			REPEAT カウンタB
				CALL CARD_GENERATE_GY(듀얼리스트,宝液ID)
			REND
		ENDIF
	ENDIF
ELSEIF 종류 == 묘지起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,79902,2)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		CALL FULLRESET
		효과範囲덱 = 1
		효과範囲묘지 = 1
		효과条件スーパー名前 = 매지컬 실크 베드룸－음마술의 비의
		효과条件カテゴリ = 블랙 매지션 걸
		효과条件종류 = モン全般
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		SIF RESULT == 1
			条件Ａ++
		CALL FULLRESET
		효과範囲필드 = 1
		효과条件カテゴリ = 서큐버스 걸
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			효과範囲묘지 = 1
			효과条件カテゴリ = 서큐버스 걸
			효과条件종류 = 마법全般
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			IF RESULT == 1
				CALL FULLRESET
				효과範囲상대마함 = 1
				효과条件除去種 = 묘지
				CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
				SIF RESULT == 1
					条件Ｂ++
			ENDIF
		ENDIF
		IF 条件Ａ != 0 && 条件Ｂ != 0
			CALL YN_CHOICE(듀얼리스트,"덱 / 묘지에서 「매지션 걸」몬스터 １장또는 「매지컬 실크 베드룸－음마술의 비의」１장을 패에 넣는다。","『묘지「서큐버스 걸」마법 카드 １장제외、상대 필드의 마법 / 함정 카드 １장을 대상파괴』를 자신 필드의 「서큐버스 걸」몬스터의 수까지 반복한다。")
			選択효과 = RESULT
		ELSEIF 条件Ｂ == 1 && 条件Ａ == 0
			選択효과 = いいえ
		ELSEIF 条件Ａ == 1 && 条件Ｂ == 0
			選択효과 = はい
		ENDIF
		IF 選択효과 == はい
			CALL FULLRESET
			효과範囲덱 = 1
			효과範囲묘지 = 1
			효과条件スーパー名前 = 매지컬 실크 베드룸－음마술의 비의
			효과条件カテゴリ = 매지션 걸
			효과条件종류 = モン全般
			CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
		ELSEIF 選択효과 == いいえ
			CALL FULLRESET
			효과範囲필드 = 1
			효과条件カテゴリ = 서큐버스 걸
			효과条件CNT = 1
			CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
			カウンタ = RESULT
			REPEAT カウンタ
				CALL FULLRESET
				효과範囲묘지 = 1
				효과条件カテゴリ = 서큐버스 걸
				효과条件종류 = 마법全般
				CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
				IF RESULT == 1
					CALL FULLRESET
					효과範囲상대마함 = 1
					효과条件除去種 = 묘지
					CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
					SIF RESULT != 1
						BREAK
				ELSE
					BREAK
				ENDIF
				CALL FULLRESET
				효과範囲묘지 = 1
				효과条件カテゴリ = 서큐버스 걸
				효과条件종류 = 마법全般
				효과条件任意 = 1
				효과条件行先플래그 = 제외行き
				CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
				IF RESULT == 1
					CALL FULLRESET
					효과範囲상대마함 = 1
					CALL CARD_REMOVAL_ADV(듀얼리스트,"単体除去",場所,"묘지")
				ENDIF
			REND
		ENDIF
	ENDIF
ENDIF