@CARDNAME_59852,参照先
;ココで指定カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 초코 서큐버스 걸
	CASE "略称"
		RESULTS = 초코 Ｓ Ｇ
	CASE "カテゴリ"
	    RESULTS:0 = 매지션 걸
		RESULTS:1 = 서큐버스 걸
		RESULTS:2 = E.R.A
ENDSELECT

@CARD_59852(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "種類"
		RETURN 融合
	CASE "属性"
		RETURN 水属性
	CASE "種族"
		RESULT:1 = 悪魔族
		RETURN 魔法使い族
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1600
	CASE "守備力"
		RETURN 1000
	CASE "性別"
		RETURN 牝性
	CASE "生成カード"
		RETURN 宝液ID
	CASE "連動ID"
		RETURN 1005
ENDSELECT
RETURN 0

@CARD_EXPLANATION_59852(種類)
#DIM DYNAMIC 種類

CALL TEXT_DECORATION("ROGUE")
SETCOLOR C_H_PINK
PRINTFORML 「チョコ・매지션 걸」❤「決闘者 %CALLNAME:MASTER%」
RESETCOLOR
;効果番号1はシルクベッドルームの召喚履歴に利用
CALL CARD_ABLE(0,攻守変動)
PRINTL ①：このカードの攻撃力は、墓地の「서큐버스 걸」魔法カードの数×２００アップする。
CALL CARD_ABLE(2,戦場起動)
PRINTL ②：自分メインフェイズに発動できる。
PRINTL 墓地の「서큐버스 걸」魔法カードを１枚除外し、デッキから「서큐버스 걸」魔法カード１枚を手札に加える。
CALL CARD_ABLE(0,ターン終了時強制)
PRINTL ③：自分エンドフェイズに、このカードが元々の持ち主のフィールドに存在する場合に発動する。
PRINT 自分は
SETCOLOR C_H_PINK
PRINT ❤契約の対価❤として
RESETCOLOR
PRINTL ３００ＬＰを失い、墓地に「賢者の宝液」トークン１枚を生成する。
CALL TEXT_DECORATION("ROGUE")

@CARDSUMMON_AA_59852

@CARDSUMMON_59852(決闘者,召喚カード,EX参照)
#DIMS DYNAMIC 決闘者
#DIM DYNAMIC 召喚カード
#DIM DYNAMIC EX参照
;素材条件

CALL RESETCONDITION_MAT
必要素材数 = 2

IF EX参照 == 0 || EX参照 == 1
	素材条件名前 = チョコ・매지션 걸
ELSEIF EX参照 >= 10
	素材条件名前 = 決闘者 %CALLNAME:MASTER%
ENDIF

IF EX参照 == 1
	CALL FUSCAN(決闘者,召喚カード,59852) 
	RETURN RESULT
ENDIF

@CARDCAN_59852(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

CALL CARD_NEGATE(決闘者,種類,ゾーン,場所,59852)
SIF RESULT == 1
	RETURN 0

IF 種類 == 攻守変動
	SIF ゾーン != "戦場"
		RETURN 0
	RETURN 1
ELSEIF 種類 == 戦場起動
	SIF 発動済_2(決闘者,59852)
		RETURN 0
	SIF ゾーン != "戦場"
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	CALL FULLRESET
	効果範囲墓地 = 1
	効果条件カテゴリ = 서큐버스 걸
	効果条件種類 = 魔法全般
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	効果範囲デッキ = 1
	効果条件カテゴリ = 서큐버스 걸
	効果条件種類 = 魔法全般
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 種類 == ターン終了時強制
	SIF ゾーン != "戦場"
		RETURN 0
	SIF ターンプレイヤー != 決闘者
		RETURN 0
	IF 決闘者 == "自分"
		SIF GETVAR(@"%決闘者%_MZ_所有:場所") != 1
			RETURN 0
	ELSE
		SIF GETVAR(@"%決闘者%_MZ_所有:場所") != 2
			RETURN 0
	ENDIF
	RETURN 1
ENDIF



@CARDEFFECT_59852(決闘者,種類,ゾーン,場所)
#DIMS DYNAMIC 決闘者
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 種類
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIMS DYNAMIC 選択カード

CALL 対面者判定(決闘者)
対面者 = %RESULTS%

IF 種類 == 攻守変動
	CALL FULLRESET
	効果範囲墓地 = 1
	効果条件カテゴリ = 서큐버스 걸
	効果条件種類 = 魔法全般
	効果条件CNT = 1
	CALL CARD_CHECK_ADV(決闘者,種類,ゾーン,場所)
	SIF RESULT > 0
		CALL STATCHANGE_BASIC(決闘者,"自身",場所,RESULT * 200)
ELSEIF 種類 == 戦場起動
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,59852,2)
	IF RESULT == 0
		CALL FULLRESET
		効果範囲墓地 = 1
		効果条件カテゴリ = 서큐버스 걸
		効果条件種類 = 魔法全般
		効果条件行先フラグ = 除外行き
		CALL CARD_ADD_GY_ADV(決闘者,ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			効果範囲デッキ = 1
			効果条件カテゴリ = 서큐버스 걸
			効果条件種類 = 魔法全般
			CALL CARD_ADD_ADV(決闘者,ゾーン,場所)
		ENDIF
	ENDIF
ELSEIF 種類 == ターン終了時強制
	CALL CARD_CHOICE(決闘者,種類,ゾーン,場所,59852)
	IF RESULT == 0
		CALL LIFE_CHANGE(決闘者,"減少","コスト",500)
		CALL CARD_GENERATE_GY(決闘者,宝液ID)
	ENDIF
ENDIF
