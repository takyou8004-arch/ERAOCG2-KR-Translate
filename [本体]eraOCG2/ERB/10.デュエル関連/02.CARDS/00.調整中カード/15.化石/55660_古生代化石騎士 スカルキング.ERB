@CARDNAME_55660,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 고생대 화석 기사 스컬킹
	CASE "略称"
		RESULTS = 고생대 화석 기사
	CASE "カテゴリ"
		RESULTS:0 = 화석
ENDSELECT


@CARD_55660(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 융합
	CASE "속성"
		RETURN 땅속성
	CASE "종족"
		RETURN 원소정령족
	CASE "レベル"
		RETURN 8
	CASE "攻撃力"
		RETURN 2800
	CASE "守備力"
		RETURN 1300
	CASE "성별"
		RETURN 雄性
	CASE "추가日"
		RETURN 250919
ENDSELECT

RETURN 0

@CARD_EXPLANATION_55660(종류)
#DIM DYNAMIC 종류

PRINTL 원소정령족モンスター＋レベル７以上のモンスター
PRINTL このカード名の③の효과は１ターンに１度しか使用できない。
CALLFORM CARD_ABLE(1,지속효과)
PRINTL ①：このカードは１度のバトルフェイズ中に２回攻撃できる。
CALLFORM CARD_ABLE(2,지속효과)
PRINTL ②：このカードが守備표시モンスターを攻撃した場合、
PRINTL その守備力を攻撃力が超えた分だけ戦闘ダメージを与える。
CALLFORM CARD_ABLE(3,필드起動,フリー誘発)
PRINTL ③：상대ターンに、상대の묘지のモンスター１体を대상として발동できる。
PRINTL 손패を１枚捨て、대상のモンスターを자신フィールドに특수소환する。

@CARDSUMMON_55660(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照

CALL RESETCONDITION_MAT
必要素材数 = 2

IF EX参照 == 0 || EX参照 == 1
	素材条件종족 = 원소정령족
ELSEIF EX参照 >= 10
	素材条件レベル:0 = 7
	素材条件レベル:1 = 8
	素材条件レベル:2 = 9
	素材条件レベル:3 = 10
	素材条件レベル:4 = 11
	素材条件レベル:5 = 12
ENDIF

IF EX参照 == 1
	CALL FUSCAN(듀얼리스트,소환カード,55660) 
	RETURN RESULT
ENDIF

@CARDCAN_55660(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,55660)
SIF RESULT == 1
	RETURN 0

IF 종류 == 지속효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == 필드起動 || 종류 == フリー誘発
	SIF 발동済_3(듀얼리스트,55660)
		RETURN 0
	SIF ターンプレイヤー == 듀얼리스트
		RETURN 0
	; 손패確認
	CALL FULLRESET
	효과範囲손패 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0

	; 상대묘지確認
	CALL FULLRESET
	효과範囲상대묘지 = 1
	효과条件종류 = モン全般
	효과条件소환可 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0

	RETURN 1
ENDIF


@CARDEFFECT_55660(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

IF 종류 == 지속효과
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"追戦",1)
	CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"貫通")
ELSEIF 종류 == 필드起動 || 종류 == フリー誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,55660,3)
	IF RESULT == 0
		CALL CARD_ADD_GY_BASIC(듀얼리스트,"손패",ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			효과範囲상대묘지 = 1
			CALL CARD_SP_SUMMON_ADV(듀얼리스트,ゾーン,場所)
		ENDIF
	ENDIF
ENDIF

