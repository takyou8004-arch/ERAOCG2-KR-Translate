@CARDNAME_99731,参照先
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 마인드 캡처 어플리케이션
	CASE "略称"
		RESULTS = MC App
	CASE "カテゴリ"
		RESULTS:0 = MC
		RESULTS:1 = E.R.A
ENDSELECT


@CARD_99731(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 엑시즈
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 기계족
	CASE "レベル"
		RETURN 3
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 2200
ENDSELECT
RETURN 0

@CARDSUMMON_99731(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照

CALL RESETCONDITION_MAT
必要素材数 = 5
素材条件数自由 = 1
素材条件レベル = 3
素材条件カテゴリ = MC

IF EX参照 == 1
	CALL XYZCAN(듀얼리스트,소환カード,99731) 
	RETURN RESULT
ENDIF


@CARD_EXPLANATION_99731(종류)
#DIM DYNAMIC 종류

CALL TEXT_DECORATION("ROGUE")
PRINTL "MC" 레벨 ３ 몬스터×２
CALLFORM CARD_ABLE(1,필드起動)
PRINTL ①：１턴에 １번、이 카드의 X소재를 １개 제거하고 발동할 수 있다。
PRINTL 자신 과상대는 덱에서 "MC" 마법 카드를 １장 패에 넣는다。
CALLFORM CARD_ABLE(0,攻撃時誘発)
PRINTL ②：상대 몬스터의 공격 선언시、이 카드의 X 소재를 １개 제거하고 발동할 수 있다。
PRINTL 그 공격을 무효로 하고、상대의 패에 무작위로 "MC" 카드 １장을 넣는다。
CALL CARD_ABLE(0,효과後誘発)
PRINTL ③：상대가 "MC" 마법 카드를 발동한 경우에 발동할 수 있다。
PRINTL 이 카드의 X 소재를 １개 늘린다。
CALLFORM CARD_ABLE(3,필드→묘지,손패→묘지,덱→묘지,破壊→묘지,戦闘→묘지)
PRINTL ④：이 카드가 묘지로 보내진 경우、자신 묘지의 레벨 ３인 "MC" 몬스터 ３장을 대상으로 하고 발동할 수 있다。
PRINTL 그 몬스터 ３장을 덱으로 되돌리고、１장 드로우한다。
CALL TEXT_DECORATION("ROGUE")

@CARDCAN_99731(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,99731)
SIF RESULT == 1
	RETURN 0
IF 종류 == 필드起動
	SIF 발동済_1(듀얼리스트,99731)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= 0
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = MC
	효과条件종류 = 마법全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과範囲상대덱 = 1
	효과条件カテゴリ = MC
	효과条件종류 = 마법全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 攻撃時誘発
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_MZ_Ｘ:場所") <= -1
		RETURN 0
	SIF バトルフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー == 듀얼리스트
		RETURN 0
	CALL FULLRESET
	효과範囲상대필드 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 효과後誘発
	SIF ゾーン != "필드"
		RETURN 0
	CALL FULLRESET
	효과条件종류 = 마법全般
	효과条件カテゴリ = MC
	CALL CARD_CHECK_FIN_DATA(듀얼리스트,"효과발동",1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 필드→묘지 || 종류 == 손패→묘지 || 종류 == 덱→묘지 || 종류 == 破壊→묘지 || 종류 == 戦闘→묘지
	SIF 발동済_3(듀얼리스트,99731)
		RETURN 0
	CALL FULLRESET
	효과範囲묘지 = 1
	효과条件종류 = レベル持ち
	효과条件レベル = 3
	효과条件カテゴリ = MC
	효과条件CNT = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT < 3
		RETURN 0
	CALL CARD_DRAW_CHECK
	RETURN 1
ENDIF

@CARDEFFECT_99731(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 조교대상

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 필드起動
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99731,1,X消費1)
	IF RESULT == 0
		CALL FULLRESET
		효과条件カテゴリ = MC
		효과条件종류 = 마법全般
		CALL CARD_ADD_BASIC(듀얼리스트,"덱",ゾーン,場所)
		IF RESULT == 1
			CALL FULLRESET
			효과条件カテゴリ = MC
			효과条件종류 = 마법全般
			CALL CARD_ADD_BASIC(対面者,"덱",ゾーン,場所)
		ENDIF
	ENDIF
ELSEIF 종류 == 攻撃時誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99731,0,X消費1)
	IF RESULT == 0
		CALL EVADE_ATTACK(듀얼리스트)
		CALL FULLRESET
		SETCOLOR C_PINK 
		SELECTCASE RAND:9
			CASE 0
				CALL CARD_GENERATE_HAND(対面者,79840)
			CASE 1
				CALL CARD_GENERATE_HAND(対面者,79841)
			CASE 2
				CALL CARD_GENERATE_HAND(対面者,79842)
			CASE 3
				CALL CARD_GENERATE_HAND(対面者,79843)
			CASE 4
				CALL CARD_GENERATE_HAND(対面者,79844)
			CASE 5
				CALL CARD_GENERATE_HAND(対面者,79845)
			CASE 6
				CALL CARD_GENERATE_HAND(対面者,79846)
			CASE 7
				CALL CARD_GENERATE_HAND(対面者,79847)
			CASE 8
				CALL CARD_GENERATE_HAND(対面者,79848)
		ENDSELECT
		RESETCOLOR
		PRINTFORML 마인드 캡처 어플리케이션이 깜빡이면서 서로의 공격은 멈추고,
		PRINTFORMW 상대는 어느샌가 최면 카드를 패에 넣고 있었다…。
	ENDIF
ELSEIF 종류 == 효과後誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99731,30)
	IF RESULT == 0
		CALL FULLRESET
		CALL SET_FIELD_INFO(듀얼리스트,ゾーン,場所,"Ｘ",1)
		PRINTFORMW 발동한 최면카드가 마인드 캡처 어플리케이션에게 힘을 부여한다…！
	ENDIF
ELSEIF 종류 == 필드→묘지 || 종류 == 손패→묘지 || 종류 == 덱→묘지 || 종류 == 破壊→묘지 || 종류 == 戦闘→묘지
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99731,3)
	IF RESULT == 0
		CALL FULLRESET
		효과範囲묘지 = 1
		효과条件종류 = レベル持ち
		효과条件レベル = 3
		효과条件カテゴリ = MC
		효과条件複数 = 3
		효과条件行先플래그 = 덱行き
		CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
		IF RESULT == 1
			CALL CARDDRAW_BASIC(듀얼리스트,1)
		ENDIF
	ENDIF
ENDIF
