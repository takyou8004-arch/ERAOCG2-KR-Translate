@CARDNAME_99736,参照先
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 마인드체인지 스파크 플러그
	CASE "略称"
		RESULTS = MC Sparkplug
	CASE "カテゴリ"
		RESULTS:0 = MC
		RESULTS:1 = E.R.A
ENDSELECT


@CARD_99736(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 싱크로
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 기계족
	CASE "レベル"
		RETURN 6
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 2000
ENDSELECT
RETURN 0

@CARDSUMMON_99736(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
#DIM DYNAMIC カウンタ

CALL RESETCONDITION_MAT
必要レベル数 = 6
IF EX参照 == 0 || EX参照 == 1
	素材条件튜너 = 1
ELSEIF EX参照 >= 10
	素材条件튜너 = -1
ENDIF

IF EX参照 == 1
	CALL SYNCAN(듀얼리스트,소환カード,99736) 
	RETURN RESULT
ENDIF

@CARD_EXPLANATION_99736(종류)
#DIM DYNAMIC 종류

CALL TEXT_DECORATION("ROGUE")
PRINTL 튜너＋튜너以外のモンスター１体以上
PRINTL このカード名の②の효과は１ターンに１度しか使用できない。
CALL CARD_ABLE(1,攻守変動)
PRINTL ①：상대묘지に「MC」마법カードが存在する場合、
PRINTL 상대フィールドのモンスターの攻撃力・守備力は１종류につき３００ダウンする。
CALL CARD_ABLE(2,발동誘発)
PRINTL ②：상대がモンスターの효과を발동した時발동できる。
PRINTL その효과は「ランダムなマインド・コントロールカード１枚を生成し손패に加える」となる。
PRINTL 상대は덱から１枚ドローする。
CALLFORM CARD_ABLE(3,ターン終了時)
PRINTL ③：자신エンドフェイズに발동できる。
PRINTL 상대はランダムなマインド・コントロールカード１枚を生成し손패に加える。
CALL TEXT_DECORATION("ROGUE")

@CARDCAN_99736(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,99736)
SIF RESULT == 1
	RETURN 0

IF 종류 == 攻守変動
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == 발동誘発
	SIF 발동済_2(듀얼리스트,99736)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	CALL FULLRESET
	효과条件종류 = モン全般
	효과条件除去種 = 無効
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == ターン終了時
	SIF ゾーン != "필드"
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_99736(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者 
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 攻撃力修正

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 攻守変動
	CALL FULLRESET
	효과範囲상대묘지 = 1
	효과条件カテゴリ = MC
	효과条件종류 = 마법全般
	효과条件CNT = 1
	효과条件固名前起動 = 1
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	攻撃力修正 = RESULT * 300
	CALL FULLRESET
	CALL STATCHANGE_BASIC(듀얼리스트,"상대필드",場所,-(攻撃力修正),-(攻撃力修正))
ELSEIF 종류 == 발동誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99736,2)
	IF RESULT == 0
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"발동無効",場所,"無効")
		SETCOLOR C_PINK 
		SELECTCASE RAND:9
			CASE 0
				CALL CARD_GENERATE_HAND(対面者,79840)
			CASE 1
				CALL CARD_GENERATE_HAND(対面者,79841)
			CASE 2
				CALL CARD_GENERATE_HAND(対面者,79842)
			CASE 3
				CALL CARD_GENERATE_HAND(対面者,79843)
			CASE 4
				CALL CARD_GENERATE_HAND(対面者,79844)
			CASE 5
				CALL CARD_GENERATE_HAND(対面者,79845)
			CASE 6
				CALL CARD_GENERATE_HAND(対面者,79846)
			CASE 7
				CALL CARD_GENERATE_HAND(対面者,79847)
			CASE 8
				CALL CARD_GENERATE_HAND(対面者,79848)
		ENDSELECT
		RESETCOLOR
		SETCOLOR C_PINK
		PRINTFORML 마인드체인지 스파크 플러그が瞬く…。
		PRINTFORMW 상대はいつのまにか최면カードを손패に加えてしまっていた…。
		PRINTFORMW そうそう、こっちの方が良いよね♪
		RESETCOLOR
		RETURN 1
	ENDIF
ELSEIF 종류 == ターン終了時
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,99736,3)
	IF RESULT == 0
		CALL FULLRESET
		SELECTCASE RAND:9
			CASE 0
				CALL CARD_GENERATE_HAND(対面者,79840)
			CASE 1
				CALL CARD_GENERATE_HAND(対面者,79841)
			CASE 2
				CALL CARD_GENERATE_HAND(対面者,79842)
			CASE 3
				CALL CARD_GENERATE_HAND(対面者,79843)
			CASE 4
				CALL CARD_GENERATE_HAND(対面者,79844)
			CASE 5
				CALL CARD_GENERATE_HAND(対面者,79845)
			CASE 6
				CALL CARD_GENERATE_HAND(対面者,79846)
			CASE 7
				CALL CARD_GENERATE_HAND(対面者,79847)
			CASE 8
				CALL CARD_GENERATE_HAND(対面者,79848)
		ENDSELECT
		SETCOLOR C_PINK
		PRINTFORML 마인드체인지 스파크 플러그が瞬く…。
		PRINTFORMW 상대はいつのまにか최면カードを손패に加えてしまっていた…。
		RESETCOLOR
	ENDIF
ENDIF
