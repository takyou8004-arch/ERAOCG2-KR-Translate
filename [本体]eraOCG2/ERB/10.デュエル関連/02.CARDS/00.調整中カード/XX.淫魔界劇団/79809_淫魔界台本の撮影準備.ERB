@CARDNAME_79809,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
RESULTS = 음마계 대본의 촬영 준비
	CASE "略称"
RESULTS = "촬영 준비"
	CASE "カテゴリ"
RESULTS:0 = 마계 대본
		RESULTS:1 = 음마계대본
		RESULTS:2 = E.R.A
ENDSELECT


@CARD_79809(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 通常마법
	CASE "추가日"
		RETURN 250605
ENDSELECT

RETURN 0

@CARD_EXPLANATION_79809(종류)
#DIM DYNAMIC 종류

CALL TEXT_DECORATION("ROGUE")
PRINTL 이 카드명의 ①②의 효과는 각각 1턴에 1번밖에 사용할 수 없다.
CALLFORM CARD_ABLE(1,마함起動,손패起動)
PRINTL ①: 덱에서 「음마계 극단」 몬스터 1장을 마법 & 함정 존에 앞면 표시로 놓는다.
PRINTL 상대 필드에 카드가 존재할 경우, 이 효과로 상대 덱의 암컷 몬스터 1장을 고를 수도 있다.
PRINTL 이 효과로 놓인 카드는 「음마계 극단」・「마계 극단」 카드로도 취급한다.
CALLFORM CARD_ABLE(2,破壊→묘지)
PRINTL ②: 필드에 「음마계 극단」 카드가 존재하고,
PRINTL 이 카드가 상대 효과로 파괴되었을 경우에 발동할 수 있다.
PRINTL 덱・묘지에서 이 카드명 이외의 「음마계 대본」 카드를 원하는 수만큼 마법 & 함정 존에 세트한다.
PRINTL 이 효과로 세트한 카드는 세트한 턴에도 발동할 수 있다.
CALL TEXT_DECORATION("ROGUE")

@CARDCAN_79809(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ


CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,79809)
SIF RESULT == 1
	RETURN 0

IF 종류 == 마함起動 || 종류 == 손패起動
	SIF 발동済_1(듀얼리스트,79809)
		RETURN 0
	SIF メインフェイズ != 1
		RETURN 0
	SIF ターンプレイヤー != 듀얼리스트
		RETURN 0
	SIF 마함数(듀얼리스트) >= 5
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件성별 = 牝性
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL CARD_DRAW_CHECK(1)
	RETURN 1
ELSEIF 종류 == 破壊→묘지
	SIF 발동済_2(듀얼리스트,79809)
		RETURN 0
	SIF 処理中_듀얼리스트 == 듀얼리스트
		RETURN 0
	CALL FULLRESET
	효과範囲필드 = 1
	효과範囲상대필드 = 1
	효과範囲마함 = 1
	효과範囲상대마함 = 1
효과条件カテゴリ = 음마계 극단
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
효과条件カテゴリ = 음마계 대본
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF



@CARDEFFECT_79809(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC 対面者
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 조교대상
#DIM DYNAMIC 条件달성
CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

IF 종류 == 마함起動 || 종류 == 손패起動
	;カード발동確認
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,79809,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		CALL FULLRESET
		효과範囲상대필드 = 1
		효과範囲상대마함 = 1
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		SIF RESULT == 1
			条件달성 = 1
		CALL FULLRESET
		효과範囲덱 = 1
		효과条件종류 = モン全般
		효과条件성별 = 牝性
		IF 条件달성 != 1
효과条件カテゴリ = 음마계 극단
		ELSE
			효과範囲상대덱 = 1
		ENDIF
		CALL CARD_SET_ADV(듀얼리스트,ゾーン,場所,"表側표시")
		IF RESULT == 1
			選択カード = RESULT:1
			CALL CARD_EROTICS(選択カード)
			IF RESULT != -1
				조교대상 = RESULT
				SETCOLOR C_YELLOW
				PRINTFORMW 撮影準備が整えられ、%CALLNAME:조교대상%へのインタビューが始まった
				PRINTL
				SELECTCASE RAND:2
					CASE 0
					PRINTFORMW %CALLNAME:조교대상%は연령を聞かれている…
						SELECTCASE TALENT:(조교대상):연령
							;子供
							CASE -2
								PRINTFORMW %CALLNAME:조교대상%の見た目はどう見ても子供だが、魔界では合法な연령らしい
							;学生
							CASE -1
								SELECTCASE TALENT:(조교대상):소속
									CASE 1
										PRINTFORMW 某アカデミアの生徒だそうだ
									CASEELSE
										PRINTFORMW 普段は学生をしているそうだ
								ENDSELECT
							;若者
							CASE 0
								PRINTFORML
							;大人
							CASE 1 TO 2
								PRINTFORML
						ENDSELECT
					CASE 1
						PRINTFORM バストサイズを聞かれた%CALLNAME:조교대상%は、
						IF TALENT:(조교대상):수치심 == 1 || TALENT:(조교대상):정조 == 1 || TALENT:(조교대상):자기과시욕 == -1
							PRINT 恥ずかしそうに
						ENDIF
						PRINTFORMW %DECIMAL_STRING(CFLAG:(조교대상):가슴초기치 + CFLAG:(조교대상):가슴성장,2)%cmの%TOSTR_CUPSIZE(조교대상)%カップだと答えた
					CASE 2
					PRINTFORM オナニーについて聞かれた%CALLNAME:조교대상%は、
					IF TALENT:(조교대상):무지 == 1 
						PRINTFORMW よくわからないと答えた
					ELSE
						IF TALENT:(조교대상):수치심 == 1 || TALENT:(조교대상):정조 == 1 || TALENT:(조교대상):자기과시욕 == -1
							PRINT 恥ずかしそうに
						ENDIF
						IF ABL:(조교대상):자위중독 > 0 || TALENT:(조교대상):자위하기쉬움 == 1
							PRINTW 毎日していることを話した
						ELSEIF EXP:(조교대상):자위경험 == 0
							PRINTW したことがないと答えた
						ELSE
							PRINTW 時々すると答えた
						ENDIF
					ENDIF
				ENDSELECT
				RESETCOLOR
				CALL EROTIC_MOVE("晒しもの",조교대상,-1,150)
			ENDIF
			TRYCALLFORM CARDNAME_{選択カード}, "カテゴリ"	
			IF !MATCH(RESULTS, "淫魔界劇団")
				CALL CARD_SET_CATEGORY(듀얼리스트,"마함",カード설치先,"魔界劇団")
				CALL CARD_SET_CATEGORY(듀얼리스트,"마함",カード설치先,"淫魔界劇団",1)
			ENDIF
			;インタビューだけで売るのはちょっと
			;SIF GETVARS(@"%듀얼리스트%_フィールド名") == "淫魔界スタジオ"
				;CALL INMAKAIFILM_GENERATE(듀얼리스트,79809,1,選択カード)
		ENDIF
	ENDIF
ELSEIF 종류 == 破壊→묘지
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,79809,2)
	IF RESULT == 0
		REPEAT 5
			SIF 마함数(듀얼리스트) >= 5
				BREAK
			CALL FULLRESET
			효과範囲덱 = 1
			효과範囲묘지 = 1
효과条件カテゴリ = 음마계 대본
효과条件固名前 = 음마계 대본의 촬영 준비
			효과条件종류 = 마함全般
			효과条件任意 = 1
			CALL CARD_SET_ADV(듀얼리스트,ゾーン,場所,"使化裏側표시")
			SIF RESULT == 9999
				BREAK
		REND
	ENDIF
ENDIF