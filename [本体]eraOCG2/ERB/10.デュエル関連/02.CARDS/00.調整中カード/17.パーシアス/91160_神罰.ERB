@CARDNAME_91160,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 신벌
	CASE "略称"
		RESULTS = 신벌
	CASE "カテゴリ"
		RESULTS:0 = 천공의 성역
		RESULTS:1 = 파샤스
ENDSELECT


@CARD_91160(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 通常함정
	CASE "レア度"
		RETURN SPR
	CASE "추가日"
		RETURN 250915
ENDSELECT

RETURN 0

@CARD_EXPLANATION_91160(종류)
#DIM DYNAMIC 종류

PRINTL このカードの발동に対して상대は효과を발동できない。
PRINTL このカード名のカードは１ターンに１枚しか발동できない。
CALLFORM CARD_ABLE(1,발동誘発)
PRINTL ①：상대が마법・함정・モンスターの효과を발동した時に자신フィールドの천사족モンスター１体をリリースして발동できる。
PRINTL （자신フィールドが「천공의 성역」である場合、リリースなしで발동する事ができる。）
PRINTL その발동を無効にし破壊する。
PRINTL 
PRINTL 

;【エンジェル・パーミッション】の核心となる、万能無効型の카운터함정（相当のチェーン불가の함정カード）
;サーチ手段を色々と手厚くして構えやすくした都合上、名称ターン１をつけて同一ターン中にサーチしてきて妨害추가は出来ないよう調整。
;そのぶん、フィールドが「천공의 성역」でなくても자신フィールドの천사족モンスター１体をコストにして발동できるようにしている。

;묘지효과などの気が利いた추가효과は持ってないスタンダードな함정カードなので、파샤스의 신역の효과等で気軽に덱に戻して再利用してもよい。


@CARDCAN_91160(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIMS DYNAMIC 対面者
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 추가条件플래그 = 0

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,91160)
SIF RESULT == 1
	RETURN 0

IF 종류 == 발동誘発
	SIF 발동済_1(듀얼리스트,91160)
		RETURN 0
	;손패발동条件を満たしているかチェック（ＰゾーンにＳアリアドネーが居るかチェック）　RESULT == 1で판정
	;除去種無効でチェックしたら、カードが無効状態かどうかを판정できる気がするので試験的に１行추가。
	CALL FULLRESET
	효과範囲마함 = 1
	효과条件名前 = 천공 사랑천사 아리아드네
	효과条件除去種 = 無効
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT == 1
		추가条件플래그 |= 1
	
	IF ゾーン != "마함"
		SIF ゾーン != "손패" || 추가条件플래그 != 1
			RETURN 0
	ELSE
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 3
			RETURN 0
		SIF GETVAR(@"%듀얼리스트%_魔_表:場所") == 1
			RETURN 0
	ENDIF
	;자신フィールドが천공의 성역でない場合、발동する時にフィールドに천사족モンスターが必要
	IF GETVARS(@"%듀얼리스트%_フィールド名") != "천공의 성역"
		CALL FULLRESET
		효과範囲필드 = 1
		효과条件종족 = 천사족
		효과条件종류 = モン全般
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		SIF RESULT != 1
			RETURN 0
	ENDIF
	
	CALL FULLRESET
	CALL CARD_CHECK_START_DATA(듀얼리스트,1)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == チェーン可否
	RETURN 1
ENDIF



@CARDEFFECT_91160(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC 효과先
#DIMS DYNAMIC 対面者

CALL 対面者판정(듀얼리스트)
対面者 = %RESULTS%


IF 종류 == 발동誘発
	;カード발동確認
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,91160,1)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所)
		IF GETVARS(@"%듀얼리스트%_フィールド名") != "천공의 성역"
			CALL FULLRESET
			효과範囲필드 = 1
			효과条件종족 = 천사족
			효과条件종류 = モン全般
			CALL CARD_ADD_GY_ADV(듀얼리스트,ゾーン,場所)
			SIF RESULT != 1
				RETURN 0
		ENDIF
		CALL FULLRESET
		CALL CARD_REMOVAL_BASIC(듀얼리스트,"발동無効",場所,"묘지")
		RETURN 1
	ENDIF
ENDIF


