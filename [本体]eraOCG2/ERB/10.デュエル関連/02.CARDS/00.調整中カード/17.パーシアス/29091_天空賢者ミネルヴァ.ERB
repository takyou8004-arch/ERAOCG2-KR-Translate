@CARDNAME_29091,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 천공현자 미네르바
	CASE "略称"
		RESULTS = 천공 미네르바
	CASE "カテゴリ"
		RESULTS:0 = 파샤스
ENDSELECT


@CARD_29091(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 효과モン
	CASE "속성"
		RETURN 빛속성
	CASE "종족"
		RETURN 천사족
	CASE "レベル"
		RETURN 4
	CASE "攻撃力"
		RETURN 1700
	CASE "守備力"
		RETURN 400
	CASE "성별"
		RETURN 牝性
	CASE "추가日"
		RETURN 250915
ENDSELECT
RETURN 0

@CARD_EXPLANATION_29091(종류)
#DIM DYNAMIC 종류

PRINTL 이 카드명의 ①②의 효과는 각각 １턴에 １번밖에 사용할 수 없다。
CALLFORM CARD_ABLE(1,효과後誘発)
PRINTL ①：자신 필드가 "천공의 성역"인 상태로 자신이 일반 함정 카드의 효과를 발동한 경우에 발동할 수 있다。
PRINTL 자신 필드의 천사족 / 빛속성 몬스터의 공격력을 ３００ 올리고、자신의 덱에서 "파샤스" 함정 카드 １장을 패에 넣는다。
CALLFORM CARD_ABLE(2,身代효과)
PRINTL ②：자신 필드의 "천공의 성역" 필드 마법 카드가 파괴된 경우、
PRINTL 대신에 필드 / 묘지의 이 카드를 제외할 수 있다。

;どうみても女の子モンスターなシルエットと名前なので牝性モンスターとする。
;파샤스の盤面固めで起用する可能性がある、という程度のモンスター。単体で展開に繋がる能力は持っていない。
;파샤스の展開盤面にこいつも横に並んでたら地獄を見ることになるが、勝ち確の盤面のダメ押しみたいな面が大きいので、
;プレイヤーが構築を詰めていくと枚数が減っていく타입のモンスターである想定。デスピアの大導劇神みたいな타입。
;ただ、だからといって不要な存在というわけではなく、サーチ先の選択肢として二番目、三番目の優先順位のカードも多少は必要である。


@CARDSUMMON_AA_29091


@CARDCAN_29091(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,29091)
SIF RESULT == 1
	RETURN 0

IF 종류 == 효과後誘発
	SIF 발동済_1(듀얼리스트,29091)
		RETURN 0
	SIF ゾーン != "필드"
		RETURN 0
	SIF GETVARS(@"%듀얼리스트%_フィールド名") != "천공의 성역"
		RETURN 0
	CALL FULLRESET
	효과条件종류 = 通常함정
	CALL CARD_CHECK_FIN_DATA(듀얼리스트,"효과발동")
	SIF RESULT != 1
		RETURN 0
	CALL FULLRESET
	효과範囲덱 = 1
	효과条件カテゴリ = 파샤스
	효과条件종류 = 함정全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 身代효과
	SIF 발동済_2(듀얼리스트,29091)
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_제외NG置換") >= 1
		RETURN 0
	SIF !GROUPMATCH(ゾーン,"필드","묘지")
		RETURN 0
	TRYCALLFORM CARD_{破壊대상ID}("종류")
	SIF RESULT != フィールド
		RETURN 0
	VARSET RESULTS
	TRYCALLFORM CARDNAME_{破壊대상ID}("カテゴリ")
	SIF !MATCH(RESULTS,"천공의 성역")
		RETURN 0
	SIF !GROUPMATCH(破壊종류,"효과")
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_29091(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM 손패選択
#DIM 選択カード
#DIM DYNAMIC 攻守修正値

IF 종류 == 효과後誘発
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,29091,1)
	IF RESULT == 0
		CALL FULLRESET
		효과条件종족 = 천사족
		효과条件속성 = 빛속성
		CALL STATCHANGE_EFFECT(듀얼리스트,"필드",場所,"永攻",300)
		
		CALL FULLRESET
		효과範囲덱 = 1
		효과条件カテゴリ = 파샤스
		효과条件종류 = 함정全般
		CALL CARD_ADD_ADV(듀얼리스트,ゾーン,場所)
	ENDIF
ELSEIF 종류 == 身代효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,29091,2)
	IF RESULT == 0
		CALL CARD_DISPOSE(듀얼리스트,ゾーン,場所,제외行き)
		IF RESULT == 1
			;CALL FULLRESET
			;효과条件カテゴリ = 천공의 성역
			;효과条件종류 = フィールド
			;CALL STATCHANGE_EFFECT(듀얼리스트,"마함",場所,"破耐",6)
			RETURN 1
		ENDIF
	ENDIF
	RETURN 0
ENDIF
