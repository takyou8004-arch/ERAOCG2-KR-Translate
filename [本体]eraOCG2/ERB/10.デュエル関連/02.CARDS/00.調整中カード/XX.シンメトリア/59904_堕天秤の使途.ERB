@CARDNAME_59904,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 타천칭의 사도
	CASE "略称"
		RESULTS = 타천칭의 사도

	CASE "要塞名"
		RESULTS = 타천칭
	CASE "カテゴリ"
		RESULTS:0 = 심메트리아
		RESULTS:1 = 타천칭
		RESULTS:2 = 포트리스
		RESULTS:3 = E.R.A
ENDSELECT


@CARD_59904(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 초차원
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 천사족
	CASE "レベル"
		RETURN 11
	CASE "コスト"
		RETURN 11
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 4000
	CASE "성별"
		RETURN 牝性
	CASE "추가日"
		RETURN 250721
ENDSELECT

RETURN 0

@CARDSUMMON_59904(듀얼리스트,소환カード,EX参照)
#DIMS DYNAMIC 듀얼리스트
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 소환カード
#DIM DYNAMIC EX参照
;素材条件

RETURN 0

@CARD_EXPLANATION_59904(종류)
#DIM DYNAMIC 종류

PRINTL 『초차원』により降臨。
PRINTL （このカードはカードの효과でのみEX덱からフィールドに특수소환・마함ゾーンに置くことができる）
CALL TEXT_DECORATION("D_HEART_F")
PRINTL 포트리스側　《타천칭》 コスト：１１
CALLFORM CARD_ABLE(1,지속효과)
PRINTL ①：このカードが存在する限り、
PRINTL 자신は直接攻撃できず、「심메트리아」または「타천칭」モンスターしかEX덱から특수소환できない。
CALLFORM CARD_ABLE(2,마함起動,フリー誘発)
PRINTL ②：상대ターンに一度、このカードの個別카운터を１つ取り除いて발동できる。
PRINTL 상대フィールド・묘지のモンスター１体を「심메트리아・スレイヴ」として、
PRINTL 지속마법カード扱いで자신の마법＆함정ゾーンに表側표시で置く。
CALLFORM CARD_ABLE(3,발동強制誘発)
PRINTL ③：「심메트리아」마법・함정カードの효과が발동した場合に발동する。
PRINTL このカードに個別카운터を１つ置く（最大２０個まで）。
CALL CARD_ABLE(4,ターン終了時)
PRINTL ④：エンドフェイズに발동できる。자신の마법＆함정ゾーンに存在しているモンスターにセクハラを行う。
PRINTL これが자신のエンドフェイズ中であれば、さらにこのカードの個別카운터の数×１００ダメージを与える。
PRINTL その後、자신フィールドに「심메트리아・スレイヴ」が４体以上存在している場合、このカードをモンスターとして특수소환できる。
CALL TEXT_DECORATION("D_HEART_2")


@CARDCAN_59904(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC 必要カード

CALL CARD_NEGATE(듀얼리스트,종류,ゾーン,場所,59904)
SIF RESULT == 1
	RETURN 0

IF 종류 == 지속효과
	;마법面①の효과（EX덱からの소환制限・直接攻撃制限）
	;モンスター面①の효과（대상耐性）
	SIF !GROUPMATCH(ゾーン, "마함","필드")
		RETURN 0
	RETURN 1
ELSEIF 종류 == 마함起動 || 종류 == フリー誘発
	;마법面②の효과
	SIF ゾーン != "마함"
		RETURN 0
	SIF ターンプレイヤー == 듀얼리스트
		RETURN 0
	SIF 발동済_2(듀얼리스트,59904)
		RETURN 0
	SIF GETVAR(@"%듀얼리스트%_魔_個Ｃ:場所") < 1
		RETURN 0
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과範囲상대묘지 = 1
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == 발동強制誘発
	;마법面③の효과（카운터を乗せる）
	SIF ゾーン != "마함"
		RETURN 0
	;카운터の上限は２０個
	CALL FULLRESET
		SIF GETVAR(@"%듀얼리스트%_魔_個Ｃ:場所") >= 20
		RETURN 0
	효과条件カテゴリ = 심메트리아
	효과条件固カテゴリ = 타천칭
	효과条件종류 = 마함全般
	CALL CARD_CHECK_START_DATA_2(듀얼리스트,2)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ELSEIF 종류 == ターン終了時 && ゾーン == "마함"
	;마법面④の효과
	SIF ゾーン != "마함"
		RETURN 0
	;セクハラ효과があるので카운터が無くても발동可能な仕様
	;空打ちできるようになっちゃうけどまあいいでしょう
	RETURN 1
ELSEIF 종류 == ターン終了時 && ゾーン == "필드"
	;モンスター面④の효과
	SIF ゾーン != "필드"
		RETURN 0
	RETURN 1
ELSEIF 종류 == 필드起動 || 종류 == フリー誘発
	;モンスター面③の효과
	SIF ゾーン != "필드"
		RETURN 0
	SIF 발동済_3(듀얼리스트,159904)
		RETURN 0
	CALL FULLRESET
	효과範囲상대필드 = 1
	효과範囲상대묘지 = 1
	효과条件종류 = モン全般
	CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
	SIF RESULT != 1
		RETURN 0
	RETURN 1
ENDIF

@CARDEFFECT_59904(듀얼리스트,종류,ゾーン,場所)
#DIMS DYNAMIC 듀얼리스트
#DIMS DYNAMIC ゾーン
#DIM DYNAMIC 설정場所
#DIM DYNAMIC 종류
#DIM DYNAMIC 場所
#DIM DYNAMIC カウンタ
#DIM DYNAMIC ダメージ量
#DIM DYNAMIC 選択カード
#DIM DYNAMIC 選択カード_ID
#DIM DYNAMIC カード추가先
#DIM DYNAMIC 조교대상


IF 종류 == 지속효과
	IF ゾーン == "마함"
		;포트리스状態での지속효과
		CALL CARD_EX_RESTRICTION(듀얼리스트,"심메트리아")
		CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"直無")
	ELSEIF ゾーン == "필드"
		;モンスター状態での지속효과
		CALL STATCHANGE_EFFECT(듀얼리스트,"自身",場所,"対耐")
	ENDIF
ELSEIF 종류 == 마함起動 || 종류 == フリー誘発
	;마법面の磔효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,59904,2)
		IF RESULT == 0
			;次にカードを置く予定の領域を취득
			カード추가先 = 現마함(듀얼리스트)
			;상대のカードを置く
			CALL FULLRESET
			효과範囲상대필드 = 1
			효과範囲상대묘지 = 1
			효과条件표시 = 10
			효과条件종류 = モン全般
			효과条件除去種 = 強奪
			CALL CARD_SET_ADV(듀얼리스트,ゾーン,場所,"表側표시")
			;名前を「Symスレイヴ」に변경（処理上はこの表記）
			CALL CARD_SET_RENAME(듀얼리스트,"마함",カード추가先,"Symスレイヴ")
			;카운터を取り除く
			CALL FULLRESET
			CALL SET_FIELD_INFO(듀얼리스트,"마함",場所,"個Ｃ",-1)
		ENDIF
ELSEIF 종류 == 발동強制誘発
	;자신に카운터を乗せる
	PRINTFORMW 「타천칭」に카운터が置かれた。
	CALL SET_FIELD_INFO(듀얼리스트,"마함",場所,"個Ｃ",1)
ELSEIF 종류 == ターン終了時 && ゾーン == "마함"
	;마법面のEP효과
	;セクハラを行う
	CALL CARD_HOSTAGE_FORT_59904(듀얼리스트,종류,ゾーン,場所)
	
	;자신のターンである事をチェック
	IF ターンプレイヤー == 듀얼리스트
		;카운터の数だけダメージを与える
		ダメージ量 = GETVAR(@"%듀얼리스트%_魔_個Ｃ:場所") * 100
		CALL CARD_DAMAGE_59904(듀얼리스트,종류,ゾーン,場所,ダメージ量)
		;「심메트리아・スレイヴ」が4枚あるかチェック
		CALL FULLRESET
		효과範囲필드 = 1
		효과範囲마함 = 1
		효과条件名前 = Symスレイヴ
		효과条件종류 = モン全般
		효과条件CNT = 1
		CALL CARD_CHECK_ADV(듀얼리스트,종류,ゾーン,場所)
		IF RESULT >= 4
			;条件を満たしているので変形
			CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,59904)
			IF RESULT == 0
				PRINTFORMW 타천칭の左右が釣り合った……
				PRINTFORML 強烈な光の中から新たな天使が現れた！
				CALL RESETCONDITION
				CALL CARD_SP_SUMMON(듀얼리스트,ゾーン,場所)
			ENDIF
		ENDIF
	ENDIF
ELSEIF 종류 == ターン終了時 && ゾーン == "필드"
	;モンスター面のEP효과
	;セクハラを行う
	CALL CARD_HOSTAGE_MONSTER_59904(듀얼리스트,종류,ゾーン,場所)
	ダメージ量 = 2000
	CALL CARD_DAMAGE_59904(듀얼리스트,종류,ゾーン,場所,ダメージ量)
ELSEIF 종류 == 필드起動 || 종류 == フリー誘発
	;モンスター面の磔효과
	CALL CARD_CHOICE(듀얼리스트,종류,ゾーン,場所,159904,3)
		IF RESULT == 0
			;カードを置く位置の情報を취득
			カード추가先 = 現마함(듀얼리스트)
			;상대のカードを置く
			CALL FULLRESET
			효과範囲상대필드 = 1
			효과範囲상대묘지 = 1
			효과条件표시 = 10
			효과条件종류 = モン全般
			효과条件除去種 = 強奪
			CALL CARD_SET_ADV(듀얼리스트,ゾーン,場所,"表側표시")
			;名前を「Symスレイヴ」に변경（処理上はこの表記）
			CALL CARD_SET_RENAME(듀얼리스트,"마함",カード추가先,"Symスレイヴ")
		ENDIF
ELSEIF 종류 == 지속효과
	;필드に出た時の書き換え処理？
	SIF ゾーン == "필드"
		CALL SET_FIELD_INFO(듀얼리스트,"필드",場所,"変化元ID",159904,上書きモード)
ENDIF





@CARDNAME_159904,参照先
;ココで지정カードの名前、略称を返す予定
#DIMS DYNAMIC 参照先

VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "名前"
		RESULTS = 타천칭의 사도
	CASE "略称"
		RESULTS = 타천칭의 사도

	CASE "要塞名"
		RESULTS = 타천칭
	CASE "カテゴリ"
		RESULTS:0 = 심메트리아
		RESULTS:1 = 타천칭
		RESULTS:2 = 포트리스
		RESULTS:3 = E.R.A
ENDSELECT

@CARD_159904(参照先)

#DIMS DYNAMIC 参照先
VARSET RESULT
VARSET RESULTS

SELECTCASE 参照先
	CASE "종류"
		RETURN 초차원
	CASE "속성"
		RETURN 어둠속성
	CASE "종족"
		RETURN 천사족
	CASE "レベル"
		RETURN 11
	CASE "コスト"
		RETURN 11
	CASE "攻撃力"
		RETURN 0
	CASE "守備力"
		RETURN 4000
	CASE "성별"
		RETURN 牝性
	CASE "추가日"
		RETURN 250721
ENDSELECT

@CARD_EXPLANATION_159904(종류)
#DIM DYNAMIC 종류

CALL TEXT_DECORATION("D_HEART_C")
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ①：このカードは대상耐性を持つ。
CALLFORM CARD_ABLE(0,지속효과)
PRINTL ②：このカードが存在する限り、「심메트리아」カードによってカード효과を選ぶ場合、
PRINTL その효과はどちらも適用され、要求する行為は過激なものになる。
;②の효과は심메트리아モンスターに추가条件として書く
CALLFORM CARD_ABLE(0,필드起動,フリー誘発)
PRINTL ③：자신・상대ターンに、상대フィールド・묘지のモンスター１体を대상として발동できる。
PRINTL そのモンスターを「심메트리아・スレイヴ」として、지속마법カード扱いで자신の마법＆함정ゾーンに表側표시で置く。
CALL CARD_ABLE(10,ターン終了時)
PRINTL ④：エンドフェイズに발동できる。자신の마법＆함정ゾーンに存在しているモンスターに過激なセクハラを行う。
PRINTL その後、２０００ダメージを与える。
CALL TEXT_DECORATION("D_HEART_2")