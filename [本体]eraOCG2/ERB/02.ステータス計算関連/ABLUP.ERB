;Ｃ감각
@ABLUP0
CALL ABL_UP(0)

;Ｖ감각
@ABLUP1
CALL ABL_UP(1)

;Ａ감각
@ABLUP2
CALL ABL_UP(2)

;Ａ감각
@ABLUP3
CALL ABL_UP(3)

;순종
@ABLUP10
CALL ABL_UP(10)

;욕망
@ABLUP11
CALL ABL_UP(11)

;기교
@ABLUP12
CALL ABL_UP(12)

;봉사정신
@ABLUP13
CALL ABL_UP(13)

;노출증
@ABLUP14
CALL ABL_UP(14)

;마조ッ気
@ABLUP15
CALL ABL_UP(15)

;새드ッ気
@ABLUP16
CALL ABL_UP(16)

;레즈ッ気
@ABLUP17
CALL ABL_UP(17)

;호모ッ気
@ABLUP18
CALL ABL_UP(18)

;자위중독
@ABLUP30
CALL ABL_UP(30)

;정액중독
@ABLUP31
CALL ABL_UP(31)

;레즈중독
@ABLUP32
CALL ABL_UP(32)

;게이중독
@ABLUP33
CALL ABL_UP(33)

;汎用処理
@ABL_UP(ARG)
CUSTOMDRAWLINE —
;上限
CALL MAXABL(ARG)
SIF RESULT
	RETURN 0

VARSET LOCAL
;個別処理
;봉사정신
SIF ARG == 13
	PRINTFORML 순종{ABL:ARG + 1}LV이상필요
;마조ッ気
SIF ARG == 15
	PRINTFORML 욕망{ABL:ARG + 1}LV이상필요
;새드ッ気
SIF ARG == 16
	PRINTFORML 욕망{ABL:ARG + 1}LV이상필요
;자위중독
SIF ARG == 30
	PRINTFORML C감각{ABL:ARG + 1}LV이상、노출증{ABL:ARG + 1}LV이상필요
;정액중독
SIF ARG == 31
	PRINTFORML 봉사정신{ABL:ARG + 1}LV이상필요
;레즈중독
SIF ARG == 32
	PRINTFORML 레즈끼{ABL:ARG + 1}LV이상필요
;게이중독
SIF ARG == 33
	PRINTFORML 호모끼{ABL:ARG + 1}LV이상필요

IF EXP_DEMAND(ARG,0,53) == 1
	PRINTFORML 이상경험있음、또한
ELSEIF EXP_DEMAND(ARG,0,53) > 1
	PRINTFORML 이상경험{EXP_DEMAND(ARG,0,43)}이상、또한
ENDIF
FOR LOCAL,0,4
	IF JUEL_DEMAND(ARG,LOCAL) == -2
		PRINTL 능력부족
		BREAK
	ENDIF
	SIF JUEL_DEMAND(ARG,LOCAL) < 0
		CONTINUE
	PRINTFORM [{LOCAL}] -
	;必要な宝珠を探す
	FOR LOCAL:1,0,100
		IF JUEL_DEMAND(ARG,LOCAL,LOCAL:1) > 0
			SIF JUEL:(LOCAL:1) < JUEL_DEMAND(ARG,LOCAL,LOCAL:1)
				LOCAL:3 ++
			PRINTFORM  %PALAMNAME:(LOCAL:1)%の珠×{JUEL_DEMAND(ARG,LOCAL,LOCAL:1)}
		ENDIF
	NEXT
	;必要な경험を探す
	FOR LOCAL:1,0,100
		IF EXP_DEMAND(ARG,LOCAL,LOCAL:1) > 0
			SIF  EXP:(LOCAL:1) < EXP_DEMAND(ARG,LOCAL,LOCAL:1)
				LOCAL:4 ++
			;이상경험は先に処理した
			SIF LOCAL:1 == 43
				CONTINUE
			PRINTFORM  %EXPNAME:(LOCAL:1)%
			IF EXP_DEMAND(ARG,LOCAL,LOCAL:1) == 1
				PRINT 가 필요
			ELSE
				PRINTFORM {EXP_DEMAND(ARG,LOCAL,LOCAL:1)}以上
			ENDIF
		ENDIF
	NEXT
	PRINT ・・・
	SIF LOCAL:3
		PRINT  보주부족
	SIF LOCAL:4
		PRINT  경험부족
	IF !LOCAL:3 && !LOCAL:4 && !LOCAL:5
		LOCAL:(10 + LOCAL) = 1
		PRINT ＯＫ
	ENDIF
	LOCAL:3 = 0
	LOCAL:4 = 0
	LOCAL:5 = 0
	PRINTL 
NEXT

PRINTL [100] - 끝내다

INPUT
IF RESULT == 100
	RETURN 0
ELSEIF RESULT < 0 || RESULT > LOCAL
	RESTART
ELSEIF !LOCAL:(10 + RESULT)
	PRINTW 조건을 충족하지 못합니다
	RESTART
ENDIF

FOR LOCAL,0,40
	JUEL:LOCAL -= JUEL_DEMAND(ARG,RESULT,LOCAL)
NEXT
ABL:ARG += 1
PRINTFORMW %ABLNAME:ARG%의 레벨이{ABL:ARG}되었습니다
RETURN 0

@ISABLUP(ARG)
#FUNCTION
VARSET LOCAL
;上限설정
IF ABL:ARG >= 99
	RETURNF 0
ELSEIF ABL:ARG >= 5 && !TALENT:3
	RETURNF 0
ENDIF

FOR LOCAL,0,4
	SIF JUEL_DEMAND(ARG,LOCAL) == -2
		RETURNF 0
	SIF JUEL_DEMAND(ARG,LOCAL) < 0
		CONTINUE
	;必要な宝珠を探す
	FOR LOCAL:1,0,100
		IF JUEL_DEMAND(ARG,LOCAL,LOCAL:1) > 0
			SIF JUEL:(LOCAL:1) < JUEL_DEMAND(ARG,LOCAL,LOCAL:1)
				LOCAL:2 ++
		ENDIF
	NEXT
	;必要な경험を探す
	FOR LOCAL:1,0,100
		IF EXP_DEMAND(ARG,LOCAL,LOCAL:1) > 0
			SIF  EXP:(LOCAL:1) < EXP_DEMAND(ARG,LOCAL,LOCAL:1)
				LOCAL:2 ++ 
		ENDIF
	NEXT
	SIF !LOCAL:2
		RETURNF 1
	LOCAL:2 = 0
NEXT

