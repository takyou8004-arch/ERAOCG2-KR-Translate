;----------------------------------------------------------
;@明度변경(ARG , ARG:1)
;지정した色を지정した明度に변경して返す式中間数。
;
;ARG 	= 	透明度（0～100％）
;ARG:1 	=	色。0xFFFFFFのような16進数で入力していると分かりやすいです。
;----------------------------------------------------------
@明度변경, ARG, ARG:1
#FUNCTION
#DIM DYNAMIC 지정色R
#DIM DYNAMIC 지정色G
#DIM DYNAMIC 지정色B
#DIM DYNAMIC L_NUM
;지정된 色を分解
지정色R = ARG:1 / POWER(16 , 4)
지정色G = ARG:1 % POWER(16 , 4) / POWER(16 , 2)
지정色B = ARG:1 % POWER(16 , 2)
;明度によって色を변경
지정色R = LIMIT(지정色R * ARG / 100 , 0 , 0xFF)
지정色G = LIMIT(지정色G * ARG / 100 , 0 , 0xFF)
지정色B = LIMIT(지정色B * ARG / 100 , 0 , 0xFF)
RETURNF 지정色R * POWER(16 , 4) + 지정色G * POWER(16 , 2) + 지정色B
