@IRAI_EVENT_473(参照先)
#DIMS DYNAMIC 参照先
#DIM DYNAMIC 대상캐릭터

VARSET RESULT
VARSET RESULTS


SELECTCASE 参照先
	CASE "名前"
		RESULTS = (CP-ANT3)강해진 힘
	CASE "이벤트種"
		RETURN 이벤트_通常
	CASE "依頼先"
		RETURN 即時開始
	CASE "詳細"
		PRINTL 몬스터를 카드화하는 것에도 익숙해졌다.
		PRINTL 이제 슬슬 다음 스테이지로 나아가도 괜찮겠지.
		PRINTL 
	CASE "条件"
		SIF 依頼_完了횟수:471 == 0
			RETURN 0
		SIF 依頼_全完了횟수() < 10
			RETURN 0
		RETURN 1
ENDSELECT

RETURN 0

@IRAI_ACT_473(参照)
#DIM DYNAMIC 参照

IF 依頼_달성상황 == 0
	PRINTFORMW 듀얼 디스크를 팔에 끼우고, 자신의 신체에 접촉한다.
	PRINTFORMW 여기에서부터 더욱 이미지를 심화시켜 간다…
	PRINTFORMW ………
	PRINTFORMW ……
	PRINTFORMW …
	PRINTFORMW 
	CALL CARD_GET_SINGLE(79880)
	ITEM:79880 += 2
	CALL CARD_GET_SINGLE(79881)
	ITEM:79881 += 2
	CALL CARD_GET_SINGLE(79882)
	ITEM:79882 += 2
	CALL CARD_GET_SINGLE(79883)
	ITEM:79883 += 2
	CALL CARD_GET_SINGLE(79884)
	ITEM:79884 += 2

	PRINTFORMW 
	PRINTFORMW ……됐다.
	IF TALENT:MASTER:매지컬자지 == 1
		PRINTFORMW 매지컬자지라니 판타지 같은 카드다.
		PRINTFORMW 뭐 실제로 있는 거니까 어쩔 수 없다.
	ELSE
		PRINTFORMW 어쩌지, 성욕의 권화 같은 카드가 만들어져 버렸다.
		PRINTFORMW 뭐야 매지컬자지라니. 원하는 건 용사의 검이지 성스러운 검이 아니라고요.
	ENDIF

	PRINTFORMW 이번에는 여기까지로 해두자…
	PRINTFORMW 

	TIME += 30

	RETURN 2
ENDIF
