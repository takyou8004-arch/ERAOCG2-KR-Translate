@EVENT_COUNTER_MESSAGE(CTおさわり確認)
#DIM DYNAMIC CTおさわり確認
SIF TARGET <= 0
	RETURN 0
CALL DATUI_MESSAGE
RESULT = 0
SIF CTおさわり確認 >= 1
	PRINTFORM 질세라、
TRYCALLFORM EVENT_COUNTE_MESSAGE{TCVAR:TARGET:전CT행동}
;PRINTL
RESETCOLOR
;CUSTOMDRAWLINE —

SIF RESULT && デバッグシステム
	PRINTFORML TCVAR값：{TCVAR:TARGET:CT행동}/전CT행동:{TCVAR:320:전CT행동}




;色っぽい仕草
@EVENT_COUNTE_MESSAGE10
PRINTFORML %CALLNAME:TARGET%는 요염한 몸짓으로 유혹해 왔다…
RETURN 1

;見つめる
@EVENT_COUNTE_MESSAGE11
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%을 촉촉하게 젖은 눈망울로 가만히 바라보고 있다…
RETURN 1

;体を摺り寄せる
@EVENT_COUNTE_MESSAGE12
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%에게 몸을 기대어 왔다…
RETURN 1

;치마めくり
@EVENT_COUNTE_MESSAGE13
PRINTFORML %CALLNAME:TARGET%는 빈틈을 노려 %CALLNAME:MASTER%의 스커트를 걷어 올렸다
IF !EQUIP:MASTER:하반신속옷２
	PRINTFORM 치마 속의 아무것도 입지 않은 하반신
ELSE
	PRINTFORM %PANTS_DESCRIPTION(EQUIP:MASTER:하반신속옷２)%%PANTSNAME(EQUIP:MASTER:하반신속옷２)%
ENDIF
PRINTL  을 들켜버리고 말았다…
RETURN 1

;息を吹きかける
@EVENT_COUNTE_MESSAGE14
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 뒤로 숨어들어 귓가에 뜨거운 입김을 불어 넣었다…
RETURN 1

;엉덩이를 쓰다듬는다
@EVENT_COUNTE_MESSAGE15
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 엉덩이를 쓰다듬어 왔다…
RETURN 1

;耳舐め
@EVENT_COUNTE_MESSAGE16
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 귀를 혀로 핥으며 침으로 흠뻑 적셨다。
RETURN 1

;;발기の上からすりすり
@EVENT_COUNTE_MESSAGE17
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 노장을 신발 위에서 문지르며 놀기 시작했다。
RETURN 1


;키스
@EVENT_COUNTE_MESSAGE30
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴에 다가가、불현듯 입술을 빼앗았다…
RETURN 1

;後ろから抱きつく
@EVENT_COUNTE_MESSAGE31
PRINTFORML %CALLNAME:MASTER%는 등 뒤에서 %CALLNAME:TARGET%에게 안겼다
SIF BUST_SIZE(TARGET) > 0
	PRINTFORML %CALLNAME:MASTER%의 등에 %CALLNAME:TARGET%의 부드러운 가슴이 와닿는다…
RETURN 1

;前ろから抱きつく
@EVENT_COUNTE_MESSAGE32
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%를 꼭 껴안고 
IF TALENT:TARGET:체형 > TALENT:MASTER:체형
	PRINTFORML 그 \@ BUST_SIZE(TARGET) > 0 ? 풍만한 가슴 # 가슴 \@으로 %CALLNAME:MASTER%의 얼굴을 감쌌다…
ELSE
	PRINTFORML 숨을 거칠게 내쉬며 몸을 비비며 다가왔다…
ENDIF
RETURN 1

;가슴を揉ませる
@EVENT_COUNTE_MESSAGE33
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 손을 잡고、자신의 가슴으로 이끌었다
PRINTFORML %CALLNAME:TARGET%의 심장의 고동이 손에서 전해져 온다…
RETURN 1

;股間をまさぐる
@EVENT_COUNTE_MESSAGE34
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 \@ TALENT:MASTER:성별 & 2 ? 가랑이에 # 비소에 \@손을 뻗어 더듬기 시작했다…
RETURN 1

;치마の中を見せる
@EVENT_COUNTE_MESSAGE35
PRINTFORML %CALLNAME:TARGET%는 \@ TALENT:수치심 > 0 ? 쭈뼛쭈뼛 # \@자기 치마를 걷어올려 
PRINTFORM %CALLNAME:MASTER%의 눈앞에 
IF !EQUIP:하반신속옷２
	PRINTFORM 치마 속의 아무것도 입지 않은 하반신
ELSE
	PRINTFORM %PANTS_DESCRIPTION(EQUIP:하반신속옷２)%%PANTSNAME(EQUIP:하반신속옷２)%
ENDIF
PRINTFORML 을 노출했다…
RETURN 1

;後ろから가슴もみもみ
@EVENT_COUNTE_MESSAGE36
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER% 등 뒤에서 가슴을 주물러 왔다…
RETURN 1

;수음
@EVENT_COUNTE_MESSAGE50
IF TALENT:MASTER:성별 & 2
	IF TCVAR:MASTER:갈것같음
		SIF TEQUIP:MASTER:0 & 2 && !TEQUIP:MASTER:3
			TEQUIP:MASTER:3 = 1
		IF TEQUIP:식스나인 >= 1
			PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴에 걸터앉은 채로
		ELSE
			PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴을 바라보며
		ENDIF
		PRINTFORML  격렬하게 남성기를 주물러 사정에 이르게 했다.…
	ELSE
		PRINTFORM %CALLNAME:TARGET%는
		SIF TEQUIP:식스나인 >= 1
			PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER% 얼굴에 걸터앉은 채로 
		IF TEQUIP:MASTER:0 & 2 && !TEQUIP:MASTER:3
			TEQUIP:MASTER:3 = 1
			PRINTFORM 속옷에서 남성기를 꺼내어
		ELSE
			PRINTFORM 노출된 남성기에
		ENDIF
		PRINTFORML  손가락을 얽어 비비기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME:TARGET%는
	IF TEQUIP:MASTER:0 & 2 && !TEQUIP:MASTER:3
		TEQUIP:MASTER:3 = 1
		PRINTFORM %CALLNAME:MASTER%의 속옷을 엇갈리게 해、
	ENDIF
	PRINTFORML 드러난 %CALLNAME:MASTER%의 음부를 손가락으로 파고들었다・・・
ENDIF
RETURN 1

;펠라
@EVENT_COUNTE_MESSAGE51
IF TALENT:MASTER:성별 & 2
	PRINTFORM %CALLNAME:TARGET%는 
	IF TEQUIP:MASTER:0 & 2 && !TEQUIP:MASTER:3
		TEQUIP:MASTER:3 = 1
		PRINTFORM 속옷에서 남성기를 꺼내서
	ELSE
		PRINTFORM 노출된 남성기를
	ENDIF
	PRINTFORML  입에 물고 빨기 시작했다…
ELSE
	PRINTFORM %CALLNAME:TARGET%는 
	IF TEQUIP:MASTER:0 & 2 && !TEQUIP:MASTER:3
		TEQUIP:MASTER:3 = 1
		PRINTFORM %CALLNAME:MASTER%의 속옷을 엇갈리게 해、
	ENDIF
	PRINTFORML 드러난 %CALLNAME:MASTER%의 음부에 혀를 기었다 …
ENDIF
RETURN 1

;스마타
@EVENT_COUNTE_MESSAGE52
IF TALENT:MASTER:성별 & 2
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%에 올라타 페니스를 음부로 문지르기 시작했다…
ELSE
	PRINTFORML %CALLNAME:TARGET%는 두 사람의 성기를 겹쳐 문지르기 시작했다…
ENDIF
RETURN 1

;파이즈리
@EVENT_COUNTE_MESSAGE53
IF TEQUIP:MASTER:0 & 2 && !TEQUIP:MASTER:3
	TEQUIP:MASTER:3 = 1
	PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 속옷에서 남성기를 꺼내어
ELSEIF TEQUIP:MASTER:0 & 2
	PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 노출된 남성기를
ELSE
	PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 남성기를
ENDIF
PRINTFORML  가슴에 끼고 비비기 시작했다…
RETURN 1

;풋잡
@EVENT_COUNTE_MESSAGE54
IF TEQUIP:MASTER:0 > 3 || (TEQUIP:MASTER:0 & 2 && !TEQUIP:MASTER:3)
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 음부를 밟고 문지르기 시작했다…
ELSE
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 남성기를 밟고 문지르기 시작했다…
ENDIF
RETURN 1

;애널애무
@EVENT_COUNTE_MESSAGE55
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 애널을 손가락으로 애무했다…
RETURN 1

;애널舐め
@EVENT_COUNTE_MESSAGE56
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 애널을 정성껏 음미하기 시작했다…
RETURN 1

;손가락삽입
@EVENT_COUNTE_MESSAGE57
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 음부에 천천히 손가락을 삽입했다…
RETURN 1

;가슴애무
@EVENT_COUNTE_MESSAGE58
IF TEQUIP:MASTER:1 & 1 && !TEQUIP:MASTER:4
	TEQUIP:MASTER:4 = 1
	PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 가슴을 풀어헤치고
ELSE
	PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의
ENDIF
IF TALENT:MASTER:성별 & 1
	PRINTFORML  가슴을 마구 주물렀다…
ELSE
	PRINTFORML  젖꼭지를 손가락으로 잡고 만지작거리기 시작했다…
ENDIF
RETURN 1

;유두빨기
@EVENT_COUNTE_MESSAGE59
IF TEQUIP:MASTER:1 & 1 && !TEQUIP:MASTER:3
	TEQUIP:MASTER:4 = 1
	PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 가슴을 풀어헤치고
ELSE
	PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의
ENDIF
PRINTFORML  젖꼭지에 달라붙어 왔다…
RETURN 1

;딥키스
@EVENT_COUNTE_MESSAGE60
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴을 잡고 입을 맞추고、정열적으로 혀를 내두르기 시작했다…
SIF BASE:TARGET:술기운 >= 1
	PRINTFORML 술 맛이 난다……
RETURN 1

;역 레이프
@EVENT_COUNTE_MESSAGE61
IF TEQUIP:TARGET:0 & 2 && !TEQUIP:TARGET:3
	TEQUIP:TARGET:3 = 1
	PRINTFORML %CALLNAME:TARGET%은 속옷을 벗고、 %CALLNAME:MASTER%의 페니스를 음부에 끼워 넣고、허리를 움직이기 시작했다…
ELSE
	PRINTFORML %CALLNAME:TARGET%는、 %CALLNAME:MASTER%의 페니스를 음부에 끼워 넣고、허리를 움직이기 시작했다…
ENDIF
IF TALENT:TARGET:처녀 && TALENT:TARGET:2 >= 1
	PRINTFORML 결합부에서는 %CALLNAME:MASTER%의 처녀혈이 흘러나오고 있다…
ELSE
	PRINTFORML 결합부에서는 %CALLNAME:TARGET%와 %CALLNAME:MASTER%의 체액이 넘치고 있다…
ENDIF
RETURN 1
;耳舐め
@EVENT_COUNTE_MESSAGE62
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 귓가에 소곤소곤 사랑의 말을 중얼거리며、벌겋게 달아오른 귀를 핥았다。
RETURN 1



;커닐링구스 강제
@EVENT_COUNTE_MESSAGE70
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴에 걸터앉아 입가에 음부를 밀어붙였다…
RETURN 1

;펠라치오強制
@EVENT_COUNTE_MESSAGE71
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%입으로 페니스를 봉사하게 했다…
RETURN 1

;애널봉사
@EVENT_COUNTE_MESSAGE72
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴에 걸터앉아 입가에 애널을 밀어붙였다…
RETURN 1

;ぱふぱふ
@EVENT_COUNTE_MESSAGE73
IF BUST_SIZE(TARGET) >= 0
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴을 양쪽에서 %OPPAI_DESCRIPTION(TARGET)%으로 부드럽게 감쌌다…
ELSE
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 얼굴을 안고 %OPPAI_DESCRIPTION(TARGET)%을 밀어붙였다…
ENDIF
RETURN 1

;乳合わせ
@EVENT_COUNTE_MESSAGE74
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 몸에 가슴을 밀어붙여 왔다…
RETURN 1

;タワシ洗い
@EVENT_COUNTE_MESSAGE75
PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 팔에 걸쳐 성기를 문질러 대고 있다…
RETURN 1







;질성교
@EVENT_COUNTE_MESSAGE500
SELECTCASE TCVAR:CT파생
	CASE 1
		PRINTFORML %CALLNAME:TARGET%는 질벽을 움직여 %CALLNAME:MASTER%의 페니스를 압박하기 시작했다…
	;だいしゅきホールド
	CASE 2
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%에게 안기면서 두 다리로 허리를 감싸 안았다…
	;성교見せつけ
	CASE 3
		PRINTFORML %CALLNAME:TARGET%는 상체를 돌려 %CALLNAME:MASTER%에게 결합부를 과시하면서 페니스를 훑고 있다…
ENDSELECT
RETURN 1

;수음
@EVENT_COUNTE_MESSAGE502
IF NOWEX:MASTER:사정
	PRINTFORML %CALLNAME:TARGET%는 넘쳐흐르는 쿠퍼액을 음경 전체에 펴 바르자마자 속도를 한층 높여 음경을 쑥쑥 비비며 훑었다.…
ELSE
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 페니스를 쥐고 천천히 위아래로 움직이고 있다…
ENDIF
RETURN 1

;펠라치오
@EVENT_COUNTE_MESSAGE503
SELECTCASE TCVAR:CT파생
	CASE 1
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 페니스를 물고、입술로 귀두를 비비고 있다…
	;ディープスロート
	CASE 3
		PRINTFORML %CALLNAME:TARGET%은 한계까지 발기한 %CALLNAME:MASTER%의 페니스를 뿌리까지 삼키며 
		PRINTFORML 격렬하게 머리를 움직이고 있다…
ENDSELECT
RETURN 1

;파이즈리
@EVENT_COUNTE_MESSAGE504
SELECTCASE TCVAR:CT파생
	CASE 1
		IF BUST_SIZE(TARGET) > 0
			PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 페니스를 가슴에 끼고 있다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 페니스에 젖꼭지를 문질러 대고 있다…
		ENDIF
	;縦파이즈리,乳ファック
	CASE 2
		PRINTFORML %CALLNAME:MASTER%는 %CALLNAME:TARGET%의 풍만한 가슴 사이로 페니스를 밀어넣고 허리를 흔들었다…
	;谷間を作る
	CASE 3
		IF BUST_SIZE(TARGET) < 0
			PRINTFORML %CALLNAME:TARGET%는 납작한 가슴을 열심히 기대어 어떻게든 페니스를 끼우려고 한다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%는 가슴을 기대고 만든 계곡에서 페니스를 끼우고 상체를 격렬하게 움직이고 있다…
		ENDIF
ENDSELECT
RETURN 1

;스마타
@EVENT_COUNTE_MESSAGE505
SELECTCASE TCVAR:CT파생
	CASE 1
		PRINTFORML %CALLNAME:TARGET%는 허벅지에 %CALLNAME:MASTER%의 페니스를 끼고 허리를 움직였다…
	;수음추가
	CASE 2
		PRINTFORML %CALLNAME:TARGET%는 허벅지에 %CALLNAME:MASTER%의 페니스를 끼다 남은 귀두를 손가락으로 몰아세웠다…
ENDSELECT
RETURN 1

;玉揉み
@EVENT_COUNTE_MESSAGE600
IF TCVAR:CT파생 == 1
	IF SOURCE:가학
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 음낭에 손을 뻗어 주물러 왔다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 음낭에 손을 뻗쳐 부드럽게 주물러 왔다…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:TARGET%은 음낭을 물고 입안에서 데굴데굴 구슬을 굴리고 있다
ENDIF
RETURN 1

;애널애무
@EVENT_COUNTE_MESSAGE601
IF ABL:MASTER:Ａ감각 < 2
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 애널을 손가락으로 자극하고 있다…
ELSE
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 애널에 손가락을 끼워 직장까지 휘저었다…
ENDIF
RETURN 1



@DATUI_MESSAGE
VARSET LOCAL
IF TCVAR:TARGET:22 == 1
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 하반신의 옷을 속옷째 벗겨냈다
	LOCAL = 1
ELSEIF TCVAR:TARGET:22 == 2
	IF TEQUIP:MASTER:0 & 1
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 스커트 속에 손을 넣어 
		PRINTFORML 안에 입고 있는 속옷을 벗겨냈다
	ELSE
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 하반신의 옷을 속옷째 벗겨냈다
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:22 == 3
	IF TEQUIP:MASTER:0 & 1
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 스커트 속에 손을 넣어 
		PRINTFORML 안에 입고 있는 속옷을 벗겨냈다
	ELSE
		PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 하반신의 옷을 속옷째 벗겨냈다
	ENDIF
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT 또한
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:23 == 1
	PRINTFORML %CALLNAME:TARGET%는 하반신의 옷을 속옷째 벗어 던졌다
	LOCAL = 1
ELSEIF TCVAR:TARGET:23 == 2
	IF TEQUIP:TARGET:0 & 1
		PRINTFORML %CALLNAME:TARGET%는 치마 속에 입고 있는 속옷을 벗어 던졌다
	ELSE
		PRINTFORML %CALLNAME:TARGET%는 하반신의 옷을 속옷째 벗어 던졌다
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:23 == 3
	IF TEQUIP:TARGET:0 & 1
		PRINTFORML %CALLNAME:TARGET%는 스커트 속에 입고 있는 의상을 벗어 던졌다
	ELSE
		PRINTFORML %CALLNAME:TARGET%는 하반신의 옷을 벗어 던졌다
	ENDIF
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT 또한
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:24 == 1
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 상반신의 옷을 속옷째 벗어 던졌다
	LOCAL = 1
ELSEIF TCVAR:TARGET:24 == 2
	PRINTFORML %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 상반신의 옷을 벗어 던졌다
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT さらに
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:25 == 1
	PRINTFORML %CALLNAME:TARGET%는 상반신의 옷을 속옷째 벗어 던졌다
	LOCAL = 1
ELSEIF TCVAR:TARGET:25 == 2
	PRINTFORML %CALLNAME:TARGET%는 상반신의 옷을 벗어 던졌다
	LOCAL = 1
ENDIF
